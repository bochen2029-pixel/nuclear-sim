# PROGRESS — Living Project State

> Maintained by every session per `README.md` §5. Claim = branch + PROGRESS edit + pushed `claim:` commit.
> Status ∈ `todo | in_progress | done | blocked`. A task is **runnable** iff `todo` AND all `depends_on` are `done`.

## Current

- **Milestone:** M1 — CPU reference transport + bare-sphere benchmarks (M0 complete; M1-T1..T3 + T4a-1 done). **M4 in progress in parallel (ADR-009); SYNC-M4 run; M4-T1 + M4-T2-a + M4-T2-b + M4-T3-a + M4-T4-a done; M4-T4-b done (negative result — see below) — the GPU event-based transport AND k-eigenvalue power iteration are live and differentially validated against the CPU oracle, with a profile-first perf baseline (Nsight FP64 fraction 0.00%; k_step divergence-free; VRAM ≤92 MiB @1e6). M4-T4-b MEASURED that swapping the eigen to the event model regresses it 2–6× (per-superstep overhead × many generations), so the eigen keeps its history-per-thread transport; the real fix is a megakernel (M4-T4-c, low priority). **M3-T1 done — α-mode kinetics (E3a) core is live (`physics/kinetics`).** **M2-T1 done — 6 canonical Trinity materials + `trinity_canonical.toml` (cited, masses ≤3%).** **M2-T2 done — Tier-1 compression.** **M3-T2 done — Tier-2 hydro (Guderley derived-t_c + energy-conserving E4 snowplow ODE); found + fixed the E4 sign defect (ADR-019).** SYNC-M2 + SYNC-M3 run. **M3-T3-a done — the tally module (`physics/tally`): `TallyResult` (03 §5) + all 9 `tally_invariants` + JSON, green on the 03 §5 normative fixture.** **M3-T3-b done — the `02 §3` coupling-loop machinery (`physics/couple`): `run_burst` + streaming `TallySink`/`BurstTally` → `TallyResult`, tier-1 hydro coupling (E3b Λ rescale + refresh-on-motion) + E6 quench (BLK-03) + integer-counter refresh + R-13 auto-halving; toy-tested to all 9 `tally_invariants` via an injectable `EigenFn`.** **M3-T3-c done — real-transport `EigenFn` adapter (`ref_eigen_fn`) + per-generation fission-site stream (`GenerationSample.sites`): `run_burst` now runs a GENUINE Monte-Carlo eigen and streams real spatial fission SITES — the volumetric chain-reaction tap for the visualizer, fast4-independent.** **M3-T3-d done — the REACTIVITY HANDLE: mass-conserving density-aware transport (`MaterialLib::with_density_scale` + `ref_eigen_fn_masscons`); a real MC eigen now shows compression IGNITES (k↑) and disassembly QUENCHES (k↓) — the physics that makes the burst self-limit (step 3 part 1).** **M3-T3-e done — the demon-core burst now SELF-LIMITS EMERGENTLY: `SnowplowShell` disassembly (E4 + E5 deposition) in `run_burst`, driven by the mass-cons eigen — a real MC supercritical Pu-sphere burst ignites → peaks → disassembles → quenches on its OWN (finite yield, k_quench < k_peak), no scripted stop. STEP 3 COMPLETE.** **M3-T3-f done — STEP 4 (the fusion API) STARTED: the `03 §6` run.json provenance contract in the NEW `src/api/` nscore fusion-API module (`RunProvenance` + `to_json`/`parse_run_json` with a required-field parse contract + `compute_unit_id` = sha256(canonical_hash ‖ overrides ‖ seed), order-independent); green on the `03 §6` example read from the spec markdown (the M3-T3-a pattern). `src/api/` is nscore's studio/binding surface — it composes physics, so it is NOT a frontend (D7). fast4-independent.** **M3-T3-g done — the fusion API's evaluate GAUGE (`src/api/studio.{h,cpp}`): `DemonCoreAssembly` (a bare Pu sphere from `pit.mass_kg` + the C-101/C-102-derived δ-Pu density, SIM Pu239/Pu240 xs — the `RealSphere` file-based template) + `evaluate(cfg) → {k_eff, k_prompt, sigma_pcm, ready}` via `ref_eigen_fn_masscons` at the compressed geometry + the `evaluate_json` cfg seam. A REAL Monte-Carlo gauge whose k rises with mass/compression and falls with Pu-240 (5 `api.` tests, common-random-numbers-verified); `ready` = prompt-supercritical. fast4-independent.** **M3-T3-h done — STEP 4 COMPLETE: `generate_run(cfg)` (`src/api/studio.{h,cpp}`) — the disassembly `run_burst` from the COMPRESSED pit (eigen r_ref = uncompressed r0) + `BurstTally` + a `GenerationSample`-collecting sink + `RunProvenance` → `{tally (03 §5), run (03 §6), samples}` JSON. Verified end-to-end: a compressed heavy pit ignites (k_peak > 1) → disassembles → quenches (k↓) with a finite yield (detonate), while an uncompressed light pit fizzles — detonate-vs-fizzle EMERGENT from k, not simstub's hardcoded rule. The `samples`/`sites` stream is the volumetric chain-reaction tap. Reconciled the `03 §4`↔`§6` `scenario_overrides` gap (clarification). The WHOLE demon-core fusion API (evaluate + generate_run + run.json) is now real, emergent, and drop-in-ready for the viz.** **M5-T1-a done — M5 STARTED (checkpoint/resume, D9): SYNC-M5 run; the `checkpoint.bin` v2 CONTAINER (`src/core/checkpoint/`) — a 128-B header + section table + **in-tree CRC32** + the **R-7 load-rejection rules** (a mismatched checkpoint never loads silently) + `CheckpointBlob`/`SimClockState` (03 §8). Delivers the `CheckpointBlob` type `05 §4`/`couple.h` deferred → partially unblocks M3-T4. 3 `checkpoint.` tests (CRC32 IEEE vector, container round-trip, every R-7 rejection class).** **M5-T1-b done — the `BurstAccumulator` checkpoint serialize/restore: a `State` snapshot (`{mant,off,log_fcum,log_ecum,log_flast,n,hist}`) + `state()`/`from_state()` + a bit-exact section codec (`serialize_accumulator_state`, physics-side so core stays physics-free) — a real burst accumulator, stepped ~40 decades then checkpointed THROUGH the M5-T1-a container, restored, and continued **bit-identically** (==) to an uninterrupted run through peak/decay/quench. The resume primitive for the T-resume gate (M5-T1-c extends it to the whole `run_burst` loop).** **M5-T1-c done — M5-T1 COMPLETE (checkpoint/resume v2 + T-resume, D9): the full `run_burst` bit-identical resume + the **T-resume gate**. `BurstCheckpoint` (loop state) + `BurstTally::State` (the sink) + `serialize_burst_state`; `run_burst` gains a `BurstResume*` (checkpoint at a refresh boundary → resume, re-deriving the eigen + reconstructing geometry from geom0+shell/t). PROVEN: a real SIM demon-core disassembly burst, killed at a refresh boundary near the middle + serialized THROUGH the `checkpoint.bin` container, resumes to a tally that is BIT-IDENTICAL (same JSON) to the uninterrupted run — same generations/eigen_calls/quench. The whole checkpoint/resume stack (container → accumulator → tally → run loop → T-resume gate) is built + green.** **M5-T2 done — the ARTIFACT STORE (`03 §7`): a SQLite `sweep.db` results store (`src/core/store/`) — `runs(unit_id PK, params_json, tally_json, wall_s, status)` + `cursor(state_json)`, created verbatim — with `record_run` `unit_id`-keyed INSERT-OR-IGNORE idempotency (a rerun / second-finisher write is a NO-OP, D6/D8) + the singleton sweep `cursor` (resume). Core infra keyed on an opaque `unit_id` string (depends only on sqlite3, NOT on `src/api`); 7 `store.` tests incl. on-disk reopen + injection-safe prepared-statement binding. Consumes M3-T3-f's `unit_id`; feeds M5-T3's sweep engine.** **M5-T3-a done — the SWEEP-PLANNING FRONT (`03 §7`/`06 §2`; the FIRST `src/app/` frontend): the `nukesim_nukefarm` lib (`src/app/nukefarm/`) — the sweep.toml loader + the axis_class/ScoreKind/objective ENFORCEMENT (MAJ-35, the mechanical `00 §3.4` "explain-don't-search" boundary: an uncertainty axis ⊆ its constant band, pedagogical only with grid+sensitivity+≤100 runs, calibrate all-physical + a target band, OptimizeExtremum rejected at load) + the `06 §2` `Sampler` interface + the grid/lhs/random space-filling samplers (deterministic; lhs/random seeded by a stable name hash). 12 `nukefarm.` tests. D7-clean (a frontend scoring a `TallyResult`, no physics). Feeds M5-T3-b's store-backed run loop.** **M5-T3-b done — the store-backed SWEEP RUN-LOOP ENGINE (`src/app/nukefarm/runner.{h,cpp}`): `run_sweep` drives the sampler points → cfg → `ns::api::studio_unit_id` (now exposed on the studio API) → the M5-T2 `SweepStore` (`is_done` SKIP = resume; `record_run` INSERT-OR-IGNORE = the no-double-count backstop); the evaluator is injectable (the fast4-independent `generate_run` for real). 3 `nukefarm.` tests: a local sweep records each unit once; partial-resume runs only the remainder + a re-run skips all; a real-burst smoke proves the up-front unit_id == the recorded run's. M5-T3-c adds the FS work-queue + lease/reclaim + the CLI.** **M5-T3-c done — the FS WORK-QUEUE library (`06 §2`, `src/app/nukefarm/queue.{h,cpp}`): `WorkQueue` over `pending→claimed→done` + lease files (`enqueue`/`claim`/`complete`/`reclaim_stale`), keyed by `unit_id`, deterministic (time + threshold are params → policy-free, no ADR). Coordination = atomic filesystem renames (no lock server). 4 `nukefarm.` tests incl. the DoD reclaim clause — an induced stale-lease reclaim re-runs a unit and the late finisher's `record_run` is a NO-OP (M5-T2 store idempotency); no double-count. M5-T3-d adds the distributed worker + the submit/worker/status/resume CLI + the 2×-median threshold policy (+ the `06 §2` `t_max_s`-fallback ADR).** **M5-T3-d done — the distributed-sweep CORE (`src/app/nukefarm/worker.{h,cpp}`): **ADR-020** fixed `06 §2`'s stale-lease fallback (a wall-clock 600 s default, NOT the sim-time `3× t_max_s` ≈ 15 µs that thrashed the queue); `stale_threshold_s` (2×-median of the last 10 `wall_s`, else the fallback; needs `SweepStore::recent_wall_s`, added) + `submit` (enqueue the sampler points by `unit_id`, skips already-done → resumable) + `run_worker` (reclaim→claim→eval→`record_run`→complete; `record_run` idempotency makes a reclaimed unit run twice a no-op). 4 `nukefarm.` tests. M5-T3-e = the `nukefarm` CLI exe (the FIRST `src/app/` executable) wrapping these.** **M5-T3-e done → M5-T3 COMPLETE (a–e): the `nukefarm` CLI exe (the FIRST `src/app/` executable — `src/app/nukefarm/main.cpp` + the testable `cli.{h,cpp}`): CLI11 `submit`/`worker`/`status`/`resume` over `cli_submit`/`cli_worker`/`cli_status`, a real `std::chrono` `wall_clock_seconds()` feeding `run_worker`'s clock seam, `--stale-lease-s` (ADR-020), `06 §5` exit codes. 2 `nukefarm.` tests + a live `nukefarm status` smoke. The WHOLE M5 batch-sweep pipeline — planning → run-loop → FS queue → distributed worker + policy → CLI — is now a runnable command; M5-T3's DoD (100-run local sweep + resume + no-double-count-under-reclaim) is closed end-to-end.** **M5-T4 done — the MCTS/PUCT sampler (`src/app/nukefarm/samplers/builtin.cpp`, `MctsSampler`): a PUCT tree over the parameter box (k-d bisection + progressive widening) behind the `06 §2` Sampler interface, `BandCenter` scoring `-|yield_kt − band_center|` toward the calibrate target; `make_sampler("mcts")` requires a calibrate band (OptimizeExtremum still rejected; pedagogical+mcts loader-rejected). 3 `nukefarm.` tests incl. the DoD convergence (mcts beats the seeded random sampler on a synthetic 2-param paraboloid over 300 evals). **M5 is now 4/5 (T1, T2, T3, T4); only M5-T5 (container + cloud dry-run) remains.**** **STRATEGIC DIRECTION (owner-delegated 2026-08-03): build toward the `viz/js/simstub.js` fusion contract (`evaluate`/`generateRun`), the demon-core chain-reaction pipeline FIRST — steps 1-2 done (real transport + fission sites); next step 3 (self-limiting disassembly) + step 4 (the fusion API). The owner's `fast4` xs (M1-T4a-2) is the CREDIBILITY unblock (the benchmark gates) + full-device transport — no longer the gate to the demon-core spectacle.**
- **VERIFY:** `cmake --preset win-x64 && cmake --build --preset win-x64-rel && ctest --preset win-x64-rel` — the `12 §2` canonical loop; falsifiable and real (configure + compile + link + tests). **On the dev machine (CUDA auto-on): 171 passing = 158 CPU + 13 `gpu.` (on the RTX 4070 Ti SUPER). On a CPU-only build / the CI runners (`NUKESIM_WITH_CUDA` OFF): 158.** Takes ~1 min warm. Per-task probes are **anchored** (`11 §1`): `ctest --preset win-x64-rel -R "^<module>\."` for module ∈ {toolchain, constants, oracle, rng, loaders, geometry, ref, eigen, benchmarks, gpu, kinetics, tier1, tier2, tally, couple, api, checkpoint, store, nukefarm} — all resolve and are disjoint. `^gpu\.` selects 13 and only registers when `NUKESIM_WITH_CUDA` is ON. Disjoint **and** exhaustive still holds: the anchored counts sum to the total — 6+11+1+10+11+15+7+9+5+13+7+5+5+5+9+14+3+7+28 = 171 (or 158 on a CPU-only build; `^kinetics\.` selects 7, `^tally\.` selects 5, `^couple\.` selects 9, `^api\.` selects 14, `^checkpoint\.` selects 3, `^store\.` selects 7, `^nukefarm\.` selects 28). The dev build also produces the **non-test** binaries `gpu_perf` (M4-T4-a, `NUKESIM_WITH_CUDA`-guarded) and the `nukefarm` CLI (M5-T3-e), plus the committed baseline `artifacts/perf_history.jsonl`; none is a ctest, so the 171 count is unchanged.
- **NEXT ACTION:** **M5-T4 is COMPLETE** — the MCTS sampler (`src/app/nukefarm/samplers/builtin.cpp`): a PUCT tree over the parameter box (k-d bisection + progressive widening) behind the `06 §2` `Sampler` interface, `score_kind() == BandCenter` scoring `-|yield_kt − band_center|` toward the calibrate target; `make_sampler("mcts")` requires a calibrate band (`OptimizeExtremum` still rejected). 3 `nukefarm.` tests incl. the DoD convergence (mcts beats random on a synthetic 2-param landscape) + the pedagogical / no-band rejections. **M5 is now 4/5 (T1, T2, T3, T4).** Execute **M5-T5** — the container + cloud dry-run (`07 §M5`, the last M5 task): a Dockerfile running `nukefarm worker` whose base image is pinned by a VERIFIED tag+digest (`12 §3` + the ONBOARDING §9 standing obligation — verify the `nvidia/cuda:13.1-devel-ubuntu22.04` tag/digest BEFORE writing it) + a cloud dry-run. **Heads-up: base-image tag/digest verification likely needs registry/network (and the dry-run, docker) — if that tooling isn't available to an autonomous session, surface it to the owner (like `fast4`) rather than inventing a digest.** See `spec/12-deployment.md §3` + `spec/06-frontends.md §2`; DoD in `07-milestones.md`. The two **owner forks** remain the highest-value steps but are owner-only: the **fusion BINDING MECHANISM** (wire the done `src/api/` `evaluate`/`generate_run`/run.json to `viz/`: WASM vs native addon vs local server) and **`fast4`** (M1-T4a-2 — step 5 + the credibility gates + real non-SIM yields). **M4-T4-c** (megakernel eigen) stays a low-value/speculative perf alternative; **M3-T4** can now build `HydroModel::serialize()` on the real `CheckpointBlob` but still needs `fast4` for its canonical t_fire sweep.

> **Handoff snapshot (READ FIRST if you are a fresh session):** `docs/HANDOFF-2026-08-04.md` — a comprehensive, warm onboarding + continuation prompt written at the end of `session-2026-08-03-d` (which shipped M5-T2 + M5-T3-a…e + M5-T4 = the whole batch-sweep/experiment infrastructure D8, + ADR-020, + the visualizer gallery, + **both halves of the fusion binding** now working end-to-end from the browser). It orients you fully (the project's soul + distinctions, exact state @ `ed19523`/171 tests, **M5 is 4/5**, the owner/-e-gated forks — `fast4` + the viz `main.js` swap — where to look, and every trap) before you touch anything. `docs/HANDOFF-2026-08-03-c.md` (+ -b, base) are prior context. This file (PROGRESS) + SESSIONS remain the authoritative living state.

## Ready-queue (runnable now)

1. **M1-T4a-2** (owner) — the cited `fast4` xs data. **THE dominant unblock** (the fast4-independent frontier is now exhausted): opens M1-T5, all formal gates, M2-T3, the canonical burst bundle (now just wiring the real `run_eigen` `EigenFn` + `make_hydro` into the done M3-T3-b `run_burst` on `trinity_canonical`), and the remaining G4.
2. **The fusion BINDING — STARTED 2026-08-04 (the C++ half DONE).** The mechanism (owner-delegated) was chosen: a **local dev-server over a CLI bridge** — not WASM (no Emscripten on the box + loses CUDA) and not a native addon yet (the Steam/M7 path). The C++ (main-track) half is done + proven LIVE: **`studio_bridge`** (`src/app/studio_bridge/`, a non-test exe) exposes `evaluate` / `generate-run` as cfg-JSON → result-JSON (wrapping the done `evaluate_json`/`generate_run_json`), and **`tools/studio_server.py`** (stdlib, no deps) serves it over HTTP — a 9 kg / 2.5× pit detonates → 1.62 kt + the 628-gen sample/site stream, fetched over HTTP, zero new C++ deps. **What's LEFT is the -e track's domain** (see `docs/FUSION-BINDING.md`): replace `viz/js/simstub.js` with a JS adapter that `fetch`es `studio_server` (debounced — a real eigen is seconds) + reconstructs the `compression(t)`/`flux(t)` closures; `main.js` unchanged. All three mechanisms wrap the SAME `evaluate_json`/`generate_run_json` surface, so WASM/native are later transport swaps, not rewrites. (STEP 4 — the demon-core fusion API M3-T3-a…h — landed green 2026-08-03; the fast4-independent demon-core frontier is exhausted.) Other runnable-autonomous alternatives: **M5-T5** (container base tag/digest verify + `nukefarm worker` Dockerfile + cloud dry-run, `07 §M5`/`12 §3` — the LAST M5 task; **may need registry/docker tooling not available to an autonomous session → surface to the owner if so**) now that **M5 is 4/5 (T1, T2, T3, T4 done)**: the whole batch-sweep pipeline — store, planning front + grid/lhs/random + the MCTS/PUCT sampler, run-loop engine, FS work-queue, distributed worker + ADR-020 policy, the `nukefarm` CLI — all done + green (171 dev / 158 CI). Also runnable: **M4-T4-c** (megakernel eigen, perf, low-value/speculative). **M3-T4** (initiator timing) can build `HydroModel::serialize()` on the real `CheckpointBlob` but still needs `fast4` for its canonical t_fire sweep (found session-2026-08-03-c).
3. **M4-T4-c** — cooperative-groups megakernel eigen (M4-T4-b's divergence fix without per-superstep overhead). Fully unblocked but **perf-only, low priority** (eigen not a bottleneck until `fast4`).
4. *(**Blocked on M1-T4a-2** (owner cited-data): M1-T5 nukebench/gates → the formal gate reports incl. **M4-T3-b** (G0c) and G0a/G0b; the M2-T3 k-scan; the canonical burst bundle (real `EigenFn` on `trinity_canonical`); and the remaining G4 criteria. The critical path for gate work.)*

**Repository:** <https://github.com/bochen2029-pixel/nuclear-sim> — public, MIT (ADR-011). Remote exists, so the full claim protocol (`README §5.3`: branch + PROGRESS edit + pushed `claim:` commit) applies from M0-T2 onward and parallel sessions are now safe.

## Gates

| Gate | Status | Evidence (gate_report.json path) | Date |
|---|---|---|---|
| G0a | not_run | — | — |
| G0b | not_run | — | — |
| G0c | not_run | — | — |
| G1a | not_run | — | — |
| G1a-tight (report-only) | not_run | — | — |
| G1b | not_run | — | — |
| G2  | not_run | — | — |
| G3  | not_run | — | — |
| G4  | not_run | — | — |
| G5  | not_run | — | — |

## Tasks

| ID | Status | claimed_by | claimed_at | depends_on | Notes |
|---|---|---|---|---|---|
| M0-T1 | **done** | session-2026-08-03-a | 2026-08-03 | — | repo init in place (BLK-12); MIT LICENSE + NOTICE; `.gitignore` gate-evidence un-ignores verified both ways (QC-07); pushed to github.com/bochen2029-pixel/nuclear-sim |
| M0-T2 | **done** | session-2026-08-02-b | 2026-08-02 | M0-T1 | canonical loop green from a clean build dir + cold vcpkg cache; 6/6 ctest incl. a real CUDA kernel round-trip. Generator `Visual Studio 17 2022` (win-x64), triplet `x64-windows-static`, baseline `d59284957…` — all recorded in `12 §1` |
| M0-T3-a | **done** | session-2026-08-02-b | 2026-08-02 | M0-T2 | 96 strict entries ↔ 96 appendix rows, bijective; `[[band]]` added (ADR-015); Φ_kt recomputed = 1.4508041e23; 26 validator guards; 15/15 ctest |
| M0-T3-b | **done** | session-2026-08-02-b | 2026-08-02 | M0-T3-a | all 6 `11 §5` sections; 9/9 tally invariants pass on the canonical example (I3 7.5e-8 independently reproduces the QC session's figure); Tier-1 endpoints exact, mass conservation 1.5e-16; §2 layer masses correctly PENDING(M2-T1); found the C-042/C-043 blanket-"~5%" wording defect |
| M0-T4 | **done** | session-2026-08-02-b | 2026-08-02 | M0-T2 | 3 published Random123 vectors reproduce (also at compile time); project-local vector emitted by an INDEPENDENT Python Philox, not self-recorded; fork + (ctr,sub) resume KATs; 10 tests |
| M0-T5 | **done** | session-2026-08-02-b | 2026-08-02 | M0-T2, M0-T3-a | xs v2 / materials / scenario loaders; positive tests parse the SPEC'S OWN examples extracted from `03` at run time; one negative test per violation class; 11 tests |
| M0-T6 | **done** | session-2026-08-02-b | 2026-08-02 | M0-T5 | `tools/ci/local_ci.{ps1,sh}` + `.github/workflows/ci.yml`; Actions green on `main` (windows-2022 + ubuntu-latest + gate-evidence archive); `.env.example` committed |
| M1-T1 | **done** | session-2026-08-02-b | 2026-08-02 | M0-T5 | LayerStack + AnalyticSphereTracker + in-tree SHA-256 + `canonical_hash()` (carried per SYNC-M1); 16 tests incl. the nudge-direction pair and the `04 §6` stability matrix |
| M1-T2 | **done** | session-2026-08-02-b | 2026-08-02 | M1-T1 | E1a–E1e implicit capture; leakage = exp(−Σ_c·R) at 3 depths + a 16-seed bias check; k_inf within 3σ on 3 media; 7 tests |
| M1-T3 | **done** | session-2026-08-02-b | 2026-08-02 | M1-T2 | power iteration + 8³ entropy + dual σ + Λ; **measured that C-901's h_tol=1e-3 is correlation-limited, not batch-limited** — M1-T5 must re-measure at C-900 before G0a; 9 tests |
| M1-T4a-1 | **done** | session-2026-08-02-b | 2026-08-02 | M0-T5 | Godiva + Jezebel from JEFF Report 16 / CSEWG F5+F1 (open); atom densities primary, all derived values cross-check the spec's old placeholders; ADR-016/017; `08 §1` placeholders replaced; 5 tests |
| M1-T4a-2 | **blocked** | owner | 2026-08-02 | M1-T4a-1 | **BLOCKED (session-2026-08-02-c): needs owner-provided cited data.** Isotope-complete 4-group `fast4` dataset — ~450 physical values needing a published multigroup library or ENDF via a documented collapse (D4 carve-out); no NJOY/ENDF tooling or data present locally, BLK-14 forbids ICSBEP. Unblock = owner supplies a citable multigroup library **or** authorises/installs an ENDF+collapse pipeline. MUST NOT be invented. |
| M1-T4b | blocked | owner | — | owner ICSBEP access | OPTIONAL, owner-gated; autonomous sessions MUST NOT claim |
| M1-T5 | todo | — | — | M1-T3, M1-T4a | nukebench + gen_gates → gates.toml |
| M2-T1 | **done** | session-2026-08-02-d | 2026-08-02 | M1-T4a-1 | 6 canonical materials (`data/materials/`: pu_ga_delta, u_natural, aluminum, he_compb, b10_acrylic[SIM B-10 loading], be_po_urchin) + `trinity_canonical.toml` + `README.md`. Atom fractions from cited compositions (appendix §2/§3 + research doc), Σ=1 exact. **oracle.py §2 mass check** (xs-free): pit −0.02% / tamper +0.42% / pusher in-band / HE booster −0.91% vs appendix, all ≤3%; 0 findings. `constants_roundtrip` covers all species' molar masses. 94/94 (oracle+constants green). Σ-dependent uses (M2-T2 k-scan, G1/G2) await `fast4` |
| M2-T2 | **done** | session-2026-08-02-d | 2026-08-02 | M2-T1 | Tier-1 parametric compression (`src/physics/hydro/tier1.*`): `smootherstep`, `Tier1Compression` (radius_scale / density_ratio / s_of), `compress(LayerStack)`. DoD met: endpoints to 1e-12 (r_0 at s=0, r_0·ratio^(-1/3) at s=1), **mass conservation to 1e-12 at s∈{0,.25,.5,.75,1}** (BLK-01 test). 5 `tier1.` tests → 99 dev / 86 CI. Full `HydroModel::step` coupling deferred to M3-T2/T3 (deps absent, SYNC-M1) |
| M2-T3 | todo | — | — | M2-T2 | k-vs-compression scan |
| M3-T1 | **done** | session-2026-08-02-d | 2026-08-02 | M1-T3 | α-kinetics core (`physics/kinetics`): E3a `BurstAccumulator` (log-domain N + cumulants, overflow-proof), `nu_eff` = Σprod/Σfiss (SYNC-M3: added `by_isotope_fissions` to `ref::FissionSource`), `isotope_fission_shares`, `rossi_alpha` (E3c), `refresh_q` (R-13). DoD tests: closed-form F_n (k=2,ν̄=2.9), renorm×3 invariance, S_n source term, + real mixed Pu/U-238 eigen (per-isotope prod=ν̄·fiss, ν̄_eff∈(2.5,2.9)). 6 `kinetics.` tests → 94 dev / 81 CI. Full `run(HydroModel&,TallySink&)` deferred to M3-T3 (deps absent, SYNC-M1 rule) |
| M3-T2 | **done** | session-2026-08-02-d | 2026-08-02 | M2-T2 | Tier-2 hydro (`src/physics/hydro/tier2.*`): `GuderleyTiming` (derived t_c, MAJ-09; constructor matches r_lens_inner + v_in to 1e-9) + `SnowplowShell` energy-conserving E4 ODE (RK4). **Found + fixed the E4 sign defect (ADR-019):** `M·R̈ = 4πR²(P_int − P_drive)` (was `P_drive − P_int`, which did not conserve). DoD met: E_int+½MṘ² conserved <1e-6 over 1e4 steps, no-motion, constructor, α_G=C-070. 5 `tier2.` tests → 104 dev / 91 CI. Full `HydroModel`/coupling → M3-T3 |
| M3-T3-a | **done** | session-2026-08-03-b | 2026-08-03 | M3-T1, M3-T2 | SPLIT from M3-T3: `src/physics/tally/{tally.h,tally.cpp}` — `TallyResult` (03 §5 schema v1) + all 9 `tally_invariants` + `to_json`/`parse_tally_json` (nlohmann ordered). DoD met: **all 9 green on the 03 §5 normative fixture** (QC-04 / 11 §4, read drift-free via `spec_examples::tally_example()`); ports `oracle.py` §6 (independent agreement; I3 7.5e-8, I4 3.8e-8). Also per-invariant negative detection, JSON round-trip idempotence, the never-merged-burnup parse contract (inv 6b), and the QC-05 per-isotope-molar-mass point. 5 `tally.` tests → **109 dev / 96 CI**. Constants from `ns::consts` (C-041/C-916/C-102/C-910-919); `t_max_s` from the 03 §4 example (5e-6). The *canonical* end-to-end bundle is M3-T3-b (needs `fast4`) |
| M3-T3-b | **done** | session-2026-08-03-b | 2026-08-03 | M3-T3-a | SPLIT from M3-T3: `src/physics/couple/{couple.h,couple.cpp}` — the 02 §3 α-mode coupling-loop MACHINERY. `run_burst` drives `BurstAccumulator` (E3a) from an injectable `EigenFn`'s `k_prompt()`/Λ/ν̄_eff with the **integer-counter** refresh cadence + tier-1 radius-motion trigger + R-13 auto-halving; tier-1 hydro coupling (E3b Λ density-rescale); streaming `TallySink`/`BurstTally` (the viz seam, 05 §5) → `TallyResult` via log-sum-exp per-isotope/shell accumulation; E6 quench (`F_n < ε_quench·F_peak`, C-909, NOT k=1 — BLK-03) with the pre/post-peak split. DoD met: 5 `couple.` tests → **all 9 `tally_invariants` green on a scripted-burst tally** + cadence + E3b Λ-rescale + subcritical + reconciliation; 114 dev / 101 CI. SYNC-M1 deferrals (documented): full `05 §4` `HydroModel`/`make_hydro`/`CheckpointBlob` (M5), tier-2 E4 disassembly + E5, `at_peak_compression` initiator (M3-T4). The *canonical* bundle (real `run_eigen` `EigenFn` on `trinity_canonical`) is fast4-gated (M1-T4a-2) |
| M3-T3-c | **done** | session-2026-08-03-b | 2026-08-03 | M3-T3-b | Real-transport `EigenFn` adapter `ref_eigen_fn` (`run_eigen` over a `RefTransport` rebuilt per geometry — the loop now runs a GENUINE Monte-Carlo eigen, not a scripted stub) + per-generation **fission-site stream** (`GenerationSample.sites`: a stratified, capped sample of `ref::FissionSource::sites` — pos/isotope/layer — fresh at the initial eigen + each refresh; the volumetric CHAIN-REACTION tap for the visualizer's demon-core mode). DoD met: 6 `couple.` tests (was 5); the new one runs a real MC eigen on a loader-built Pu sphere and asserts the streamed sites are real positions INSIDE the sphere, isotope-tagged, ≤ the cap. 115 dev / 102 CI. **fast4-INDEPENDENT** (real transport on synthetic-real xs; cited-real when `fast4` lands). Strategy steps 1-2 toward the `viz/js/simstub.js` fusion contract. |
| M3-T3-d | **done** | session-2026-08-03-b | 2026-08-03 | M3-T3-c | STRATEGY step 3 (part 1 — the reactivity handle): mass-conserving density-aware transport. `MaterialLib::with_density_scale(factor)` (Σ ∝ ρ; scales `density` + `macro`) + `ref_eigen_fn_masscons` (infers the uniform scale s = r/r_ref, applies ρ/ρ₀ = s⁻³). DoD met: 7 `couple.` tests; the new one runs a real MC eigen and shows **k_compressed > k0 > k_expanded** — compression IGNITES, disassembly QUENCHES (the opposite of a fixed-density sphere, which reads backwards). fast4-INDEPENDENT. The `SnowplowShell` dynamics that DRIVE this → M3-T3-e. |
| M3-T3-e | **done** | session-2026-08-03-b | 2026-08-03 | M3-T3-d | STRATEGY step 3 (part 2): the emergent self-limiting burst. `SnowplowShell` E4 (M3-T2) + E5 impulse deposition wired into `run_burst` (CoupleConfig `disassembly` mode): each gen deposits `E_n` = F_n·`e_f_joules` (Joules) into `E_int`, RK4-steps the shell over Λ, feeds R → the geometry that `ref_eigen_fn_masscons` dilutes (ρ = ρ₀·(r₀/R)³); density-ratio for the Λ rescale made mode-agnostic. DoD met: 8 `couple.` tests; the new one runs a REAL MC-transport supercritical Pu-sphere burst that ignites → peaks → disassembles → **quenches on its own** (`report.quenched`, k_quench < k_peak, finite yield, post_peak > 0) — no scripted k, no hardcoded stop. 117 dev / 104 CI. fast4-INDEPENDENT (test-scale core mass for speed; e_f physical). **The demon-core chain reaction is now FULLY EMERGENT.** |
| M3-T3-f | **done** | session-2026-08-03-c | 2026-08-03 | M3-T3-e | SPLIT from step 4 (the fusion API): the `03 §6` **run.json provenance contract** in a NEW `src/api/` nscore module (the fusion/studio API surface `viz/js/simstub.js` calls) — `RunProvenance` struct + `to_json`/`parse_run_json` (nlohmann ordered, required-field parse contract) + `compute_unit_id` (`03 §6`: sha256(canonical_hash ‖ overrides ‖ seed), order-independent). Tested against the `03 §6` example read from the spec markdown (the M3-T3-a pattern). fast4-INDEPENDENT. |
| M3-T3-g | **done** | session-2026-08-03-c | 2026-08-03 | M3-T3-f | SPLIT from step 4 (was evaluate+generate_run; split again for a clean low-risk foundation): the cfg→**demon-core assembly builder** (`DemonCoreAssembly`: a bare Pu sphere from `pit.mass_kg` + the C-102-consistent δ-Pu density 15.23, Pu239/Pu240 SIM xs, the `RealSphere` file-based template) + **`evaluate(cfg)` → {k_eff, k_prompt, sigma_pcm, ready}** via `ref_eigen_fn_masscons` at the compressed geometry, in `src/api/studio.{h,cpp}`. A testable gauge (k rises with mass/compression, falls with Pu-240). fast4-INDEPENDENT. |
| M3-T3-h | **done** | session-2026-08-03-c | 2026-08-03 | M3-T3-g | SPLIT from step 4 (final): **`generate_run(cfg)`** on the M3-T3-g assembly — the disassembly `run_burst` from the COMPRESSED pit (eigen r_ref = uncompressed r0) + `BurstTally` + a `GenerationSample`-collecting sink + `RunProvenance` → `{tally (03 §5), run (03 §6), samples}` JSON (the `samples`/`sites` stream is pitscope's "main course" tap), with **detonate-vs-fizzle EMERGENT from k** (supercritical+quenched+yield vs never-prompt-critical) + reasons. Reconciles the `03 §4`↔`§6` `scenario_overrides` gap (adds the field to `RunProvenance` + the `03 §6` example). fast4-INDEPENDENT (demon core). |
| M3-T4 | todo | — | — | M3-T3-b, M5-T1 | initiator timing. **Dep corrected (session-2026-08-03-c):** `at_peak_compression()` is on the `05 §4 HydroModel` (`serialize()` → `CheckpointBlob`), so M3-T4 needs M5-T1 (`couple.h` deferred that hierarchy — building it now would declare `serialize()` ahead of its M5 consumer); the canonical t_fire sweep also needs `fast4`. Not cleanly runnable before M5-T1. |
| M4-T1 | **done** | session-2026-08-02-c | 2026-08-02 | M1-T1 | SoA buffers + device Philox (**reuses `core/rng`** via `NUKESIM_HD` + `--expt-relaxed-constexpr`, one source, no copy) + deterministic slots (exclusive prefix-sum, no atomic cursor) / streams (`fork`) / reductions (fixed-point int64, no FP atomicAdd). Device KATs match CPU incl. `fork(42,1000,3)`; reduction bit-identical across 4 launch configs, scan across 3 tile sizes. 6 `gpu.` tests; CUDA-guarded so CI stays 75 |
| M4-T2-a | **done** | session-2026-08-02-c | 2026-08-02 | M4-T1 | device geometry (float `AnalyticSphereTracker` mirror, `geometry.cuh`) + device material/xs (`materials.cuh`: per-layer macro Σ_t/Σ_tr + per-isotope slots). Float↔double parity vs CPU tracker (axial/tangential/miss/inside-out rays) and `mix()` (Σ_t/Σ_tr/νΣ_f); 2 new `gpu.` tests → 83 total dev. Per-isotope nu/chi/transfer + `global_index` deferred to M4-T2-b (built with the collision kernel that uses them) |
| M4-T2-b | **done** | session-2026-08-02-c | 2026-08-02 | M4-T2-a | Event-based fixed-source GPU transport (`transport.cu`): SoA + superstep loop (E1a–E1e implicit capture), **prefix-sum compaction** of live particles, **deterministic fission bank** at exclusive-prefix-sum slots + `fork` streams. T-diff vs `ref/` (G0c): pure-capturer leakage + scattering/fission k_inf, both within 3σ; bit-identity (tallies + bank) across launch configs. 3 new `gpu.` tests → 86 dev. **Deferred:** per-event branchless-kernel split → M4-T4 (perf); bank PROPAGATION → M4-T3 (eigen) |
| M4-T3-a | **done** | session-2026-08-02-c | 2026-08-02 | M1-T3, M4-T2-b | SPLIT from M4-T3: GPU k-eigenvalue power iteration (`eigen.cu`) — fission-source iteration (reservoir fission sites → bank at prefix-sum slots → next-gen source, `fork` streams) + fixed-8³ entropy (E2b); **differential vs `ref/` eigen** (GPU k matches within toy-batch tolerance) + bit-identity (k / checksum / entropy) across launch configs. 2 `gpu.` tests → 88 dev |
| M4-T3-b | blocked | owner | 2026-08-02 | M4-T3-a, M1-T5 | SPLIT from M4-T3: the FORMAL G0c gate — `nukebench diff` on the fixed 3-seed set → committed `gate_report.json` (`08 §2`, ≤100 pcm at C-900 batch). Needs `nukebench`+`gates.toml` (M1-T5, behind blocked M1-T4a-2). Statistical equivalence already validated by M4-T3-a's differential test |
| M4-T4-a | **done** | session-2026-08-02-d | 2026-08-02 | M4-T3-a | SPLIT from M4-T4 (§8): GPU perf **profile-first** baseline. `tools/perf/gpu_perf` harness + observational VRAM instrumentation (`peak_vram_bytes`, `device_info()`); gen/s + particles/s-vs-batch + VRAM → `artifacts/perf_history.jsonl` (committed). **Nsight FP64 fraction = 0.00%** on `k_step`+`k_generation` (MAJ-33 ≤1% MET). **Profile finding:** `k_step` already 100% thread-efficient (31.99/32) → the deferred **branchless split is NOT warranted**; `k_generation` (eigen) 29% → the real divergence hotspot. VRAM ≤92 MiB @1e6 (≪12 GB). 88/88 dev, CI 75 |
| M4-T4-b | **done** | session-2026-08-02-d | 2026-08-02 | M4-T4-a | SPLIT from M4-T4 (§8): profile-motivated opt — **INVESTIGATED, NEGATIVE RESULT (measure-before-AND-after).** Built the event-based eigen transport (compaction + reservoir-in-SoA, `k_step_eigen`); validated **bit-identical** to the old eigen (k/checksum/entropy exact via `gpu_perf --golden`) + 88/88. But **measured a 2–6× wall-clock regression** (batch 1e3: 1262→194 gen/s; 1e5: 213→95) despite thread-eff 29%→72% — the eigen's ~30 gens × ~40 supersteps × (4 kernels+sync+memcpy) = **~5000+ launches/solve vs history-per-thread's 30**; per-superstep overhead dominates the divergence savings (unlike transport.cu's single huge generation). **Reverted** to the faster history-per-thread; prototype preserved at commit `b04b7d7`. `gpu_perf --golden` kept. Real fix → M4-T4-c |
| M4-T4-c | todo | — | — | M4-T4-a | Cooperative-groups **megakernel** eigen: ONE launch/generation with an in-kernel superstep loop (`grid.sync()`) + in-kernel compaction, landing M4-T4-b's divergence fix (`k_step_eigen`, 72% thread-eff, preserved at `b04b7d7`) WITHOUT the per-superstep launch/sync overhead that made M4-T4-b regress. Reuse the reservoir-in-SoA + per-flight physics from `b04b7d7`. Bit-identity vs the history-per-thread golden; **adopt only if measured faster** (profile-first). Perf-only; not gate-bearing, and the eigen is not a current bottleneck (no gate runs it at scale until `fast4`) — low priority vs physics/critical-path work |
| M5-T1-a | **done** | session-2026-08-03-c | 2026-08-03 | M3-T3-b, M4-T3 | SPLIT from M5-T1 (SYNC-M5): the `checkpoint.bin` v2 **CONTAINER** (`src/core/checkpoint/`) — the 128-B header (magic `NSCKPT02`, schema_version, endianness, backend, bank_precision, scenario/data sha256, code_version, git_hash) + section table (id/offset/length/CRC32) + **in-tree CRC32** + `write`/`read_checkpoint` with the **R-7 load-rejection rules** (reject on any magic/version/endianness/identity mismatch or section-CRC failure, precise diagnostic) + `CheckpointBlob`/`SimClockState` (§8 §1). Round-trip + rejection tests. Defines the `CheckpointBlob` type `05 §4`/`couple.h` deferred (unblocks M3-T4). fast4-independent. |
| M5-T1-b | **done** | session-2026-08-03-c | 2026-08-03 | M5-T1-a | SPLIT from M5-T1 (again): `BurstAccumulator` checkpoint **serialize/restore** — a `State` struct (the full private log-domain state `{mant, off, log_fcum, log_ecum, log_flast, n, hist}`) + `state()`/`from_state()` + a `CheckpointBlob` codec (section 7) + a **bit-identical resume test** (step → checkpoint → restore → continue == uninterrupted). The core stochastic burst state is the crux of T-resume; proving the ~24-decade log-domain state survives a checkpoint bit-for-bit. fast4-independent. |
| M5-T1-c | **done** | session-2026-08-03-c | 2026-08-03 | M5-T1-b | SPLIT from M5-T1: the couple-loop resume — capture the full `run_burst` state (SimClock + accumulator (M5-T1-b `State`) + **the `BurstTally` sink's accumulators** + geometry radii + `ShellState` + refresh counters) as a `BurstCheckpoint`, wire checkpoint-at-boundary + resume into `run_burst`, + the **T-resume gate** (`11 §T-resume`: kill at 25/50/75% of a SIM demon-core α-mode run → bit-identical `tally.json` same backend + CRC/mismatch negatives). **Bit-identity traps traced this session:** (a) the SINK (`BurstTally`) has state too (log-sums/peaks/k-readouts/population_series) — it needs its own serialize/restore, else the resumed tally is wrong; (b) **checkpoint at a REFRESH boundary** (`n % eigen_refresh_gens == 0`) so the cached eigen (k_p/ν/Λ/shares) can be RE-DERIVED by re-running the deterministic eigen on resume instead of serialized — simplest bit-identity; (c) watch `timing.eigen_calls` — a resume that re-runs the initial eigen must NOT double-count it. Intricate/exacting (any missed state var or miscount → mismatch) — do as a deliberate focused pass. fast4-independent (SIM burst). |
| M5-T2 | **done** | session-2026-08-03-d | 2026-08-03 | M5-T1 | SYNC-M5b run. The `03 §7` `sweep.db` results store (`src/core/store/store.{h,cpp}`, SQLite): `runs(unit_id TEXT PK, params_json, tally_json, wall_s REAL, status)` + `cursor(state_json)` created verbatim. `SweepStore` — `record_run` (INSERT-OR-IGNORE on the unit_id PK → **rerun / second-finisher write is a no-op**, returns whether it inserted; D6/D8), `get_run`/`is_done`/`status_of`/`count`/`count_with_status`, `set_cursor`/`get_cursor` (the singleton `cursor` resume blob). All params bound via prepared statements (injection-safe; a `DROP TABLE`-payload test round-trips intact). **Layering:** core/store depends only on `sqlite3` (via `nukesim_deps`) and keys on `unit_id` as an opaque STRING — NO dependency on `src/api` (`compute_unit_id` stays in api). 7 `store.` tests (empty store, round-trip, injection-safe binding, idempotency no-op, is_done/status, cursor-singleton replace, on-disk reopen-persistence) → 143 dev / 130 CI. nukefarm CLI + FS queue + samplers deferred to M5-T3 (need the sweep engine; a shell now = the SYNC-M1 anti-pattern). fast4-independent. |
| M5-T3-a | **done** | session-2026-08-03-d | 2026-08-03 | M5-T2 | SYNC-M5c run (first `src/app/` frontend). SPLIT from M5-T3 (the store-independent planning front): `src/app/nukefarm/sweep.{h,cpp}` + `samplers/builtin.cpp` (the `nukesim_nukefarm` static lib). `SweepManifest::load`/`parse` — the `03 §7` sweep.toml loader with FULL validation + the **axis_class/ScoreKind/objective enforcement** (MAJ-35, the mechanical "explain-don't-search" boundary of `00 §3.4`): numerical=free; uncertainty needs `constant_id` + range ⊆ [`ns::consts::get_lo`/`get_hi`(id)] band; pedagogical only {grid, budget≤100, sensitivity}; calibrate needs a target band + all-non-pedagogical; unknown key/sampler/constant_id hard-error. The `06 §2` `Sampler` interface (`next`/`report(ParamSet,TallyResult)`/`score_kind`/`supports_categorical`) + grid/lhs/random space-filling samplers (grid = `floor(budget^(1/n))`/axis lattice; lhs/random seeded by a stable FNV-1a of the manifest name, reproducible) + `make_sampler` (rejects OptimizeExtremum + `mcts`→M5-T4). 12 `nukefarm.` tests → 155 dev / 142 CI. D7-clean (frontend consuming a TallyResult; no physics). fast4-independent. |
| M5-T3-b | **done** | session-2026-08-03-d | 2026-08-03 | M5-T3-a | The store-backed run-loop ENGINE (`src/app/nukefarm/runner.{h,cpp}`): `run_sweep(manifest, store, eval)` drives the M5-T3-a `make_sampler` points → `apply_point` (ParamSet → StudioConfig via the flat dotted keys) → `ns::api::studio_unit_id` → `store.is_done` SKIP (resume) else `eval(cfg)` → `store.record_run` (INSERT-OR-IGNORE = no double-count) + cursor every `checkpoint_every_runs`. Evaluator injectable (`Evaluator`=`std::function`; `default_evaluator` wraps the fast4-independent `generate_run` + times it). **Exposed `ns::api::studio_unit_id(cfg)`** on studio.h/.cpp (the exact unit_id generate_run records, deduped) so the loop skips a unit without re-running its burst. 3 new `nukefarm.` tests (15 total): a 25-point local sweep records each unit once; a partial-resume runs only the 15 remaining + a re-run skips all 25 (no double-count); a real-burst smoke asserts the up-front `studio_unit_id` == the recorded `run.json` unit_id. → 158 dev / 145 CI. fast4-independent (SIM burst). |
| M5-T3-c | **done** | session-2026-08-03-d | 2026-08-04 | M5-T3-b | The `06 §2` filesystem work-queue LIBRARY (`src/app/nukefarm/queue.{h,cpp}`): `WorkQueue` over `queue/pending→claimed→done` + `.lease` files — `enqueue` (idempotent by `unit_id`), `claim(now_s)` (atomic pending→claimed rename + lease stamp), `complete` (claimed→done + drop lease), `reclaim_stale(now_s, threshold_s)` (lease age > threshold → back to pending), + per-dir counts. Deterministic: time (`now_s`) + threshold are PARAMETERS, so the lib is policy-free (no ADR; the 2×-median policy + the `t_max_s`-fallback amendment ride with the worker/CLI in M5-T3-d). Coordination = atomic FS renames (no lock server); an unparseable lease is treated as stale. 4 new `nukefarm.` tests: the pending→claimed→done lifecycle; empty-claim → nullopt; the stale-threshold boundary; and **the DoD reclaim clause** — an induced stale-lease reclaim re-runs a unit and the late finisher's `record_run` is a no-op (store idempotency), one row, B's result stands. 162 dev / 149 CI. fast4-independent. |
| M5-T3-d | **done** | session-2026-08-03-d | 2026-08-04 | M5-T3-c | The distributed-sweep CORE (`src/app/nukefarm/worker.{h,cpp}` + `SweepStore::recent_wall_s` + `param_json` exposed). **ADR-020** amended `06 §2`: the stale-lease fallback is now a wall-clock `stale_lease_fallback_s` (600 s), NOT the sim-time `3× t_max_s` (~15 µs, which thrashed the queue). `stale_threshold_s(store, fallback)` = 2× median of the last 10 completed `wall_s` else the fallback; `submit(manifest, queue, store)` enqueues the sampler's points by `studio_unit_id` (payload = the override JSON, skips already-done → resumable); `run_worker(queue, store, eval, clock, threshold)` drains (reclaim_stale → claim → `from_json` → eval → `record_run` → complete), with `record_run` idempotency making a reclaimed unit run twice a no-op. 4 new `nukefarm.` tests: the ADR-020 policy (fallback <10, 2×-median ≥10); submit enqueues 25 by unit_id (idempotent); run_worker drains 25 into the store; run_worker reclaims a pre-staled lease + runs it. 166 dev / 153 CI. fast4-independent. |
| M5-T3-e | **done** | session-2026-08-03-d | 2026-08-04 | M5-T3-d | **M5-T3 COMPLETE.** The `nukefarm` CLI exe — the FIRST `src/app/` executable (`src/app/nukefarm/main.cpp` + testable handlers `cli.{h,cpp}`). CLI11 `submit --sweep --queue --db [--local]` / `worker --queue --db [--stale-lease-s]` / `status --db [--queue]` / `resume` — thin dispatch over `cli_submit`/`cli_worker`/`cli_status` (which open a `WorkQueue` + `SweepStore` from paths and drive `submit`/`run_worker`/`stale_threshold_s`), with `wall_clock_seconds()` the one real `std::chrono` clock feeding `run_worker`'s injected-clock seam, `--stale-lease-s` default 600 (ADR-020), and `06 §5` exit codes (0/1/3). 2 new `nukefarm.` tests (a submit→worker→status round-trip drives a 9-unit grid via a stub eval + a real temp-file db; status is 0 on a fresh store) + a live smoke (`nukefarm status` prints `done 0 | queue …`; `--help` exits 0). Non-test exe (like `gpu_perf`) — ctest count unchanged; 168 dev / 155 CI. fast4-independent. |
| M5-T4 | **done** | session-2026-08-03-d | 2026-08-04 | M5-T3-e | The MCTS sampler (`src/app/nukefarm/samplers/builtin.cpp` — `MctsSampler`): a PUCT tree over the parameter box behind the `06 §2` `Sampler` interface. Each node is a hyperrectangle that bisects its longest axis into two child half-boxes ("continuous axes node-discretized then refined"), added by progressive widening; `next()` descends by PUCT (`Q + c·√N_parent/(1+N_child)`) — expand a child or select the best — and returns the leaf box CENTRE (deterministic + reproducible; refinement, not RNG, drives convergence); `report()` scores `-|yield_kt − band_center| / half_width` (`score_kind() == BandCenter`) + backpropagates the last path. `make_sampler("mcts")` requires a calibrate objective with `target_yield_kt` (BandCenter needs a centre; else SweepError) — `OptimizeExtremum` still rejected, pedagogical+mcts already loader-rejected. 3 new `nukefarm.` tests (was "mcts not-yet-available" → now "builds"): **the DoD convergence** — mcts's best `|yield−center|` beats the seeded random sampler's over 300 evals on a paraboloid peaked at (7,3); mcts-needs-a-band (sensitivity → throws); pedagogical+mcts → parse throws. 171 dev / 158 CI. fast4-independent. |
| M5-T5 | todo | — | — | M5-T3 | container (verify base tag+digest) + cloud dry-run |
| M6-T1 | todo | — | — | M4-T3 | OptiX tracker + parity |
| M6-T2 | todo | — | — | M6-T1 | 32-lens geometry generator |
| M6-T3 | todo | — | — | M6-T2, G2 | c_a derived per C-071/C-072; G3 EXECUTED + recorded (never "G3 passes" as DoD) |
| M7-T1 | todo | — | — | M3-T3-b | field grids + dumps + budget guardrails |
| M7-T2 | todo | — | — | M7-T1 | raymarcher + color + tonemap + temp calibration |
| M7-T3 | todo | — | — | M7-T2, M4-T4 | nukestudio + G5 parity |
| M7-T4 | todo | — | — | M7-T2 | nukecinema + make_film |
| M7-T5 | todo | — | — | M7-T3, M6-T3 | Sandbox mode (ADR-018): per-detonator `[[lenses.detonators]]` schema + loader validation; free clip plane (shells + volume); deterministic seeded neutron trails (BURST phase); timeline event markers from computed state. Non-canonical discipline unchanged; no physics/gate change |

## Milestone-boundary SYNC checklist

**SYNC-M1 — run 2026-08-02 by session-2026-08-02-b, before claiming M1-T1.** Compared every implemented module against `04`; `05` has no implementation yet. Three findings: (1) `04 §3` named `MatXS mix(const Material&, const FewGroupXS&)` and it did not exist — the logic was inline in `MaterialLib::load_file`; **fixed**, extracted and declared in `core/material` (the dependency runs opposite to `04 §3`'s placement). (2) `04 §5`'s `Material::fracs` was `vector<pair<const IsotopeXS*, double>>`, which cannot carry a species that has a molar mass but no cross sections in the set — appendix §3 requires the mass per species regardless; **spec amended** to the implemented `Constituent` form. (3) `canonical_hash()` (`04 §6`) remains unimplemented — its test is grouped with geometry in `04 §7`, so **M1-T1 carries it**. Scenario paths vs `03 §4` and constants vs the appendix were already covered by `ctest -R loaders` and `constants.roundtrip_bijection` respectively; both green.

**SYNC-M4 — run 2026-08-02 by session-2026-08-02-c, before claiming M4-T1.** M4 enters `src/gpu/` (empty but for `optix/.gitkeep`). Audited vs `04`/`05`/`03`: (1) **`04 §2` device RNG** — `core/rng/{philox,rng}.h` are `constexpr` and the philox header already anticipates the device port ("the same source compiles for device code unchanged … the GPU port (M4-T1) substitutes `__umulhi`"). Making them device-compilable is M4-T1's job. Resolution: a behaviour-preserving `NUKESIM_HD` macro (`__host__ __device__` under `__CUDACC__`, empty on a host compiler) via new `src/core/hd.h`; frozen KAT values and the host build are unchanged (test_rng re-verifies). **Not a spec amendment** — it is the implementation of the port `04 §2`/`05 §6` already specify. (2) **`05 §6` GPU design** — M4-T1 declares **no** `GpuTransport` interface: declaring an unimplemented method is exactly what SYNC-M1 caught for `mix()`. M4-T1 ships only the foundation primitives (SoA buffers, device Philox, slots/streams/reductions); the `RefTransport`-shaped `GpuTransport` arrives at M4-T3. (3) **Scenario paths / constants** — N/A for a buffers task; the C-907 stream registry is already generated, and the fixed-point reduction scale is a documented numerical detail (like the Philox multipliers, deliberately not a physical constant — philox.h). No divergence found; no amendment needed.


**SYNC-M3 — run 2026-08-02 by session-2026-08-02-d, before claiming M3-T1.** M3 enters `src/physics/kinetics/` (new). Audited the kinetics needs vs `05 §3` / `01 §4` and the implemented `physics/eigen`: (1) **`05 §3`'s `AlphaKinetics(const Scenario&, EigenFn); run(HydroModel&, TallySink&)`** — `HydroModel` (M2-T2/M3-T2) and `TallySink` (M3-T3) do not exist. So M3-T1 implements the E3a **core** (ν̄_eff, k_prompt consumption, E3a generation stepper, Λ/α, q diagnostic + auto-halving, log-renorm) as testable free functions/structs; the full `run()` is M3-T3 (coupling). **Not declared** until it has an impl (SYNC-M1 rule). (2) **k_prompt/β_eff/Λ/α already on `EigenResult`** (`physics/eigen/eigen.h`: `k_prompt()`, `beta_eff`, `lambda_s`, `alpha_per_s`, per ADR-013/M1-T3) — kinetics consumes them, never re-derives (the (1−β) factor is applied exactly once). (3) **ν̄_eff gap (FIXED this task):** `01 §4`/`05 §3` compute ν̄_eff from the per-isotope fission source, but `ref::FissionSource` carried only `by_isotope` = per-isotope **production** (νΣ_f-weighted). ν̄_eff = Σ production / Σ **fissions** needs per-isotope fissions too. Added `by_isotope_fissions` to `FissionSource` + one accumulation line in `ref_generation` (additive; existing eigen behaviour unchanged, re-verified) so ν̄_eff = Σ`by_isotope`/Σ`by_isotope_fissions` is exact and wireable — no per-isotope ν̄ lookup needed. (4) **Paths/constants:** `src/physics/kinetics/` per `02 §2`; the stepper's inputs (k_prompt, ν̄_eff, Λ, S_n) are provided at call time, so no new constant is consumed by M3-T1's units (C-051 initiator schedule / refresh cadence constants are consumed by M3-T3's coupling).

**SYNC-M2 — run 2026-08-02 by session-2026-08-02-d, before claiming M2-T1.** M2 is data + Tier-1 hydro. Audited the material/scenario needs vs `03 §3`/`§4`, the M0-T5 loaders, and the appendix: (1) **Material schema** (`03 §3`): `isotopes` are **atom fractions** (Σ = 1 ± 1e-6), `composition_check` declares derived wt% (loader recomputes, WARNs > 0.2 pp); the loader computes per-layer mass = geometry×density and **WARNs** > `mass_tolerance_pct` (3%, SIM) — never errors (reconstructed masses carry real spread; the C-102 pit density discrepancy is recorded, not resolved). M0-T5's loader implements all this. (2) **fast4 dependency (key):** the C++ loader's mass-WARN fires only when a *scenario* loads, which needs `xs_set="fast4"` (BLOCKED, M1-T4a-2). So M2-T1's mass check runs **xs-free via `oracle.py` §2** (material JSON density × geometry constants C-100..C-109) + `constants_roundtrip` (molar-mass completeness, now non-vacuous). `trinity_canonical.toml` is AUTHORED (valid `03 §4`) but its transport/loader-mass path awaits `fast4`. (3) **Constants present:** geometry OD + `mass_kg`/`mass_lo_kg`/`mass_hi_kg` (C-100..C-109) and molar masses (C-910+, appendix §3 completeness) all in `constants.data.toml`. The **b10_acrylic B-10 loading** is not cited numerically (research doc: "B-10 in acrylic", no wt%) → tagged **SIM/RECONSTRUCTED** with a README caveat; it has no appendix mass, so it is not mass-gated (honest labelling, not fabrication). (4) **oracle.py §2** already emits a PENDING(M2-T1) layer-mass block keyed on `pu_ga_delta.json` — extend it, don't add a separate check.

**SYNC-M5c — run 2026-08-03 by session-2026-08-03-d, before claiming M5-T3-a.** M5-T3 enters `src/app/nukefarm/` — the FIRST `src/app/` frontend code (the dir + `samplers/` are `.gitkeep` in the `02 §2` tree). Audited the sweep-planning needs vs `03 §7` + `06 §2` + `00 §3.4` + the implemented modules: (1) **Sampler interface** — `06 §2` gives it verbatim (`next()` / `report(ParamSet, Tally)` / `score_kind()` / `supports_categorical()`); `Tally` binds to `ns::physics::TallyResult` (`03 §5`) — a frontend consuming an nscore OUTPUT (D7-clean; no physics logic in `src/app/`). (2) **Constant-band lookup** — the `uncertainty`-axis rule (range ⊆ [lo,hi] of `constant_id`) resolves via the existing `ns::consts::get_lo(id)` / `get_hi(id)` runtime lookup (`04 §1`; throws on an unknown/PENDING id, so a bad `constant_id` is rejected). No new lookup needed. (3) **Enforcement (`03 §7`/MAJ-35)** is LOAD-time validation: numerical = SIM knob (free); uncertainty = `constant_id` required + range ⊆ band; pedagogical = only {sampler=grid, budget_runs≤100, kind=sensitivity}; calibrate = every axis numerical/uncertainty; the sampler's ScoreKind ∈ {Coverage, BandCenter} (OptimizeExtremum rejected at load). (4) **D7** — nukefarm is a frontend, so the DoD-testable logic (loader, samplers) lives in a `nukesim_nukefarm` static lib that the CLI (M5-T3-b) + the tests link; no physics in `src/app/`. (5) **Design choices (documented; unspecified areas → no ADR):** grid discretizes each continuous axis into `floor(budget_runs^(1/n))` evenly-spaced points (reuses the existing `budget_runs`; no schema field added); lhs/random seed a deterministic FNV-1a hash of the manifest name (reproducible per sweep). (6) **No spec amendment** (implements `03 §7` + `06 §2` as written). Paths: `src/app/nukefarm/` (+ `samplers/`) per `02 §2`.

**SYNC-M5b — run 2026-08-03 by session-2026-08-03-d, before claiming M5-T2.** A focused re-audit of the artifact store vs `03 §7` (M5's milestone SYNC-M5 already ran; M5-T2 is not a new milestone). M5-T2 enters `src/core/store/` (empty `.gitkeep`; `02 §2` tree line "core/ … checkpoint/ store/"). Findings: (1) **Schema (`03 §7`) implemented verbatim:** `runs(unit_id TEXT PK, params_json TEXT, tally_json TEXT, wall_s REAL, status TEXT)` + `cursor(state_json TEXT)`. (2) **Layering (key):** the store keys on `unit_id` as an opaque STRING, so `src/core/store/` depends ONLY on `sqlite3` (already linked via `nukesim_deps` — the `toolchain.sqlite3` test proves it) and NOT on `src/api/` (where `ns::api::compute_unit_id` lives, M3-T3-f). This mirrors the M5-T1 discipline (the core container is a generic holder; the owning layer supplies the semantics) and keeps core free of api/physics — the caller (api/nukefarm) computes `unit_id` and hands the string down. (3) **Frontend scope:** the `nukefarm` CLI (`submit`/`worker`/`status`/`resume`) + the filesystem work-queue + samplers are M5-T3/T4 — they need the sweep ENGINE; a nukefarm shell built now would be an engine-less frontend (the SYNC-M1 "don't declare an unimplemented API" anti-pattern). So M5-T2 = the store LIBRARY (`src/core/store/`) + tests; the `src/app/nukefarm/` column of the `07 §M5` DoD is satisfied by M5-T3 consuming this store. (4) **Idempotency (`03 §7`/D6/D8):** `record_run` is INSERT-OR-IGNORE on the `unit_id` PK → the "second finisher's write is a no-op" (`06 §2`); rerunning a recorded unit is a no-op by construction. Retry/requeue of a failed unit is a queue policy (M5-T3), not the store's concern. (5) **Bundle deferred:** the per-run `artifacts/<unit_id>/{run.json,tally.json,checkpoint.bin}` emission (`03 §6`) is the RUN-emitter's job (api/nukefarm) — pulling FS-bundle writing into core/store would either duplicate api types in core or leak api into core, so it stays out; `sweep.db` + run bundles are untracked object-storage artifacts (`02 §2` line 67 / R-21). (6) **No spec amendment** (implements `03 §7` as written). Paths: `src/core/store/` per `02 §2`.

**SYNC-M5 — run 2026-08-03 by session-2026-08-03-c, before claiming M5-T1.** M5 enters `src/core/checkpoint/` (empty `.gitkeep`). Audited the checkpoint/resume needs vs `03 §8` + `05 §4` + `11 §T-resume` + the implemented state structs: (1) **`CheckpointBlob` (05 §4) is the type M5-T1 DEFINES** — `HydroModel::serialize()`/`restore()` + `couple.h` both deferred it ("consumers don't exist yet"); providing it is exactly what unblocks M3-T4's `HydroModel` hierarchy. (2) **`03 §8` sections vs existing state:** SimClock — no struct (define `SimClockState`, the §8 §1 fields); RNG — `core/rng::Stream` state is `(seed, stream, ctr, sub)` (`03 §8 §2` / MAJ-56); neutron bank — `ref/` is HISTORY-based (no persistent inter-generation bank; its "bank" is the fission source), so the bank section is GPU-native / a -b concern; geometry — `LayerStack` (canonical_form); hydro — tier1/tier2 (versioned `§5`); eigen — `EigenResult` exists; tally — `BurstTally` accumulators. (3) **CRC32 needed in-tree** (`03 §8` per-section CRC) — added like the in-tree SHA-256 (checked against the known IEEE `"123456789"→0xCBF43926` vector), NOT a new dependency (`02 §4` ADR avoided, same grounds). (4) **T-resume (`11 §T-resume`)** kills at 25/50/75% of a canonical α-mode run → bit-identical `tally.json`; the "canonical run" can be the SIM demon-core burst (fast4-INDEPENDENT); the bit-identical gate + the run-loop resume wiring are the harder half. **Split M5-T1** → **M5-T1-a** (the `checkpoint.bin` v2 CONTAINER: 128-B header + section table + in-tree CRC32 + the load-rejection rules R-7 + `SimClockState` + `CheckpointBlob`) + **M5-T1-b** (the live-state typed sections wiring RNG/bank/geometry/hydro/eigen/tally + resume into `run_burst` + the T-resume bit-identical gate). Paths: `src/core/checkpoint/` per `02 §2`. No spec amendment (implements `03 §8`/`05 §4` as written).

Before claiming the first task of a new milestone: run the SYNC audit (`07-milestones.md` §SYNC) — APIs vs `04`/`05`, paths vs `03 §4`, constants vs appendix; log results in SESSIONS.md.

## Environment notes

- Dev machine (confirmed 2026-08-02): **Windows 11, Git Bash; RTX 4070 Ti SUPER (sm_89, 16,376 MiB, driver 610.47, RT cores); CUDA 13.1 (V13.1.80); MSVC 14.44; CMake 4.3.3; Python 3.13.2; OptiX SDK 9.1.0; vcpkg `C:\vcpkg`**. Version strings live ONLY in `spec/12-deployment.md` §1. Default backend: `--backend gpu` (ADR-009).
- Sibling CUDA projects (proven toolchain + process patterns): `C:\Buddhabrot_CUDA`, `C:\backrooms` (vcpkg manifest presets), `C:\Booster_Lander_Simulator` (DECISIONS/HANDOFF pattern), `C:\blackhole`.
- Cloud: RunPod H200 target (M5, sm_90, no RT cores → analytic tracker + plain-CUDA raymarch, D2). Hosting: public GitHub + MIT (ADR-011) — repo live at <https://github.com/bochen2029-pixel/nuclear-sim> since 2026-08-03.

## Blockers

- ~~**M0-T6-b:** owner creates the public GitHub repo (ADR-011).~~ **RESOLVED 2026-08-03** — repo live at <https://github.com/bochen2029-pixel/nuclear-sim> (public, MIT). M0-T6 collapses to a single task: local CI + Actions workflow, then Actions green on main.
- **M1-T4a-2 (blocks M1-T5/G0a + all of M2) — NEW 2026-08-02:** needs owner-provided cited cross-section data — a published 4-group / multigroup fast library, or an ENDF + documented-collapse pipeline (NJOY-class), neither present locally. BLK-14 forbids an autonomous session seeking ICSBEP access. **Owner action to unblock:** supply a citable multigroup library, or authorise/install an ENDF processing pipeline. Assessed by session-2026-08-02-c: no local NJOY/ENDF/ACE tooling; the benchmark scenarios already reference `xs_set = "fast4"`, which does not yet exist, so G0a/G0b cannot run until this lands.
- **M1-T4b (optional):** owner ICSBEP access decision. Not required — M1-T4a (open literature) is the default path.
- **C-945 (`g4_perf_budgets`) stays PENDING — NEW 2026-08-02 (M4-T4-a):** M4-T4-a resolved the one G4 criterion that is a pure GPU-kernel property — the **FP64 instruction fraction = 0.00% ≤ 1%** (MAJ-33) — and recorded dev-GPU throughput/VRAM baselines (`artifacts/perf_history.jsonl`). The REST of the 08 §2 G4 budget list needs workloads that do not exist yet: the **Godiva gate eigen** (2.5e8 histories) needs the blocked `fast4` xs (M1-T4a-2); **canonical-burst gen/s + eigen-calls-per-burst** need M2/M3; **render ≥30 fps** needs M7. So C-945's `resolved_by="M4-T4"` is **optimistic** — its full resolution spans M4-T4 / M2–M3 / M7. Owner action: the same M1-T4a-2 unblock also opens the Godiva-gate-eigen G4 measurement. No budget/ADR change this session (can't measure the gated workloads).
