# NUCLEAR-SIM — Session Handoff (2026-08-05) → the gate ledger, the first passing gate, and the vision made real

*Written by `session-2026-08-05-a` at the end of a very large single-day run. Read this, then
`spec/PROGRESS.md` + `spec/SESSIONS.md` (the authoritative living state), then the memories. If
anything here disagrees with `PROGRESS.md`, trust `PROGRESS.md` and re-run VERIFY.*

---

## 0. TL;DR (60 seconds)

- **`main` @ `f8d094d`, fully green: 184 dev (168 CPU + 16 `gpu.` on the RTX 4070 Ti SUPER), 168 CI.**
  Every increment this session CI-verified (the last, a cosmetic test rename, is CI-watching —
  near-certain green). VERIFY: `cmake --preset win-x64 && cmake --build --preset win-x64-rel && ctest --preset win-x64-rel`.
- **The gate ledger is now honest, both ways:**
  | Gate | Verdict | Number | Why |
  |---|---|---|---|
  | G0a Godiva | **FAIL** | +2560 pcm | the coarse 4-group xs residual (ADR-022), NOT the data |
  | G0b Jezebel | **FAIL** | +1601 pcm | same |
  | **G0c** ref≡gpu | **PASS** ✅ | **Δk = 6.3 pcm** | the two backends agree (≤100 pcm C-932 + ≤3σ) |
  **G0c is the project's FIRST passing gate** — the cross-backend proof of ADR-021, committed at
  `artifacts/gate_reports/{G0a,G0b,G0c}/gate_report.json` (QC-07). A real pass *means* something
  precisely because the fails weren't fudged.
- **`fast4` is no longer a blocker** (owner authorized it last session; the cited ENDF/B-VIII.0
  4-group a-set is committed). The demon-core physics, the whole M5 sweep infra, and the fusion
  binding are all done + green from prior sessions.
- **The "main course" vision is now resurfaced, normative, AND reconciled** (see §3) — the biggest
  non-code outcome of this session.

---

## 1. What this session shipped (the arc, all on `main`)

| # | Increment | Commit | Essence |
|---|---|---|---|
| 1 | **M1-T5-c-1** — committed C-900 G0a/G0b evidence + report provenance | `53a9712` | the credibility keystone: honest FAIL reports in-repo (QC-07); `cli_gate` now stamps git/device/ISO timestamps |
| 2 | **ADR-023** — the demon-core vision → **normative spec** | `e1a72cc` | `09 §4` (true-3D-not-points) + `10-ui` Panel 7; a flat-points / scene-hiding-mode / O(N²) render is now spec-non-conformant |
| 3 | **M1-T5-c-2** — the **GPU gate backend** | `06b6e8b` | `run_gate` dispatches `ref\|gpu` (CUDA-guarded); `nukebench gate --backend gpu` runs G0a's 5 seeds in **0.79 s** vs ~70 min ref |
| 4 | **M1-T5-c-3** — `nukebench diff` → the **G0c** differential | `6c78889` | `run_diff` runs a gate on ref + gpu, checks `08 §2` (a) \|k_ref−k_gpu\| ≤ C-932 + ≤3σ; G0c emitted into gates.toml |
| 5 | **M1-T5-c-3b** — the committed **C-900 G0c report: PASS** | `d53a599` | Δk 6.3 pcm — the first passing gate |
| 6 | cosmetic: rename the stale `mcts` test title | `f8d094d` | flagged in HANDOFF-2026-08-04; body asserted the opposite |

Plus a huge **vision archaeology + reconciliation** thread (§3) that produced `docs/viz-hookup-state.md`
and the `viz/reconcile-true3d` branch.

---

## 2. Where to look (read-order after this doc)

- `spec/PROGRESS.md` — living state: task table, gate ledger, ready-queue, the single NEXT ACTION,
  VERIFY, blockers. **Ground truth.**
- `spec/SESSIONS.md` — the last ~8 entries are all `session-2026-08-05-a`; their "Notes for next
  session" are written for YOU.
- `spec/DECISIONS.md` — **ADR-023** (the vision → normative) is newest; ADR-021/022 (the transport
  correction + the 4-group residual) are the physics headlines from last session.
- **Memories** (`C:\Users\user\.claude\projects\C--NUCLEAR\memory\`, auto-loaded): `core-vision-3d-chain-reaction`
  (heavily updated this session — the verbatim owner source + the reconciliation status),
  `viz-seam-coordination`, `autonomous-cadence`.
- `docs/viz-hookup-state.md` — the sim↔viz seam state, the core-vision requirements + the honest
  gap-vs-code + the reconciliation (§3.2). The orientation doc for the -e/viz track.
- `docs/HANDOFF-2026-08-04.md` — the prior session's comprehensive snapshot (fast4, the fusion
  binding, M5). Good background; this file supersedes it for current state.
- Code: `src/app/nukebench/` (the gate infra — `gates`, `gate_report` incl. `run_gate`/`run_diff`,
  `cli`, `main`); `src/gpu/eigen.h` (the host-callable `gpu_eigen` the gate backend uses).

---

## 3. The "main course" vision — resurfaced, normative, reconciled (this session's headline)

The owner asked me to resurface a past conversation where they laid out the project's core vision. A
**7-agent transcript archaeology** found it: **session `58656ddb`, 2026-08-03T17:45Z** (in
`C:/NUCLEAR/claude_chat_export_2026-08-05b/C--NUCLEAR__MERGED-13sessions.chat.md`). The owner's own
words: *"10000 to 50000 representative 'atoms' (not just point dots but actual spherical shaped in
true 3d) in a true 3d volumetric sphere where the user can zoom in and pan around INSIDE as well as
outside… time slider… 5 seconds to as long as five minutes… so that nothing is ever faked… then
around that is everything else that makes up the fat boy… everything that leads up to that moment."*

The four load-bearing requirements (owner, reconfirmed + sharpened 2026-08-05), now **normative in
`09 §4` + `10-ui` via ADR-023**:
1. **ONE holistic spacetime** — one continuous SimClock timeline + one scene; the demon-core moment
   is NOT a carved-out demo; time slows most in the burst but never cuts.
2. **True 3D volumetric particles** — instanced 3D geometry, NEVER points/2D/sprites.
3. **Zoom = camera focus, not a mode** — defaults to a close zoom on the core; the user keeps full
   zoom-out/in/pan; never hides the rest of the scene.
4. **Representative sampling of the real `FissionSource::sites`** (O(N) MC, never O(N²)).

**Reconciled → branch `viz/reconcile-true3d` (pushed, reviewable).** The two viz forks were unmerged:
`viz/atom-proto` had the true-3D `InstancedMesh` render (nuclei that split + `LineSegments` neutron
streaks); `main`/`viz` had the real-physics `studio-adapter.js` but a flat `THREE.Points` pitscope.
The reconciliation (produced in an ISOLATED worktree — the -e session's ~478 lines of uncommitted
`viz/` WIP were never touched) marries them + reworks the microscope from a scene-hiding mode-switch
into an **embedded camera-zoom**. It is a **drop-in for the -e track** (no `main.js` change; recipe in
`docs/viz-hookup-state.md` §3.2). Visual render-verify is the -e adoption step (the host wiring is
their live `main.js`).

---

## 4. Ready-queue (runnable now — pick per owner)

1. **G0c criteria (b) + (c)** — per-shell fission-source + population-series equivalence (`08 §2`
   G0c b/c). Needs `EigenResultGpu` (`src/gpu/eigen.h`) **and** the ref `EigenResult` extended to
   emit those series from the eigen, then the criteria added to `run_diff`. The k-equivalence (a) is
   already a committed PASS.
2. **`nukebench run`** (scenario → tally.json) — completes the `06 §1` run/gate/diff set (reuses the
   `src/api` studio surface / the couple machinery).
3. **M1-T4a-2b (fast4-b)** — the remaining 8 structural/HE/initiator isotopes via the same
   `tools/xs/` pipeline (ENDF collapse under WSL — see the M1-T4a-2a SESSIONS notes for the exact
   re-fetch steps), for full-device transport / step 5.
4. **M2-T3** — k-vs-compression scan (now has `fast4`).
5. **The -e visual adoption of `viz/reconcile-true3d`** — the -e/viz track drops in the reconciled
   `pitscope.js` + `scenario.js` and *sees* the true-3D chain reaction on real physics.
6. **M5-T5** (container) — docker/registry-gated; surface to the owner (like `fast4` was), do NOT
   invent a base-image digest.

---

## 5. Traps + hard-won context (read every one)

- **`Σ ∝ ρ` (density coupling):** for any moving geometry use `ref_eigen_fn_masscons`, never
  `ref_eigen_fn` — else compression/expansion read backwards.
- **The transport correction (ADR-021):** collide on σ_tr, not σ_t — a no-op for μ̄=0 (SIM xs) but
  ~17000 pcm leverage on real xs. Fixed on BOTH backends; G0c PASS is the proof it's consistent.
- **NEVER tune xs to a gate** (ADR-022 / the c_a discipline). G0a/G0b FAIL honestly; that's the
  point. `fast4` MUST NOT be invented (BLK-14) — it's owner-authorized cited ENDF now.
- **The GPU gate backend is DEV-only** (CUDA). CI is CPU-only: the gpu path `#ifdef`s out, `--backend
  gpu` throws "requires a CUDA build", `nukesim_gpu` links into the nukebench lib ONLY inside
  `if(NUKESIM_WITH_CUDA)`. So `diff`/G0c real runs + the +2 `gpu.` tests are dev-only; **CI stays 168.**
- **ASCII-only `TEST_CASE` names** — a `§`/`—`/`,` breaks ctest↔Catch2 matching ("No test cases
  matched"). Hit twice this session; keep titles ASCII.
- **CI cadence:** space out `main` pushes (rapid pushes → a GitHub concurrency cancel-cascade). Watch
  each push to green before the next. **gcc (Linux CI) finds what MSVC misses** on new C++. A dead
  `gh run watch`/poll (machine sleep/reboot, exit 4) is NOT a failed CI — re-check the run's
  `conclusion` by `headSha` directly (this bit us on the reboot; the CI was green).
- **The shared working tree with the -e (viz) session:** `viz/` + `.zcode/` carry the -e session's
  UNCOMMITTED work. **Stage EXPLICIT paths, NEVER `git add -A`;** verify `git diff --cached
  --name-only` has no `viz/` before every commit. Do NOT edit `viz/` from the main track (this
  session's viz work was done in an ISOLATED worktree — `viz/reconcile-true3d` — precisely for this).
- **`data/constants.toml` is still invalid TOML on `main`** (unescaped quotes in a cite, a
  `gen_constants` emission bug). HARMLESS (the C++ reads the generated header; `gen_gates` line-scans
  the strict source), but a real latent bug. A fix was written in a parallel worktree
  (`claude/objective-volhard-156298`) but is NOT merged — verify before relying on it.
- **`.gitignore` un-ignores `artifacts/gate_reports/**`** (QC-07) — gate reports are committable +
  committed; that's the cross-session root of trust.

---

## 6. The one-paragraph orientation

You are continuing a spec-first, benchmark-provable GPU Monte-Carlo simulator of the Trinity device
whose *soul* is an interactive, real-physics 3D visualization of the demon-core fission chain reaction
— now a NORMATIVE requirement (ADR-023) with a reconciled true-3D implementation waiting on a branch
(`viz/reconcile-true3d`). The engine is real and honest: this session put the **first passing gate**
(G0c cross-backend equivalence, Δk 6.3 pcm) on `main` alongside the honest G0a/G0b fails (the 4-group
residual), which is exactly the discipline that makes the visuals *mean* something. `main` is green at
184/168. Read `PROGRESS.md`, hold the two invariants — **nothing faked, and `Σ ∝ ρ`** — then pick from
the ready-queue and build one clean, green, CI-verified increment. Welcome; go well.
