# NUCLEAR-SIM — COMPREHENSIVE HANDOFF + BOOTSTRAP PROMPT (session-2026-08-06-c)

> **This file is the authoritative, ALWAYS-CURRENT bootstrap for the ENTIRE project.** It is
> maintained live: the owner's standing instruction (2026-08-06) is to **update the LIVE LOG (§0)
> BEFORE every action and AGAIN AFTER it**, so no matter when a session dies, this file reflects
> reality. A fresh session should be able to read ONLY this file + `spec/PROGRESS.md` and be fully
> productive. Trust files over memory; verify with the VERIFY loop (§9).

---

## 0. LIVE LOG (newest first — updated before+after each action; the crash-safe state)

- **[2026-08-06-c ~T9] PAUSED at owner request** to give run/interact instructions. State CLEAN:
  `main` = `b782c67`, all pushed; only the -e WIP + this handoff are uncommitted (handoff committed
  with this entry). Core-view is a **strong, complete final hyperreal deliverable**. To resume: keep
  polishing the aesthetic (owner: "keep going"), or take new direction. **Owner test path:**
  `cd C:\NUCLEAR\viz && python -m http.server 8099` → open `http://127.0.0.1:8099/core-view.html` →
  IGNITE (or Space) · drag=orbit · scroll=zoom · SLOW-MO toggle · RESET.
- **[2026-08-06-c ~T8] `main` = `b782c67`** (loading overlay + WebGL fallback committed; verified the
  app still renders). Core-view render + module at a **strong, complete final state**.
- **[2026-08-06-c ~T7] `main` = `6d25778`** (chromatic bokeh). Added loading overlay + WebGL fallback
  to `core-view.html` (done → T8).
- **[2026-08-06-c ~T6] Added chromatic bokeh + regenerated gallery (`1ff8835`), committed chroma
  (`6d25778`).** Full effect stack: PBR nuclei (detail-2) · warm-key/cool-rim lighting · custom
  rack-focus DOF (30-tap, chromatic) · god-rays · UnrealBloom · filmic teal-orange grade (sharpen/
  CA/vignette/grain) · nebula backdrop · tamed peak. Done → T7.
- **[2026-08-06-c ~T5] Regenerated `docs/gallery/` JPGs from the FINAL look** — done → T6.
- **[2026-08-06-c ~T4] `main` = `63546d3`** (committed+pushed: nuclei icosphere detail 1→2 +
  tamed peak white-out). Verified eyes-on: smoother focal beads + peak now shows nuclei/neutron-spray
  structure. **NEXT:** a couple more small polish rounds (neutron glow readability), then regenerate
  the `docs/gallery/` JPGs from the FINAL look (the committed gallery @717d65c is from an earlier
  core-view state). Core-view.js currently CLEAN (all committed).
- **[2026-08-06-c ~T3] Refined core-view** (nuclei detail 1→2 + peak taming) — done → T4.
- **[2026-08-06-c ~T2] PUSH INTEGRATED. `main` = `origin/main` = `7e4dd1b`** (rebased DOF+nebula +
  this handoff, on top of the repo-refresh README+gallery `717d65c`). CI will run (~45 min; viz/docs
  only, trivially green). -e WIP preserved. **NEXT: continue the core-view hyperreal refinement**
  (owner: keep going). Discipline reminder: keep this handoff current ON DISK before/after each
  action (crash-safety); commit it periodically, not every edit.
- **[2026-08-06-c ~T1] Integrating the diverged push** via `git rebase --autostash origin/main` +
  commit handoff + push. (Done → T2.)
- **[2026-08-06-c ~T0] Wrote this handoff.** Context ~80%. `origin/main`=`717d65c` (repo-refresh
  README+gallery); local `main`=`8f7b460` (DOF+nebula) diverged (ahead 1/behind 1, no conflict).
  - Working tree: the **-e viz session's uncommitted WIP** (`viz/README.md`, `viz/index.html`,
    `viz/js/{device,main,simstub}.js`, `viz/style.css` + untracked `viz/js/{pitscope,scenario}.js`,
    `viz/START-GADGETLAB.bat`, `viz/gadgetlab-standalone.html`, `viz/build-standalone.mjs`,
    `.zcode/`, `claude_chat_export_*`). **NOT MINE — never stage/commit these.**
  - Viz `core-view` is at a strong hyperreal state; a Python static server is running on :8099
    (background) + the headless shot harness is live in the scratchpad.
- **[pending]** After handoff: integrate the push, then continue refining the core-view aesthetic
  (owner: "keep going until 100% context; I'll manage context").

---

## 1. WHAT THIS PROJECT IS (read this to understand the soul)

A **spec-first, benchmark-provable GPU Monte-Carlo simulator of the Trinity ("Gadget") implosion
device**, whose SOUL is an **interactive, real-physics 3D volumetric visualization of the demon-core
chain reaction** (zoom + slow-mo, ~10–50k bundled "atoms" splitting, matching the transport math).

- **Two products, one repo (ADR-011, ADR-023, `core-vision-3d-chain-reaction` memory):**
  - **MIT GitHub = the validated ENGINE** (headless nukebench/MCTS/gates = *validation*, not the
    product). Repo: <https://github.com/bochen2029-pixel/nuclear-sim> (public).
  - **Eventual Steam release = the visual "nukestudio"** (the 3D sandbox; spirit of the owner's
    *DF-41 Simulator*). Real validated physics (the gates / `fast4`) is the differentiator.
- **Honesty invariants (non-negotiable):** nothing faked; Σ ∝ ρ; **NEVER tune the xs OR the group
  structure to a benchmark** (ADR-022); representative sampling is labeled; no real classified
  weapon data (public-derived only, `NOTICE.md`, `00-overview.md §2–3`, BLK-14).
- **Two PARALLEL work tracks share one git tree / one branch (`main`):**
  - **MAIN track** (this session's lineage) = the physics/engine (`src/`, `spec/`, `tools/`,
    `data/`) + the NEW `viz/core-view.*` module.
  - **`-e` track** = the browser viz/UI "GadgetLab" (`viz/js/main.js`, `pitscope.js`, `device.js`,
    `scenario.js`, `simstub.js`, `index.html`, `style.css`, `README.md`). It carries **uncommitted
    WIP** in the shared tree. **DO NOT EDIT `-e` FILES FROM THE MAIN TRACK** (memory
    `viz-seam-coordination`). The two coordinate via spec-stable data contracts.

**Read-order for a fresh session:** THIS file → `spec/PROGRESS.md` (living task/gate state) →
`spec/DECISIONS.md` (ADRs, esp. 021/022/023/024/025) → the auto-loaded memories → then work.

---

## 2. CURRENT STATE — THE HEADLINE (2026-08-06-c)

**Physics: `main` green, TWO passing gates.**
- **G0c PASS** (ref↔gpu cross-backend equivalence, mean |Δk| 6.3 pcm).
- **G0a Godiva PASS** — **the 2nd passing gate, shipped THIS session.** Self-consistent per-assembly
  weighting (ADR-025) took Godiva **+2560 → −270 pcm (WITHIN ±500)**, committed C-900 report.
- **G0b Jezebel: honest FAIL, +1315 pcm** (improved from +1601; ACCEPTED as an honest ADR-022
  residual — de-risked: neither self-consistent weighting NOR finer groups NOR the two combined
  (+1241) close it; it is the genuinely hard gate, limited by the coarse-multigroup approximation).
- **G1a: honest FAIL** (uncompressed canonical ~1.029 vs C-934 band [0.84, 0.96] — the device-scale
  over-estimate; measured informally this session, not yet a formal committed gate).
- **VERIFY: 193 dev / 177 CI green** (last full run this session was on the ADR-025 set).

**Viz: the NEW `core-view` module is a strong hyperreal demon-core view (owner's "main course").**
- `viz/core-view.html` + `viz/js/core-view.js` — a **self-contained** AAA module (zero collision
  with `-e` files) rendering the ADR-023 volumetric chain-reaction: true-3D **instanced** nuclei
  (never sprites), per-instance emissive fission flashes, streaking neutrons, a living burst core,
  and a **UE5/Modern-Warfare-register** post stack (custom rack-focus DOF + volumetric god-rays +
  UnrealBloom + filmic teal-orange grade + PBR + dramatic lighting + nebula backdrop). Owner-steered
  aesthetic. **Still being refined** (owner: keep pushing).
- The `-e` GadgetLab (`viz/js/main.js` + `pitscope.js`) is the parallel prototype; `pitscope.js` was
  the original flat-`THREE.Points` chain-reaction microscope — `core-view` supersedes it visually.
- The public README + `docs/gallery/` (5 core-view render JPGs) were refreshed on main (`717d65c`).

---

## 3. THE PHYSICS TRACK (engine) — detail

**Milestones:** M0 done; M1 mostly done (CPU ref transport + bare-sphere gates); M4 (GPU event
transport + k-eigen) done + differentially validated; M2/M3 done (materials, hydro tiers, tally,
coupling, the demon-core fusion API `src/api`); M5 4/5 done (checkpoint/resume, store, the
`nukefarm` batch-sweep CLI + MCTS sampler); only M5-T5 (container/cloud) + M7 (the visual studio)
remain. Full task table: `spec/PROGRESS.md`.

**The `fast4` cross-section set (the credibility keystone):**
- `data/xs/fast4.json` — **16 isotopes, 5-group (4 fast + 1 thermal, schema v3)**, collapsed from
  public **ENDF/B-VIII.0** (NNDC, NOT ICSBEP — BLK-14 respected) via `tools/xs/` under WSL.
- **Weighting = ADR-025 self-consistent per-assembly:** the 8 benchmark isotopes are collapsed
  under each assembly's OWN openmc fundamental-mode flux (`data/xs/weights/{godiva,jezebel}.spectrum.json`,
  map `fast4_weights.json`); the 8 structural isotopes keep the analytic fast-metal weight. openmc
  is a flux-SHAPE calculator only — NO xs touched. Provenance: `data/xs/PROVENANCE-fast4.md`.
- **The WSL openmc pipeline is INTACT** (memory `fast4-wsl-pipeline-false-blocker`): activate with
  `. /root/miniforge3/etc/profile.d/conda.sh && conda activate omc` (openmc 0.15.3); data at
  `/root/nukesim_hdf5/endfb80-lowtemp/` (16 `.h5` + a `cross_sections.xml`, so full openmc transport
  works). `np.trapz` gone → use `np.trapezoid`. NEVER pipe `conda activate` into anything.

**Key ADRs (`spec/DECISIONS.md`):**
- **ADR-021** — consistent transport correction (collide on σ_tr; μ̄ has ~17000 pcm leverage on Godiva).
- **ADR-022** — the coarse few-group set can't clear ±500 honestly; two non-tuning paths: (a) self-
  consistent weighting, (b) finer structure. ACCEPTS honest FAILs.
- **ADR-023** — the demon-core 3D chain-reaction view is a normative M7 deliverable (one holistic
  spacetime, TRUE-3D volumetric particles NEVER sprites, zoom=camera-focus, representative sampling
  tracking the real `FissionSource::sites`).
- **ADR-024** — the thermal group (5-group schema v3).
- **ADR-025** — self-consistent per-assembly weighting (ADR-022 path a) → **G0a PASS**.

**Memories (auto-loaded, `C:\Users\user\.claude\projects\C--NUCLEAR\memory\`):**
`fast-group-residual-derisk` (CORRECTED: weighting closes Godiva, Jezebel is the hard one — both
levers + combined all measured insufficient) · `g1a-canonical-supercritical-finding` ·
`fast4-wsl-pipeline-false-blocker` · `core-vision-3d-chain-reaction` · `autonomous-cadence` ·
`viz-seam-coordination` · `msbuild-stale-binary-trap` · `repo-refresh-coordination`.

---

## 4. THE VIZ TRACK — detail

**`viz/core-view.js` (MAIN track's, MINE to edit) — structure:**
- Three.js r160 (CDN via importmap in `core-view.html`). Renderer: ACES tone-map, PCF soft shadows,
  `preserveDrawingBuffer:true` (for the headless capture).
- **Post pipeline (order matters):** `RenderPass → DofShader (custom rack-focus DOF, 30-tap
  golden-angle disc, HDR-safe) → GodRaysShader (screen-space crepuscular shafts from centre) →
  UnrealBloomPass → OutputPass → GradeShader (filmic teal-orange + unsharp sharpen + CA + vignette +
  grain)`. **BokehPass does NOT work here** (breaks the HDR bloom pipeline → black) — the custom DOF
  is the replacement.
- **Geometry:** `nuclei` = `InstancedMesh` of icospheres (24000), PBR `MeshStandardMaterial`
  (metallic) + a per-instance `aGlow` emissive attribute injected via `onBeforeCompile` (the fission
  flash). `neutrons` = `InstancedMesh` (8000 cap) velocity-stretched into streaks. `core` = additive
  emissive sphere + glow shells (the burst heart). Backdrop = graded canvas + nebula wisps.
- **Sim:** synthetic but spec-shaped (representative sampling; a spatial-hash transport with
  capture→fission→ν, leakage; a phase timeline idle→init→growth→peak→quench→afterglow; burst
  intensity driven by `nAlive/MAXN`; auto time-dilation through the burst). The seam the real
  `ns::api generate_run` `GenerationSample` stream will later drive (see `docs/FUSION-BINDING.md`).
- **`window.__cv` debug/agent-eyes hook (CRITICAL for iteration):** `.shot(maxW,q)` → JPEG data URL
  of the composited canvas; `.pose(eye,target)` → move camera; `.step(dt,render)` → deterministic
  fixed-dt advance (framerate-independent capture); `.pause(v)`; `.ignite()`, `.reset()`, `.state`.
- **Run it:** `cd viz && python -m http.server 8099` → `http://127.0.0.1:8099/core-view.html`.

**`-e` GadgetLab (DO NOT EDIT from main):** `viz/js/main.js` (scene/renderer/UI/clock),
`pitscope.js` (the old flat-points microscope), `device.js`, `scenario.js` (the `03 §4` contract as
JS), `simstub.js` (the synthetic fusion seam emitting `03 §5/§6` shapes), `studio-adapter.js`.

---

## 5. THE AUTONOMOUS SCREENSHOT HARNESS (the capability that unlocks eyes-on iteration)

The in-app browser pane can't composite screenshots headless. Solution (owner's Booster-Lander
insight; ref `C:/Booster_Lander_Simulator/ui/src/hud/screenshot.ts` for the in-page RT-capture idea):
a **headless Chrome harness** that screenshots straight to disk.
- Location: `<scratchpad>/shot/` (scratchpad = `C:\Users\user\AppData\Local\Temp\claude\C--NUCLEAR\
  c92df3e5-ace9-4f52-892b-a93b0679b892\scratchpad`). `npm install puppeteer-core` (uses **system
  Chrome** at `C:/Program Files/Google/Chrome/Application/chrome.exe`, no download).
- `shot.mjs`: launches headless Chrome (`--use-gl=angle --use-angle=swiftshader`), loads the URL,
  optionally `--ignite --steps=N --pose=ex,ey,ez` (deterministic advance via `__cv.step`), then
  `page.screenshot()` to a PNG. Usage: `node shot.mjs out.png --ignite --steps=30 --pose=4,1.7,5
  --w=1920 --h=1080`. **Then `Read` the PNG to SEE the render.** SwiftShader is slow (~30–50s/hi-res
  shot) but reliable; the RTX box could do hardware GL if needed.
- The harness is a dev tool (has a `node_modules`); it lives in the scratchpad, NOT committed. Recreate
  it in a fresh session with the above (it is the single most useful capability for viz iteration).

---

## 6. GIT STATE + DISCIPLINE (CRITICAL — shared tree, multiple pushers)

- **`main` is pushed to by MULTIPLE independent sessions** (this one + spawned repo tasks + the -e
  session). So **fetch/rebase before pushing**; expect non-fast-forward rejections. Resolve with
  `git rebase --autostash origin/main && git push origin main` (autostash preserves the -e WIP; our
  commits touch `core-view.*` / physics files that don't collide with README/gallery/-e-viz).
- **NEVER `git add -A` / `git add .` / `git checkout -- .`** — that would sweep or revert the -e
  session's uncommitted WIP. **Stage EXPLICIT paths only.** Before EVERY commit, run
  `git diff --cached --name-only | grep -E '^viz/(README|index|style)|viz/js/(main|pitscope|device|scenario|simstub)|\.zcode|claude_chat'` and abort if it matches.
- **Mine to commit:** `viz/core-view.{html,js}`, `src/**`, `spec/**`, `tools/**`, `data/**`,
  `tests/**`, `docs/**`, `artifacts/**`. **NOT mine:** the -e files listed in §0/§4.
- **Space out main pushes; watch CI to green** (`gh run watch <id> --exit-status`; ~45–50 min per
  run on this repo — the "watch to green" genuinely means ~45 min). CI = CPU-only (177); the 16
  `gpu.` tests + real G0c are dev-only.
- Recent `main` (origin): `717d65c` (README+gallery) ← `bc0d961` (hyperreal pass) ← `69d3b96`
  (core-view module) ← `babbe9a` (Jezebel de-risk docs) ← `7fb7541` (G0a PASS gate reports) ←
  `d0dfdd6` (ADR-025 self-consistent weighting).

---

## 7. TRAPS (each cost real time historically)

- **Paths in shell commands:** write Windows paths with **forward slashes** (`C:/NUCLEAR`) or quote
  backslash paths — Git Bash eats unquoted backslashes (machine-wide CLAUDE.md).
- **MSBuild stale-binary** (memory): `cmake --build` can exit 0 yet not recompile an edited `.cu`/
  relink an exe → false-green on STALE binaries. This session: `gate_probe.exe` was left as a stale
  17-group de-risk build; had to rebuild. Verify recompiles happened before trusting a result.
- **WSL openmc** (§3): the pipeline is intact; the trap is falsely declaring it gone / using the
  wrong dir (`nukesim_hdf5` not `nukesim_endf`) / base python (openmc only in the `omc` env).
- **`Math.random()`/`Date.now()`:** BANNED in Workflow-tool scripts (break resume); **FINE in
  browser JS** (`core-view.js` uses them freely).
- **BokehPass** breaks the HDR bloom pipeline (black screen) — use the custom DOF pass.
- **Async on Windows:** detach with `Start-Process`, never `Start-Job`; watch an artifact/marker,
  never a bare PID (Windows recycles PIDs).

---

## 8. OPEN DECISIONS / READY-QUEUE (what to do next)

- **VIZ (active, owner-directed "keep going"):** keep refining the `core-view` hyperreal aesthetic
  (UE5/MW register). Candidate levers not yet done: chromatic bokeh, a defined plasma core (the peak
  whites out), motion polish, a cinematic intro camera. Iterate eyes-on via the harness (§5).
- **PHYSICS (accepted residuals; owner to steer if resumed):** Jezebel/G0b stays an honest ADR-022
  residual (do NOT re-try modest weighting/structure tweaks — all measured insufficient, incl. the
  9-group+self-consistent combination at +1241). A native ref C-900 G0a report is an optional
  follow-up (the committed one is gpu-backend, G0c-equivalent). Formal G1a (gates.toml + the C-933
  mass-fraction critical-mass search) is a real task. M5-T5 (container/cloud). The fusion binding JS
  half (`viz/simstub.js`→real engine) is the -e track's.
- **REPO:** README + gallery refreshed (`717d65c`). When the core-view aesthetic is FINAL, regenerate
  the gallery JPGs from the finalized look (harness §5) and update `docs/gallery/`.

---

## 9. OPERATIONAL CHEAT-SHEET

```bash
# VERIFY (canonical loop; 193 dev / 177 CI)
cmake --preset win-x64 && cmake --build --preset win-x64-rel && ctest --preset win-x64-rel

# measure a gate (ref transport)
./build/win-x64/Release/gate_probe.exe data/scenarios/godiva.toml --repo C:/NUCLEAR --batch 120000 --inactive 30 --active 120 --seed 1
# committed C-900 gate report (gpu backend, fast; G0c-equivalent)
./build/win-x64/Release/nukebench.exe gate --gate G0a --backend gpu --git <hash> --repo C:/NUCLEAR

# regenerate the xs set (WSL omc env, from /mnt/c/NUCLEAR)
wsl bash -lc '. /root/miniforge3/etc/profile.d/conda.sh && conda activate omc && cd /mnt/c/NUCLEAR && \
  python tools/xs/assembly_spectrum.py --out data/xs/weights && \
  python tools/xs/build_fast4.py --hdf5 /root/nukesim_hdf5/endfb80-lowtemp/neutron --out data/xs/fast4.json \
  --weights data/xs/weights/fast4_weights.json --weights-dir data/xs/weights \
  --isotopes U234,U235,U238,Pu239,Pu240,Pu241,Ga69,Ga71,Al27,B10,Be9,C12,H1,N14,O16,Po210'

# run the viz + capture the core-view (headless, eyes-on)
cd viz && python -m http.server 8099          # serve
cd <scratchpad>/shot && node shot.mjs out.png --ignite --steps=30 --pose=4,1.7,5 --w=1920 --h=1080
```

---

## 10. THIS SESSION (2026-08-06-c) — what happened

Started from `docs/HANDOFF-2026-08-06-b.md` (thermal group shipped). Arc:
1. **Owner picked "self-consistent weighting"** at the §5 fork. Built ADR-025: openmc per-assembly
   fundamental-mode flux weights (`tools/xs/assembly_spectrum.py`), per-isotope weight support in
   `build_fast4.py`, re-collapsed the 16-isotope `fast4.json`. **G0a Godiva flipped fail→PASS**
   (−270 pcm); structural isotopes byte-identical; VERIFY green 193/193; committed C-900 gate
   reports. Corrected the `fast-group-residual-derisk` memory (the inversion).
2. **Jezebel de-risk** (owner time-boxed): 9-group + self-consistent = +1241, still out → ACCEPTED
   as honest residual. **G1a-k** measured: ~1.029, honest fail.
3. **Pivoted to the viz "main course."** Built the NEW `viz/core-view.*` self-contained module (the
   AAA demon-core view), the **autonomous screenshot harness** (Booster-Lander insight), and pushed
   it hard into a **hyperreal UE5/MW register** (owner-steered): PBR + dramatic lighting + custom DOF
   + god-rays + bloom + filmic grade + nebula. Committed `69d3b96`, `bc0d961`, `8f7b460` (local).
4. A spawned repo-refresh session refreshed the README + gallery (`717d65c` on origin).
5. **Owner directive (this point):** maintain THIS always-current handoff; keep refining the viz.

**Immediate next:** integrate the diverged push (`git rebase --autostash origin/main && git push`),
then continue the core-view hyperreal polish.
