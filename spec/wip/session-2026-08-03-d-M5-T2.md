# WIP — session-2026-08-03-d — M5-T2 (artifact store + SQLite + unit_id idempotency)

Task: `03 §7` `sweep.db` results store in `src/core/store/`. Append one line per
non-obvious finding BEFORE acting on it.

- SYNC-M5b (recorded in PROGRESS): store keys on `unit_id` as an opaque STRING →
  `src/core/store/` depends only on `sqlite3` (already in `nukesim_deps`), NOT on
  `src/api/`. Mirrors M5-T1 (generic core container; owning layer supplies semantics).
- Scope: the store LIBRARY only. nukefarm CLI + FS queue + samplers = M5-T3/T4 (need the
  sweep engine); a nukefarm shell now = engine-less frontend (SYNC-M1 anti-pattern).
- Schema verbatim `03 §7`: `runs(unit_id TEXT PK, params_json TEXT, tally_json TEXT,
  wall_s REAL, status TEXT)` + `cursor(state_json TEXT)`.
- Idempotency = INSERT-OR-IGNORE on the `unit_id` PK; `record_run` returns whether it
  inserted (false = the row already existed = the "second finisher's write is a no-op",
  `06 §2`/D6). A rerun of a recorded unit is a no-op.
- `params_json`/`tally_json` are arbitrary JSON (quotes, braces) → MUST bind via
  `sqlite3_prepare_v2` + `sqlite3_bind_text`, never string-concatenate (quoting/injection).
- `<sqlite3.h>` stays in the .cpp; the header forward-declares `struct sqlite3;` (opaque),
  like `checkpoint.h` keeps its sections opaque.
- `cursor(state_json)` is a single-column singleton table → set = clear+insert in a txn;
  get = SELECT … LIMIT 1 → optional.
- Non-copyable, non-movable (owns a `sqlite3*`); callers needing to move use unique_ptr —
  least-risk vs a hand-written move that could double-close.
- Test DB: `:memory:` for behaviour + a temp-file DB (build/temp dir, cleaned up) for the
  reopen-persistence case. `sweep.db` is untracked (`02 §2`), so nothing lands in the repo.
- ASCII Catch2 test names, NO commas / em-dashes (M5-T1 got bitten twice — breaks the
  ctest↔Catch2 filter). Prefix `store.`; keep the anchored-probe sum true in PROGRESS.
