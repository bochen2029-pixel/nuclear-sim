# WIP — session-2026-08-02-b — M0-T2 (toolchain)

One line per non-obvious finding, dead end, or rejected approach, written BEFORE acting on it (README §5.4b).

- **Session-id date discrepancy.** The system clock and every git commit date read `2026-08-02` (last commit `bdf89e7` at 2026-08-02 03:44:33 -0500; this session started 03:51). The two most recent SESSIONS.md entries are labelled `2026-08-03` and their author called itself `session-2026-08-03-a`. The log's dates run one day ahead of the machine. Protocol (§5.4) says "scan SESSIONS.md for today's date, take the next unused letter" — today is 2026-08-02, `-a` is taken by the spec-authoring session, so this session is `session-2026-08-02-b`. Recording rather than silently smoothing: the SESSIONS log is append-only and will now read 08-02, 08-03, 08-03, 08-02, which looks like a clock rollback and is not one.

- **The NEXT ACTION's premise is half wrong: `C:\Astrophage` has no presets and no vcpkg.** PROGRESS.md and `07 §M0-T2` both say to "port the CMake/preset idioms from `C:\Astrophage`". Astrophage has **no `CMakePresets.json`** and **no `vcpkg.json`** — it uses `FetchContent` and a hand-rolled `scripts/build.ps1` that locates cmake/ninja and imports `vcvars64.bat` itself. So there are no preset or vcpkg idioms there to port. What IS portable from it, and is being ported: `CMAKE_CUDA_ARCHITECTURES` set *before* `project()` (CMake's CUDA detection reads it there); `LANGUAGES C CXX CUDA` with the C-is-required-for-GLFW/GLAD note (bites at M7); the per-target `astro_strict()` warnings function, which is exactly `02 §4`'s "per-target, never globally"; static CUDA + MSVC runtimes; `find_package(CUDAToolkit REQUIRED)`; and the `gate.ps1` milestone-runner shape. The **vcpkg manifest-preset idiom comes from `C:\backrooms`** instead — which is what PROGRESS.md's own Environment-notes line actually says ("`C:\backrooms` (vcpkg manifest presets)"). The NEXT ACTION line collapsed the two siblings into one.

- **vcpkg baseline SHA.** `C:\vcpkg` HEAD = `d592849579fb1fb22f87406b2184522ea21a8783` (2026-06-12). `C:\backrooms` pins that exact SHA as its `builtin-baseline`, so it is a known-good baseline on this machine, not an untested guess. Pinning the same one.

- **`12 §1` names the wrong file for the baseline pin.** It says "**`vcpkg-configuration.json` MUST pin a `builtin-baseline` commit SHA**". `builtin-baseline` is a `vcpkg.json` field; `vcpkg-configuration.json`'s equivalent is `default-registry.baseline`. vcpkg rejects a manifest that specifies both. Testing which combination configures cleanly before deciding; whichever way it lands, `12 §1` needs a clarification edit + CHANGELOG line (typo/clarification class, §6 — no ADR).

- **Generator choice is a VERIFY-probe correctness question, not a taste question.** DoD VERIFY is the bare `cmake --preset win-x64 && cmake --build --preset win-x64-rel`. With the Ninja generator that command fails outside a VS developer shell (no `cl.exe`), so the next session running verify-first from Git Bash would see red on a green tree — a probe that lies. Checking whether the CUDA VS-integration MSBuild files are installed; if they are, the `Visual Studio 17 2022` generator makes the probe shell-independent and is natively multi-config (which is what the `<configure>-deb`/`<configure>-rel` preset naming in `12 §1` presupposes).

- **"Empty vcpkg cache" (DoD) will NOT be done by deleting the user's global binary cache.** `%LOCALAPPDATA%\vcpkg\archives` is shared with every other project on this machine. Pointing `VCPKG_DEFAULT_BINARY_CACHE` at a scratch dir for the verification run proves the same thing — no hidden cache state — without wrecking sibling projects' build times.
