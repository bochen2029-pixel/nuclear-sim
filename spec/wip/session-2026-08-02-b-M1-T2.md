# WIP — session-2026-08-02-b — M1-T2 (ref/ history-based transport)

One line per non-obvious finding, dead end, or rejected approach, written BEFORE acting on it (README §5.4b).

- **This is the first task whose DoD is physics, not plumbing.** Both checks are analytic: pure-capturer leakage must equal `exp(−Σ_c·R)` within 1σ at three optical depths, and infinite-medium `k_inf` must equal `ν̄Σ_f/(Σ_c+Σ_f)` within 3σ. Writing both oracles from the closed form BEFORE the transport loop, so the loop is written against a target rather than the target being read off the loop.
- **QC-09: the leakage test REQUIRES Σ_f = Σ_s = 0.** With scattering on, `exp(−Σ_c·R)` is not the leakage — it is the uncollided flux. A test that leaves Σ_s non-zero and loosens the tolerance to compensate is measuring nothing.
- **ADR-012 item 1 adopted FULL implicit capture**, not analog-with-implicit-absorption. The estimator must match `01 §2 E1c`; getting this wrong double-counts Σ_f/Σ_t and shifts k by a few hundred pcm — inside the range where it still looks plausible.
