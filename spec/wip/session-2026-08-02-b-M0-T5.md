# WIP — session-2026-08-02-b — M0-T5 (xs / materials / scenario loaders)

One line per non-obvious finding, dead end, or rejected approach, written BEFORE acting on it (README §5.4b).

- Reading `04 §3`–`§6` (module APIs) as well as `03 §2`–`§4` (data contracts) BEFORE writing. M0-T3-a shipped four defects by reading only the `03` contract and skipping the `04` module spec; that is now a standing lesson, not a one-off.
