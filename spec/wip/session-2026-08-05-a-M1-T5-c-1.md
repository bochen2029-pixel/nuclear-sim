# WIP — M1-T5-c-1 (commit the real C-900 G0a/G0b gate_report.json evidence)

**Session:** session-2026-08-05-a · **Claimed:** 2026-08-05 · **Branch:** `task/M1-T5-c-1-gate-evidence`
**Depends on:** M1-T5-b (the `nukebench gate` runner + `gate_report.json`, done).

## Goal (tight scope)

The credibility keystone: turn the ad-hoc G0a/G0b measurements into **committed, honest gate
evidence** the gate table cites. QC-07 (`08 §3`): *a gate claim whose report is not in the
repository is void.* The gate table currently says "Formal gate_report pending M1-T5".

**Deliverable:**
1. Complete the report **provenance** the M1-T5-b `cli_gate` left empty: `git` (short HEAD hash),
   `device`, and `started`/`finished` (ISO-8601 UTC) — a committed evidence artifact with a blank
   `git` field is not defensible provenance (this project's whole ethos).
2. Generate the real **C-900** (`1e6 / 50 / 200`, C-900) `gate_report.json` for **G0a (Godiva)** and
   **G0b (Jezebel)** over the normative 5-seed set on the **ref** oracle.
3. Commit them to `artifacts/gate_reports/{G0a,G0b}/gate_report.json` and cite the paths in the
   PROGRESS gate table.

**Out of scope (→ c-2 / c-3):** the gpu gate backend (c-2); `nukebench run`/`diff` + G0c (c-3).

## Discipline

- **Do NOT fake a pass.** G0a/G0b are honest **FAILs** (+2562 / +1581 pcm, ADR-022 — the coarse
  4-group residual, not the data). The committed report records `verdict: fail`, exit 4. Never
  touch the xs to move the pcm.
- **ref oracle, not gpu.** G0a/G0b are absolute-k gates defined against the reference MC; the GPU
  is float32 per-event (`01 §9`) and validated only to *statistical* equivalence (that is G0c, c-3).
- Provenance honest: `backend: "ref"`, real `device`, real git hash of the code that produced the
  measurement, real timestamps. The `dirty` flag is per `03 §11`; a FAIL verdict is unaffected by
  dirty (conditional only downgrades a PASS).
- Shared tree with the -e (viz) session: stage **explicit paths only**, never `git add -A`; verify
  no `viz/` / `.zcode/` in the staged set before every commit.

## Plan

1. Add `--git` + `--device` options to `nukebench` `main.cpp`; thread them + auto-filled
   `started`/`finished` (chrono UTC → ISO-8601) through `cli_gate` → `GateReport`.
2. A `nukebench.` test that the provenance fields are populated (git/device threaded,
   started/finished non-empty, ordering finished ≥ started).
3. VERIFY green (expect 182: +1 provenance test).
4. Build the release `nukebench.exe`; run G0a + G0b at **full C-900** (default batch) in the
   background (~25 min/gate on ref, 5 seeds each — run the two gates in parallel).
5. Confirm both are honest FAILs at ~+2562 / ~+1581 pcm; commit the two `gate_report.json` under
   `artifacts/gate_reports/` (un-ignored per `.gitignore` gate-evidence rule, QC-07).
6. Cite the committed report paths in the PROGRESS gate table; SESSIONS + CHANGELOG; fold-delete
   this journal; merge green; watch CI.

## Findings (append before acting)

- (init) `run_gate` (M1-T5-b) already measures k/sigma_pcm/k_deviation_pcm per seed and evaluates
  the criteria; `cli_gate` fills `gates_toml_sha256`/`spec_sha256`/`code_version`/`dirty` but leaves
  `git`/`device`/`started`/`finished` empty. Git hash in this codebase is a caller-supplied string
  (checkpoint `identity.git_hash`, run.json `git`) — no in-C++ auto-detection — so `main.cpp` will
  pass it (via `--git`, defaulting to a `git rev-parse` at invocation time from the shell).
- gates.toml G0a/G0b: seeds `[1..5]`, `[gate.eigen]` batch `1000000` / inactive `50` / active `200`
  (= C-900); criteria `k_deviation_pcm abs_le C-930(500)` + `sigma_pcm le C-931(25)`.
