# NUCLEAR-SIM — Session Handoff (2026-08-06) → the gate CLI closed, G0c a full PASS, the cross-section set completed, and a real 4-group limit found at the device scale

*Written by `session-2026-08-05-b` at the end of a very large multi-thread run (it spanned the 08-05→08-06
boundary). Read this, then `spec/PROGRESS.md` + the last ~6 `spec/SESSIONS.md` entries (authoritative living
state), then the auto-memories. If anything here disagrees with `PROGRESS.md`, trust `PROGRESS.md` and re-run
VERIFY.*

---

## 0. TL;DR (60 seconds)

- **`main` @ `0b4f687`, fully green: 193 dev / 177 CI.** Every increment CI-verified green.
  VERIFY: `cmake --preset win-x64 && cmake --build --preset win-x64-rel && ctest --preset win-x64-rel`.
- **Five commits this session**, each milestone-advancing and CI-green:
  1. `660fa4c` **M1-T5-c-4** `nukebench run` → the `03 §6` bundle — the `06 §1` run/gate/diff triad is COMPLETE.
  2. `f14a6bb` **M2-T3** k-vs-compression scan tool (`kscan`) + committed CSV curve — **M2 complete**.
  3. `d321f07` **M1-T5-c-5** G0c criteria (b) per-shell + (c) population-series — the full G0c mechanism.
  4. `7a8df8d` **M1-T5-c-5b** committed **C-900 G0c (a)+(b)+(c) report — the first FULL cross-backend PASS**.
  5. `0b4f687` **M1-T4a-2b** fast4-b → **`data/xs/fast4.json` is now the full 16 isotopes** (+ 2 real pipeline
     bug fixes) — unblocks G1a/G1b + full-device transport.
- **Gate ledger:** G0a/G0b honest **FAIL** (4-group residual, ADR-022); **G0c full PASS** (all three 08 §2
  criteria, committed at C-900); G1–G5 later.
- **Live frontier — G1a de-risked, NOT built.** The first multi-layer eigen revealed the canonical device reads
  **k=1.042 supercritical uncompressed** — resolved to a real 4-group thermal-group limit (NOT a transport bug).
  See §3 + the memory `g1a-canonical-supercritical-finding`. **Do not build the G1a/G1b gate until the
  group-structure question is settled.**
- Two owner-requested outreach emails (Alex Wellerstein, Melinda Green) drafted in the owner's voice and **sent**.

---

## 1. What this session shipped (the arc)

| # | Increment | Commit | Essence |
|---|---|---|---|
| 1 | **M1-T5-c-4** `nukebench run` | `660fa4c` | scenario cfg → the `03 §6` bundle `{run.json, tally.json}`; reuses the `src/api` studio surface; completes `06 §1` |
| 2 | **M2-T3** k-vs-compression scan | `f14a6bb` | `kscan` + `scan.{h,cpp}` reusing `ref_eigen_fn_masscons` (Σ∝ρ, no backwards trap); committed `data/benchmarks/compression_scan_jezebel.csv` (k 1.016→1.47) |
| 3 | **M1-T5-c-5** G0c (b)/(c) | `d321f07` | `diff_criteria.{h,cpp}` (pure, CI-verifiable) + `EigenResultGpu.k_history`/`per_shell_source` (bit-identical) + `run_diff` evaluates all 4 criteria. **Two subtle bugs fixed** (the (c) σ_k random-walk; the (b) count normalization) |
| 4 | **M1-T5-c-5b** committed G0c report | `7a8df8d` | C-900 (a)+(b)+(c): mean \|Δk\| 6.3 pcm, per-shell 0.46-0.57, population 0.30-0.64 — all PASS |
| 5 | **M1-T4a-2b** fast4-b | `0b4f687` | the 8 b-set isotopes → 16-isotope fast4.json + 2 pipeline fixes (see §4); gate-safe (Jezebel byte-identical) |
| — | G1a de-risk | (branch, uncommitted) | the §3 finding; `gate_probe` extended to multi-layer + `--layers` |

---

## 2. Where to look (read-order after this doc)

- `spec/PROGRESS.md` — living state (task table, gate ledger, ready-queue, NEXT ACTION, VERIFY). **Ground truth.**
- `spec/SESSIONS.md` — the last ~6 entries are all this session; their "Notes for next session" are for YOU.
- **Auto-memories** (`C:\Users\user\.claude\projects\C--NUCLEAR\memory\`, auto-loaded): **`g1a-canonical-supercritical-finding`** (the live frontier — read it before touching G1a), `msbuild-stale-binary-trap` (a Windows+CUDA build gotcha that cost real time this session), plus `core-vision-3d-chain-reaction`, `autonomous-cadence`, `viz-seam-coordination`.
- `data/xs/PROVENANCE-fast4.md` — the full 16-isotope provenance + the b-set pipeline enhancements + the modeling notes.
- Code: `src/app/nukebench/` (`diff_criteria`, `scan`, `cli`, `gate_report`); `tools/xs/build_fast4.py` (the enhanced collapse) + `gate_probe.cpp` (now multi-layer).

---

## 3. The live frontier — G1a de-risk found a real 4-group limit (READ before building G1a/G1b)

Owner directed G1a/G1b (static criticality + compression on the canonical pit, now unblocked by fast4-b). Before
building the gate I de-risked by extending `gate_probe` to the full multi-layer stack and running the **first
multi-layer/multi-material eigen** (all prior gates are single-layer bare spheres). Result: the full canonical
assembly reads **k = 1.042 uncompressed (SUPERCRITICAL)**, when the device must be subcritical (C-052: the pit is
0.78 of a tamped critical mass; the G1a band is [0.84, 0.96]).

**Structural k breakdown** (`gate_probe --layers N`, batch 40k): bare pit **0.745** → +tamper **0.992** →
+b_shell 0.996 → +pusher(Al) **1.026** → +HE booster **1.042**.

**Resolution — NOT a transport bug.** The pit (0.745) and the tamped core (0.992) are physical, so the multi-layer
transport is validated. The whole +4600 pcm is the **light-moderating outer layers** (Al pusher + the 22.5-cm HE
booster) now moderating (the correct fast4-b elastic-downscatter fix, A<30 — which includes Al) interacting with
the **4-group structure's missing thermal group** (bounds bottom at 1 keV): moderated neutrons pile into group 3
(1-100 keV) with elevated Pu-239 fission and no thermalization-to-capture sink. A physically-correct fix EXPOSED a
4-group thermal-inadequacy (and the A<30 isotropic threshold likely over-moderates Al — the anisotropic-elastic
follow-up would soften it).

**Next-session options (owner-relevant — this is a `03 §2` group-structure question, credibility-bearing):**
- Build G1a on **pit + tamper** (k=0.992, the reflected core) — an honest small 4-group over-estimate → a band-miss
  with interpretable values (M_pit/M_c would come out ~0.98 vs the real 0.78). Document the full-stack artifact.
- OR settle the group structure first — add a thermal group / more groups (`03 §2` amendment, ADR), then G1a on the
  full stack becomes meaningful. This also revisits the fast4 anisotropic-elastic follow-up.
- Either way: do NOT tune xs to hide it (ADR-022 discipline); a gate built on the artifact-laden full stack would be
  faked credibility.

The G1a machinery still to build (once the assembly is settled): the full-stack builder (pattern is in the extended
`gate_probe`) + the **critical-mass bisection** (bisect pit radius at fixed density, shells maintaining thickness,
to k=1.000±0.002 → M_pit/M_c) + the 2 G1a criteria + the report. G1b adds the compression scan (mechanism exists in
`kscan`/M2-T3) + the mass-fraction. Both ride the (validated) multi-layer transport.

---

## 4. fast4-b — the b-set + two real pipeline bug fixes (context for §3)

The a-set collapse pipeline (`build_fast4.py`) was written for actinides; the light/absorber b-set exposed two
physics bugs, both verified against the ENDF evaluations and fixed:
1. **Absorption:** `sigma_c` counted only (n,γ). B-10's absorber role is entirely **(n,α)** (0.22 b @1 MeV vs 6e-5 b
   (n,γ)). Now sums the (n,γ)+(n,p)/(n,α)/... disappearance channels.
2. **Elastic downscatter:** elastic was ALL within-group → H-1 (the moderator) had an identity transfer. Now a
   finite-A isotropic-CM downscatter for **A<30** (light), within-group for heavy (forward-peaked).

**Gate-safe by construction and confirmed:** Jezebel (G0b) byte-identical, Godiva (G0a) <<1 pcm, G0c (a differential)
untouched. Ga's absorption was ALSO undercounted and is now corrected (improves the canonical pit). **Documented
follow-up:** the exact **anisotropic** elastic-downscatter kernel (from the ENDF elastic p(μ)) — it matters for
the moderating regions (the §3 finding shows why) and would replace the A<30 isotropic approximation.

---

## 5. Traps + hard-won context (read every one)

- **The 4-group has NO thermal group** (bounds `[20, 3, 1, 0.1, 1e-3] MeV`). It is fine for bare fast metal
  (Godiva/Jezebel) but inadequate for a MODERATING region (the §3 finding). This is the ADR-022 family, scaled up.
- **`Σ ∝ ρ`:** for moving geometry use `ref_eigen_fn_masscons`, never `ref_eigen_fn`. (`kscan` reuses the proven
  adapter precisely so compression can't read backwards.)
- **NEVER tune xs to a gate** (ADR-022). fast4-b's fixes are physics corrections from ENDF, not benchmark fits.
- **The multi-layer transport is now validated** (§3) but was UNexercised before this session — trust it for
  layered assemblies, but it is young code.
- **MSBuild stale-binary trap** (memory + it cost real time): `cmake --build` can exit 0 yet not recompile an
  edited `.cu` / relink exes. Verify exe mtime > lib mtime (or delete the obj/exe) before trusting a GPU/dev-only
  result. Caught this session by a 2-criteria report where 4 were expected.
- **CI = CPU-only** (`NUKESIM_WITH_CUDA` OFF): 177. Dev (CUDA) = 193 (+16 `gpu.`). The gpu path + G0c/diff real runs
  are dev-only; the pure `diff_criteria` functions ARE CI-verified.
- **Shared tree with the -e (viz) session:** `viz/` + `.zcode/` carry the -e session's UNCOMMITTED WIP. Stage
  EXPLICIT paths, NEVER `git add -A`; verify `git diff --cached --name-only` has no `viz/` before every commit.
- **Space out `main` pushes; watch each to green** (GitHub concurrency cancel-cascade otherwise). Every push this
  session was watched to green before the next.

---

## 6. The one-paragraph orientation

You are continuing a spec-first, benchmark-provable GPU Monte-Carlo simulator of the Trinity device whose soul is
an interactive real-physics 3D visualization of the demon-core chain reaction. This session closed the `06 §1`
gate-CLI frontier (run/gate/diff all exist), turned G0c into a committed FULL cross-backend PASS, completed the
cross-section set to all 16 isotopes (fixing two real actinide-only pipeline bugs in the process), and — the
headline for you — de-risked G1a and found that the canonical device reads supercritical uncompressed in the
4-group model, resolved to a genuine 4-group thermal-group limit (the multi-layer transport itself is sound). The
engine stays honest: G0a/G0b fail openly, G0c passes fully, and the G1a finding is surfaced not fudged. `main` is
green at 193/177. Read `PROGRESS.md` and the `g1a-canonical-supercritical-finding` memory, hold the invariants
(nothing faked; `Σ∝ρ`; never tune xs to a gate), then settle the G1a group-structure question before building the
gate. Welcome; go well.
