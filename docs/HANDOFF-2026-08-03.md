# NUCLEAR-SIM — Session Handoff (2026-08-03)

*Written by session-2026-08-02-d at the end of a long multi-task run, as a
consolidated continuation prompt. The authoritative living state is still
`spec/PROGRESS.md` + `spec/SESSIONS.md`; this file is a convenience snapshot.*

## TL;DR
`main` @ **`ef5bd19`**, fully green: **104/104 dev** (91 CPU + 13 `gpu.`) / **91 on
CI (CPU-only)**, working tree clean, `spec/wip/` clean, CI green on windows-2022 +
ubuntu. **The `fast4`-independent frontier is now exhausted — the single dominant
unblock is owner-provided cited `fast4` cross-section data (M1-T4a-2).**

---

## Continuation prompt (paste into a fresh session)

```
You are continuing the NUCLEAR-SIM implementation at C:\NUCLEAR — a spec-first,
multi-session GPU Monte-Carlo simulator of the 1945 Trinity device, built only
from public/declassified sources. The spec in spec/ is the single authority;
code that contradicts it is a bug. No session finishes the project — you finish
ONE task with a clean, green handoff.

READ FIRST (spec/README.md §3 order):
  1. spec/README.md (fully — router + session protocol §5 + amendment §6)
  2. spec/PROGRESS.md — living state: task table, ready-queue, the single NEXT
     ACTION, the VERIFY command, Blockers. GROUND TRUTH.
  3. spec/SESSIONS.md — last ~6 entries (all session-2026-08-02-d): M4-T4-a/-b,
     M3-T1, M2-T1, M2-T2, M3-T2. "Notes for next session" are written for YOU.
  4. spec/DECISIONS.md — skim; do NOT re-litigate. Most relevant now: ADR-013
     (ν̄ is TOTAL; k_prompt derived once), ADR-019 (E4 shell-motion sign),
     ADR-009 (GPU-first), ADR-014 (who may push), ADR-004/D4 (few-group xs),
     ADR-018 (sandbox-mode UI, the parallel -e track).
  5. docs/ONBOARDING.md — accumulated practical context + traps.
  Check spec/wip/ for orphaned journals (should be only .gitkeep = clean handoff).

VERIFY before building on anything (trust gates, not prose):
  cmake --preset win-x64 && cmake --build --preset win-x64-rel && ctest --preset win-x64-rel
  Dev machine (CUDA auto-on): 104 = 91 CPU + 13 gpu. CI / CPU-only: 91.
  Anchored per-module probes are disjoint AND sum to the total:
    6+11+1+10+11+15+7+9+5+13+6+5+5 = 104   (ctest -R "^<module>\." for module ∈
    {toolchain,constants,oracle,rng,loaders,geometry,ref,eigen,benchmarks,gpu,
     kinetics,tier1,tier2}; ^gpu\. = 13, only when NUKESIM_WITH_CUDA is ON).

THE DECISION YOU FACE (Blockers in PROGRESS.md): the whole fast4-INDEPENDENT
frontier is built — GPU backend (M4), kinetics (M3-T1), materials (M2-T1),
hydro Tier-1 + Tier-2 (M2-T2 / M3-T2). Everything transport/gate-facing now waits
on M1-T4a-2 — the cited 4-group `fast4` cross-section dataset (~450 physical
values, owner-gated). It MUST NOT be invented (fabricated xs is the one failure
the provenance discipline exists to prevent). BLK-14 forbids ICSBEP access.
FIRST establish: has the owner provided cited xs data since 2026-08-03?
  • IF YES → highest value. Author data/xs/fast4.json + its provenance card
    (M1-T4a-2), flip it out of `blocked`, then M1-T5 (nukebench + gen_gates →
    gates.toml). M1-T5 also owes: measure σ_H at C-900 before G0a (05 §2), and
    gen_gates must read [[band]] entries WITHOUT synthesising a nominal
    (ADR-015). fast4 → M1-T5 → the formal gates (G0a/G0b/G0c), M2-T3 (k-vs-
    compression scan), the M3-T3 canonical burst bundle, and the full G4.
  • IF NO / unclear → surface the fast4 ask to the owner EARLY (it unblocks far
    more than anything below). The only tasks runnable without it:
    - M3-T3 (coupling): wire physics/kinetics (M3-T1) + physics/hydro (M3-T2
      make_hydro tier 1|2) + the eigen refresh (02 §3) + TallySink. The loop
      logic + tally_invariants (03 §5, all 9) are toy-testable NOW, but the DoD's
      *canonical* end-to-end artifact bundle needs fast4 — so start it as a CORE
      (the SYNC-M1 pattern) and complete when the xs data lands.
    - M4-T4-c (perf, LOW priority): cooperative-groups megakernel eigen — one
      launch/generation with an in-kernel superstep loop, to land M4-T4-b's
      divergence fix WITHOUT the per-superstep host overhead that made M4-T4-b
      regress. The validated event-based prototype is preserved at commit b04b7d7
      (its reservoir-in-SoA + per-flight physics are reusable). Adopt only if
      measured faster; the eigen is not a bottleneck until fast4 anyway.

PROTOCOL (README §5, normative): claim before working — branch task/Mx-Ty-name,
edit PROGRESS (in_progress + claimed_by=session-YYYY-MM-DD-<letter> + claimed_at),
ONE pushed `claim:` commit. Create spec/wip/<session-id>-<task-id>.md and append
findings BEFORE acting. Constants from data/constants.toml (no magic numbers).
Amendments: typo→CHANGELOG; behavior/API/schema/gate/constant→ADR + spec edit +
CHANGELOG + grep the whole spec for the fact (MAJ-40). END (always): green
build+tests, update PROGRESS (status + fresh falsifiable VERIFY + exactly one
imperative NEXT ACTION), append SESSIONS, fold-and-delete WIP, commit referencing
the task ID, merge to main ONLY green. Pushing origin/main is ordinary work
(ADR-014). git commit -F - with a heredoc (backticks in -m execute). Today's real
date governs the session id (§5.4). If entering a NEW milestone, run the SYNC
audit first (07 §SYNC) — SYNC-M2/M3 already done; M5/M6/M7 would need their own.
SPACE OUT pushes to main — three rapid pushes trigger a GitHub concurrency
cancel-cascade on CI (benign but confusing).
```

---

## What session-2026-08-02-d accomplished (six tasks + fixes)

| Task | Outcome |
|---|---|
| **M4-T4-a** | GPU profile-first perf baseline. Nsight FP64 = 0.00% (MAJ-33 met); `k_step` measured divergence-free → the deferred **branchless-split is NOT warranted (dropped)**; VRAM ≤92 MiB @1e6. `tools/perf/gpu_perf` (dev-only exe) + committed `artifacts/perf_history.jsonl`. |
| **M4-T4-b** | **Measured negative result** — built the event-based eigen swap (bit-identical to the old, thread-eff 29%→72%) but measured a **2–6× wall-clock regression** (per-superstep launch/sync overhead × ~30 generations). **Reverted**; prototype preserved at **`b04b7d7`**; spawned **M4-T4-c**. Lesson: thread-efficiency ≠ throughput; measure wall-clock. |
| **M3-T1** | α-mode kinetics core (E3a): `BurstAccumulator` (log-domain, overflow-proof, renorm-invariant), `nu_eff` = Σprod/Σfiss, `isotope_fission_shares`, `rossi_alpha` (E3c), `refresh_q` (R-13). **SYNC-M3.** Added `by_isotope_fissions` to `ref::FissionSource` so ν̄_eff is exact. Consumes `EigenResult::k_prompt()` (ADR-013), never re-derives. |
| **M2-T1** | 6 canonical Trinity materials (`data/materials/`: pu_ga_delta, u_natural, aluminum, he_compb, b10_acrylic[SIM], be_po_urchin) + `data/scenarios/trinity_canonical.toml` + `data/materials/README.md`. Cited atom fractions (Σ=1 exact). **SYNC-M2.** `oracle.py §2` mass check (xs-free): pit/tamper/pusher/HE-booster all ≤3% vs appendix. |
| **M2-T2** | Tier-1 parametric compression (`physics/hydro/tier1`): smootherstep + `Tier1Compression` + `compress(LayerStack)`. Endpoints + **mass conservation to 1e-12** (BLK-01-strength test). |
| **M3-T2** | Tier-2 hydro (`physics/hydro/tier2`): `GuderleyTiming` (derived `t_c`, MAJ-09) + `SnowplowShell` energy-conserving E4 (RK4). **Found + fixed an E4 sign bug → ADR-019** (`M·R̈ = 4πR²(P_int − P_drive)`; the old `(P_drive − P_int)` didn't conserve `E_int + ½MṘ²`), verified by the conservation test. |

Plus **`viz/`** — the GadgetLab Three.js source brought into the repo (owner-directed) + **`docs/gallery/`** gallery page. Tests **88 → 104**.

## Load-bearing context

- **The "-core" pattern (SYNC-M1 rule):** M3-T1, M2-T2, M3-T2 shipped the *testable math* and deferred the full `05` interfaces (`AlphaKinetics::run`, `HydroModel::step`/`make_hydro`) to **M3-T3**, whose deps (`EnergyField`, `CheckpointBlob`, `TallySink`, the burst loop) don't exist yet. M3-T3 composes them.
- **ADR-019** corrected `01 §5` E4's force sign (one normative location; `05 §4` was already correct; `spec/reviews/` archives left as historical, per MAJ-40).
- **`b10_acrylic` is SIM** — its B-10 loading (5 wt%) is NOT cited; a real cited loading is a `03 §6` amendment. No appendix mass (not gated). All provenance choices are in `data/materials/README.md`.
- **`trinity_canonical.toml` does NOT load** — it references the blocked `xs_set="fast4"`. It is a schema-valid *definition*; solid-pit / omitted-cavity / omitted-lens modelling choices are in its header. M2-T1's mass checks are xs-free (via `oracle.py`).
- **Parallel session (-e)** owns the UI/rendering track (ADR-018 M7-T5 sandbox mode; the `viz/` sketch). Rebase onto fresh `main` before editing living-state files; expect the occasional concurrency CI-cancel.
- **Environment:** Windows 11, RTX 4070 Ti SUPER (sm_89), CUDA 13.1, MSVC 14.44, CMake 4.3.3, vcpkg C:\vcpkg. Nsight Compute at `C:\Program Files\NVIDIA Corporation\Nsight Compute 2025.4.0\...\ncu.exe`. Repo: <https://github.com/bochen2029-pixel/nuclear-sim>.

## Open items
1. **Optional:** the 4 GadgetLab still-images aren't in the repo (pasted images can't be extracted to files; the runnable app source `viz/` *is* committed). Drop `01-armed-idle.png … 04-detonation.png` into `docs/gallery/` to make the gallery embeds render.
2. **The unblock:** cited `fast4` xs data (M1-T4a-2) — a citable multigroup library **or** an ENDF+collapse (NJOY-class) pipeline. Owner-only; opens the entire remaining chain (gates, M2-T3, M3-T3, G4).
