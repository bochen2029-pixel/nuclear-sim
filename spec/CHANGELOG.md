# CHANGELOG — Spec Amendments

> Format: `YYYY-MM-DD — <file(s)> — <what changed> — <ADR ref if behavioral> — <author session>`.

2026-08-02 — all — spec v0.1 authored (README, 00–13, PROGRESS, SESSIONS, DECISIONS, appendix/constants) — session-2026-08-02-a.
2026-08-02 — 02, 05, 07, 08, 12, PROGRESS, DECISIONS — toolchain pins to confirmed machine (CUDA 13.1, OptiX 9.1.0, RTX 4070 Ti SUPER sm_89 16 GB, vcpkg C:\vcpkg); GPU-first policy; M4 retimed in parallel with M2/M3; G4 targets re-based — ADR-009 — session-2026-08-02-a.
2026-08-02 — **spec v0.2 omnibus (ADR-010/011/012, three-review triage)** — files: 00-overview (scope interactive-vs-automated, E1–E7), 01-physics (E1c implicit capture; E2b fixed 8³ entropy mesh; E3a source term + ν̄_eff; E3b Λ density-scaled; E3c α; E4 energy-conserving state eqn; E5 defined; Tier-1 formula fix; Tier-2 derived t_c + γ=5/3; asymmetry c_a C-071-rule; E6 ε·F_peak quench + yield_split; E7 defined symbols; crosscheck-constant policy; determinism/precision policy), 02-architecture (repo tree complete + single-tree + spec/wip; main loop integer-scheduled + phase-gated quench + snapshot threading; warnings first-party; versions-only-in-12 rule; integer-scheduling rule; determinism addendum), 03-data-contracts (constants lo/hi/PENDING/registry/derived/use; xs schema v2 semantics: sigma_c rename, mu_bar required, transfer required prob-matrix, sigma_t computed, 1e-3 lower bound; materials composition_check + corrected pu_ga_delta + pu_ga_jezebel rule; scenario: valid TOML layers array, [data], [time], [transport], [source], [overrides], [ui.*] annotations, initiator range fix, generation_time_s_initial, eigen_refresh_dr_frac, quench_epsilon, dump budgets; tally: invariants ×8, k_eff peak/at_initiator/at_quench, σ fields, yield_split, fissions_by_isotope, non_canonical, shell_edges_cm + leaked_fissions, illustrative-values header; run.json data_hashes + unit_id; sweep axis_class + band-center scoring; checkpoint v2 identity+CRC+native-precision+eigen section; fields.f16; NEW §10 gates.toml + §11 gate_report.json schemas; §12 versioning policy), 04-module-core (RNG normative layout + fork + (ctr,sub) + KATs + uniform_d; xs GroupData v2; geometry nudge-along-direction + set_radii/scale_radii + kOutside + material index; scenario canonical_hash normative + override grammar; verify tools), 05-module-transport (SourceSpec; ref implicit-capture algorithm; EigenResult Λ; kinetics per 01 §4; hydro per 01 §5; GPU deterministic slots/streams/reductions; batch-as-GPU-parameter; TD comb), 06-frontends (gate no-seed + all-seeds + spec_sha256; diff hash checks; farm unit_id idempotency + lease rule; sampler in-tree C++ registry + ScoreKind; exit codes; --vram-report; studio badges + async refresh + G5), 07-milestones (all DoDs post-triage; M1-T4a/b split; per-task VERIFY probes; circularity rule; gate table → labels-only pointer; SYNC audits), 08-validation (PUBLIC-DERIVED benchmarks + Jezebel isotopics + isotope set; G0a/b gate config 1e6/50/200 + dual σ + CONDITIONAL fallback; G0c equivalence criteria; G1a mass-fraction + derived band + G1a-tight; G1b 4-criterion; G2 7-criterion incl. [16.6,26.8]kt envelope; G3 validation-not-calibration; G4 per-device full list; G5; statistical integrity rules), 09-rendering (temp calibration [7500,9500]K; ideal-gas formula; physical-honesty rule — no unfounded 3D asymmetry structure; G4 device fix), 10-ui (annotation-driven table, real paths, badges, non-canonical + beyond-envelope), 11-testing (per-backend goldens + structural compare; mandatory negative/contract/gate tests; quick/full split; perf rotation), 12-deployment (OPTIX_SDK_ROOT unified; vcpkg builtin-baseline; CMAKE_POLICY_VERSION_MINIMUM; arch 89-real/80,90-virtual; NUKESIM_WITH_CUDA auto semantics; docker tag/digest verify; hosting row public+MIT; cost guidance §6), 13-risks (residual column; R-1/R-4 re-rated H/H + transport-correction mitigation; R-3 raw=H; R-9 I=M; NEW R-12 ICSBEP, R-13 quasi-static q, R-17 gate-tuning, R-19 claim races, R-20 session crash journals, R-21 artifact governance; R-14/15/16/18 CLOSED by amendments), appendix/constants (value+lo/hi columns; C-021b/C-022 new; C-041 derived-exact; C-042/043 crosscheck-labeled; C-050/050b density-split; C-052 mass-fraction fix; C-070 γ=5/3 relabel; C-102 density-derived note; NEW §3 molar masses C-910–918; §4 SIM splits C-900/b/c + C-908/909/909b; NEW §5 gate bands C-930–C-946 with derivations), spec/README (read-order incl. DECISIONS; falsifiable VERIFY; ready-queue + claim tiebreak; session-id allocation; WIP journals; impact-analysis rule; single-spec-tree rule; bootstrap full read-order; status v0.2), PROGRESS (depends_on column; ready-queue; falsifiable VERIFY; G1a-tight + G5 rows; SYNC checklist; owner blockers M0-T6-b + M1-T4b), SESSIONS (bootstrap pointer; triage entry), DECISIONS (ADR-010 D6 retro; ADR-011 public+MIT resolved by owner; ADR-012 omnibus dispositions), root README (repo-root + MIT + single-tree notes) — session-2026-08-02-a.

2026-08-03 — **spec v0.3 — QC pass on the v0.2 triage (ADR-013 + QC-01…QC-16)** — files: 01-physics (§1 notation: ν̄ = TOTAL, β_i/β_eff, k_eff vs k_prompt rows; §4 k_prompt derived-once block replacing the contradictory prompt/total paragraph — ADR-013/QC-01), 02-architecture (§2 .gitignore made normative with `artifacts/gate_reports/**` + `perf_history.jsonl` un-ignored — QC-07; §3 main loop uses k_eff→k_prompt and prompt-supercritical flag — ADR-013), 03-data-contracts (§2 `beta` REQUIRED per isotope + `nu` = TOTAL semantics — ADR-013; §5 canonical tally example replaced with values satisfying ALL invariants (I3 was 5.5e-4 and I4 1.25e-3 against a 1e-6 tolerance — QC-04), `k_prompt` field added, invariant 4 rewritten to sum per-isotope molar masses (the M_Pu239-for-all-Pu form was unsatisfiable at 1e-6 with any Pu-240 fissions — QC-05), NEW invariant 9 yield_split sum — QC-10; §11 gate reports located at `artifacts/gate_reports/<gate>/` and committed — QC-07), 05-module-transport (EigenResult.beta_eff — ADR-013; M1-T2 DoD pure-capturer Σ_f=Σ_s=0 and Σ_a→Σ_c — QC-09), 07-milestones (M0-T1 remote + NOTICE + verbatim .gitignore + check-ignore assertion + bootstrap exemption; M0-T2 port Astrophage's proven CUDA-13.1/sm_89 CMake rather than re-deriving CMake-4/vcpkg friction; M0-T3 emits docs/VERIFICATION.md + molar-mass completeness hard error; M1-T2 DoD Σ_c; M3-T3 all 9 invariants), 08-validation (G1b coupling + criterion-1-implied-by-G1a notes — QC-11/QC-12; G2 eigen configuration made normative (interactive C-900b in-burst, gate C-900 at t=0) — QC-06; G2 criterion 4 restated via Φ_kt/C-910/C-916 because the kt/kg form used a `use=crosscheck` constant inside a gate and `11 §4`'s own static check greps for it — QC-02; G4 Godiva budget annotated as first-light not target — QC-14; §3 gate reports committed or the claim is void — QC-07), 11-testing (all 9 invariants + the §5 example is itself a fixture — QC-04; perf_history committed, rotations ignored — QC-07; NEW §5 independent verification oracle, generated not hand-written, porting Astrophage's canon.py/VERIFICATION.md pattern — QC-16), 13-risks (closed risks R-14/15/16/18 moved to their own section — QC-13; NEW R-22 ν̄-convention silent violation, R-23 gate-evidence loss), appendix/constants (C-020/021/021b relabelled TOTAL + ν̄ convention note; NEW C-022b–C-022e β for U-235/U-238/Pu-240/Pu-241; C-053 marked derived-not-independent — QC-12; NEW C-915a/b Ga-69/71 + C-919–C-929 covering all 16 species of `08 §1` + molar-mass completeness rule — QC-03), spec/README (§5.3 bootstrap exemption: the claim protocol needs a remote and M0-T1 is the task that creates it — QC-15; §10 v0.3), spec/wip/ created, DECISIONS (ADR-013), PROGRESS (owner pre-step, VERIFY note), SESSIONS (QC entry) — session-2026-08-03-a.

2026-08-03 — root (.gitignore, .gitattributes, LICENSE, NOTICE.md, README.md), spec/PROGRESS, spec/SESSIONS — M0-T1 executed: repository initialised in place at `C:\NUCLEAR` and published at github.com/bochen2029-pixel/nuclear-sim (public, MIT — ADR-011). `.gitignore` carries the `02 §2` normative block verbatim plus a labelled non-normative hygiene block (clarification class, no ADR); `.gitattributes` added for LF normalisation consistent with `canonical_hash()` (`04 §6`). PROGRESS: M0-T1 done, ready-queue → M0-T2, falsifiable VERIFY probe installed, M0-T6-b owner blocker resolved — session-2026-08-03-a.

2026-08-02 — 02-architecture (§2 tree gains `scripts/`; §4 dependency bullet names the correct vcpkg field), 12-deployment (§1 CMake row records the generator choice and why; vcpkg row corrects the baseline field name, records the pinned SHA `d59284957…`, the `x64-windows-static` triplet and the M7 `render` manifest feature), PROGRESS, SESSIONS — M0-T2 executed. Clarification class, no ADR: `07 §M0-T2` already listed `scripts/` in its Files column while `02 §2`'s "complete set of directories" omitted it (§2 permits a directory amendment without an ADR), and `builtin-baseline` is a `vcpkg.json` key, not a `vcpkg-configuration.json` one — vcpkg rejects a manifest that sets both, so the pin lives in `default-registry.baseline` in the single file `12 §1` names — session-2026-08-02-b.

2026-08-02 — 02-architecture (§2 tree gains `docs/`; §4 CUDA host-flag sentence corrected against the emitted nvcc command line + the C4211 hole recorded), 11-testing (§1 NEW normative `TEST_PREFIX` convention + module→prefix→probe table), 07-milestones (M0-T6 Files/DoD gain `.env.example`), 12-deployment (§1 triplet restated as a pin with its silent-failure mode; CMake row cross-references it), CHANGELOG, SESSIONS — M0-T2 post-merge review fixes, clarification class, no ADR. `02 §4` had said "CUDA host flags routed with `-Xcompiler`", which is false on MSVC: the VS generator hoists `/W4` onto the CUDA host pass and delivers `/WX` via MSBuild's `TreatWarningAsError`, so first-party `.cu` host code already compiles at `/W4 /WX` and routing would raise D9025 — R-8 (spec contradicted by code) closed in M0. `TEST_PREFIX` was implemented in M0-T2 but existed only as a CMake comment, leaving M0-T3/T4/T5's committed `ctest -R …` probes with no normative basis and a false-red failure two tasks out — session-2026-08-02-b.

2026-08-02 — 03-data-contracts (§1 documents the three array types + the `static_assert` band check + the PENDING deleted-function rule), 07-milestones (M0-T3 split into M0-T3-a/-b with per-half DoDs and VERIFY probes), DECISIONS (ADR-015), PROGRESS, SESSIONS, CHANGELOG — M0-T3-a executed. **ADR-015:** `03 §1`'s scalar-plus-band schema could not represent appendix §4 (tuples) or §5 (pure gate bands with no nominal); forcing a band into `value`+`lo`+`hi` invents a midpoint that is not a physical quantity and that a consumer could compare against. Added `[[band]]`, documented `[[registry]]`, and made `appendix_text` mandatory on every entry as a drift detector. **M0-T3 split (§8, oversized):** -a is the constants pipeline (96 strict entries, generator, `data/constants.toml`, headers, roundtrip, negative tests, ctest); -b is the generated `docs/VERIFICATION.md` oracle per `11 §5`. `M0-T5`'s `depends_on` retargeted to M0-T3-a — loaders need the constants, not the oracle — session-2026-08-02-b.

2026-08-02 — appendix/constants (§1 convention note: the C-042/C-043 basis difference restated PER NUCLIDE, +5.3% for Pu-239 vs +0.5% for U-235, replacing a blanket "~5%" that overstated C-043's gap by an order of magnitude — measured by the new oracle, `docs/VERIFICATION.md` §1), 03-data-contracts (§1 documents the geometry `appendix_mass_text`/`mass_kg`/`mass_lo_kg`/`mass_hi_kg` fields), PROGRESS, SESSIONS, CHANGELOG — M0-T3-b executed: `tools/verify/oracle.py` generates `docs/VERIFICATION.md` from `constants.data.toml` and the spec markdown, never linking nscore (`11 §5`). Clarification class, no ADR — the constants are unchanged; only the wording describing them was wrong. The appendix amendment is the oracle's first catch, which is the intended loop: oracle finds it, spec is amended, `oracle.verification_current --strict` keeps it green — session-2026-08-02-b.

2026-08-02 — 03-data-contracts (§4 example: `layers` moved ABOVE `[data]` so it is genuinely top-level, plus a validation-rules note; TOML reparents any bare key written after a table header, so the array as previously formatted parsed to `data.layers`), 04-module-core (§3 `IsotopeXS` gains `beta`, `cite`, `status` — the struct predated ADR-013, which makes `beta` REQUIRED per isotope), PROGRESS, SESSIONS, CHANGELOG — M0-T5 executed. Clarification class, no ADR: neither changes a contract's intent, both make the written form match it. Found by implementation contact (README §10) — writing the loader against the example is what exposed the reparenting — session-2026-08-02-b.

2026-08-02 — 02-architecture (§2 tree gains `.env.example` and `.github/workflows/`), 12-deployment (§1 MSVC row records the `windows-2022` CI runner pin and why it is not `windows-latest`), PROGRESS, SESSIONS, CHANGELOG — M0-T6 executed; **M0 complete**. Clarification class, no ADR — a directory/file addition per `02 §2`'s own rule, and a CI detail recorded where `12 §1` already owns version pins — session-2026-08-02-b.

2026-08-02 — 04-module-core (§3 `mix()` relocated to `core/material` with the reason; §5 `Material` gains `status`/`cite`/`mean_molar_mass` and `fracs` becomes a named `Constituent` struct) — **SYNC-M1 audit** before claiming M1's first task (`07 §SYNC`). Clarification class, no ADR: the implemented shape is strictly richer and the spec's shape could not carry a species that has a molar mass but no cross sections, which appendix §3 requires — session-2026-08-02-b.

2026-08-02 — PROGRESS, SESSIONS, CHANGELOG — M1-T1 executed (geometry + analytic tracker + `canonical_hash()`). No spec change: `04 §4`/`§6` were implementable as written, which after M0-T2/T3/T5 each needing an amendment is worth recording. SHA-256 added in-tree rather than as a vcpkg dependency — `02 §4` makes a new dependency an ADR, and an in-tree implementation checked against the published NIST vectors avoids one — session-2026-08-02-b.

2026-08-02 — 05-module-transport (§1 M1-T2 DoD: leakage criterion 1σ → **3σ** plus a 16-seed mean-pull bias check, with the reason), 07-milestones (M1-T2 DoD likewise; every `VERIFY: ctest -R …` probe anchored), 11-testing (§1 probes MUST be anchored `^<module>\.`; table extended with oracle/geometry/ref), PROGRESS, SESSIONS, CHANGELOG — M1-T2 executed. Clarification class, no ADR: neither changes what is being measured. **The 1σ leakage criterion was unachievable** — a within-1σ acceptance test has a 31.7% per-check false-failure rate (all three depths pass 31.8% of the time) and raising the history count cannot help, since the pull is N(0,1) regardless of N; 3σ matches the k_inf half of the same DoD and the added bias check resolves systematic error at 0.75σ, i.e. more tightly than the criterion it replaces. **Probe anchoring** fixes real collisions found by running them: `-R ref` matched `constants.runtime lookup resolves, and refuses what it must` — session-2026-08-02-b.

2026-08-02 — 05-module-transport (§2: NEW measured note — the E2b entropy-window noise is **correlation-dominated, not batch-dominated**, so C-901's `eig_h_tol = 1e-3` was unreachable and raising the batch is not the fix; M1-T5 MUST re-measure σ_H at C-900 before G0a is claimed), 11-testing (§1 probe table gains the eigen row), PROGRESS, SESSIONS, CHANGELOG — M1-T3 executed. Clarification class, no ADR: C-901 is unchanged, and what is added is a measurement plus an obligation to re-measure at the gate configuration before relying on it — session-2026-08-02-b.

2026-08-02 — 08-validation (§1: every `[M1-T4a]` placeholder for Godiva and Jezebel REPLACED with finalized open-literature values — atom densities primary, derived ρ/mass/wt%, `status = PUBLIC-DERIVED`, experimental-uncertainty caveat), DECISIONS (ADR-016 Godiva, ADR-017 Jezebel), 11-testing (§1 probe table gains the benchmarks row), PROGRESS (M1-T4a SPLIT into -1/-2 per §8), SESSIONS, CHANGELOG — M1-T4a-1 executed. Gate-affecting change per `08 §preamble` ("replacing a `[M1-T4a]` placeholder is a gate change"), hence one ADR per benchmark with cited evidence, per MAJ-46. **M1-T4a-2 (the cited `fast4` dataset) deliberately left open** — it is ~450 physical values that must not be invented — session-2026-08-02-b.

2026-08-02 — docs/ONBOARDING.md (NEW), spec/README (§9 bootstrap prompt now points at it), 02-architecture (§2 tree), CHANGELOG — accumulated practical session context written down: current state, the build/verify loop, module-by-module gotchas, the ten recurring traps found by running things, the non-negotiable boundaries, and a collected table of standing obligations owed by future tasks. Explicitly **supplementary** — README §9 remains the single place the prompt text lives (MIN-12) — session-2026-08-02-b.

2026-08-03 — 07-milestones, DECISIONS, PROGRESS — ADR-014: repository created under owner direction; M0-T6 loses its owner gate and becomes a single task; publication authority clarified (per-action owner instruction; pushing to this repo's main is ordinary work, new public repos / visibility changes / releases / other remotes each need fresh instruction; secret scan required before any first push to a public remote) — ADR-014 — session-2026-08-03-a.

2026-08-02 — spec/PROGRESS, spec/SESSIONS (living state; the code — src/gpu, src/core/hd.h, core/rng device annotations, CMake — is implementation, not spec) — **M4-T1 executed**: GPU backend foundation (SoA buffers + device Philox reusing core/rng + deterministic slots/streams/reductions); 81/81 on the dev machine (CUDA on), CI 75. SYNC-M4 recorded; M1-T4a-2 marked **blocked** (owner cited-data). **No normative spec change** — the RNG-header `NUKESIM_HD` annotations are the device port `04 §2`/`05 §6` already specify. Then **M4-T2 SPLIT (§8, clarification class, no ADR) into M4-T2-a** (device geometry + material/xs, float↔double parity) **and M4-T2-b** (event transport + fission bank + T-diff): event-based GPU transport plus the device data it reads is oversized for one task, and the static-data half is independently testable via parity — session-2026-08-02-c.

2026-08-02 — spec/PROGRESS, spec/SESSIONS (living state; code — src/gpu/{geometry,materials,device_data}, CMake, tests/unit/test_gpu — is implementation) — **M4-T2-a executed**: device geometry (float AnalyticSphereTracker mirror) + device material/xs (per-layer macro Σ_t/Σ_tr + per-isotope slots), float↔double parity-tested vs the CPU tracker (rays) and `mix()` (Σ_t/Σ_tr/νΣ_f); 83/83 on the dev machine (8 `gpu.`), CI 75. **No normative spec change** — device float re-implementations of `04 §4`/`04 §3` math, guarded by parity per `01 §9`/G0c — session-2026-08-02-c.

2026-08-02 — spec/PROGRESS, spec/SESSIONS (living state; code — src/gpu/{transport,device_problem}, tests/unit/test_gpu — is implementation) — **M4-T2-b executed**: event-based fixed-source GPU transport (SoA + superstep loop, E1a–E1e implicit capture) with prefix-sum compaction of live particles + a deterministic fission bank at exclusive-prefix-sum slots (`fork` streams). **First differential (G0c-style) validation vs `ref/`**: pure-capturer leakage + scattering/fission k_inf within 3σ; bit-identity (tallies + bank) across launch configs. 86/86 dev (11 `gpu.`), CI 75. **No normative spec change.** Deferred (recorded in the task table): per-event branchless-kernel split → M4-T4; fission-bank propagation → M4-T3 — session-2026-08-02-c.

2026-08-02 — spec/PROGRESS, spec/SESSIONS (living state; code — src/gpu/{gpu_backend,transport,eigen} observational VRAM/device-info instrumentation, tools/perf/gpu_perf, CMake, artifacts/perf_history.jsonl — is implementation) — **M4-T4-a executed** (SPLIT from M4-T4): GPU **profile-first** perf baseline. Perf harness times the GPU eigen/transport across a batch sweep → committed `artifacts/perf_history.jsonl` (11 §1). **Nsight Compute** measured **FP64 instruction fraction = 0.00%** on `k_step`+`k_generation` (MAJ-33/05 §6 ≤1% met); `k_step` at **100% thread efficiency (31.99/32)** so the M4-T2-b-deferred **branchless-kernel split is NOT warranted and is dropped**; the eigen `k_generation` (history-per-thread) at **29%** is the real divergence hotspot → M4-T4-b. VRAM ≤92 MiB @1e6 (≪12 GB). 88/88 dev (13 `gpu.`; `gpu_perf` is a non-test exe), CI 75. **No normative spec change** — the VRAM/`device_info` additions are observational diagnostics (cudaMemGetInfo), and cannot affect physics or determinism (re-verified: 88/88 bit-identity holds). **M4-T4 SPLIT (§8, clarification class, no ADR)** into M4-T4-a (done) and **M4-T4-b** (event-based transport inside the eigen + device-side per-gen reductions; NOT the branchless split). **Finding recorded (PROGRESS Blockers):** C-945 (`g4_perf_budgets`) stays PENDING — its `resolved_by="M4-T4"` is optimistic; the Godiva-gate-eigen / canonical-burst / render budgets need `fast4` (M1-T4a-2) / M2–M3 / M7 — session-2026-08-02-d.

2026-08-02 — 03-data-contracts (§4 `[lenses]` gains OPTIONAL `[[lenses.detonators]]` — `enable` + `delay_s ∈ [0, 1e-6]`, count-matched, absent = canonical all-fire — plus the validation rule and a `[ui."lenses.detonators"]` annotation whose range bounds `delay_s` in schema units), 09-rendering (§2 clip-plane discard rule — view state, never in `canonical_hash()`; §4 BURST neutron trails from a run-seed-selected deterministic sample + clip plane applies to all phases), 10-ui (widgets table gains the Detonators row; Geometry panel gains free clip plane + detonator grid; Timeline panel gains computed-state event markers; Rules gain detonator non-canonical + `enable=false` beyond-envelope badge), 06-frontends (§3 nukestudio panel enumeration names the M7-T5 additions — impact-analysis catch), 07-milestones (NEW **M7-T5** sandbox-mode task, depends M7-T3 + M6-T3), DECISIONS (**ADR-018**), PROGRESS (M7-T5 row), CHANGELOG — **sandbox-mode interaction extensions** (owner direction, 2026-08-02 brainstorm): per-detonator selection, free clip plane, neutron trails, time-warp markers — interaction/visualization layer only; no physics, no constants, no gate changes; safeguards (`axis_class`/`ScoreKind`, non-canonical discipline, `09 §4` physical-honesty) restated unchanged — ADR-018 — session-2026-08-02-e.

2026-08-02 — spec/PROGRESS, spec/SESSIONS, spec/CHANGELOG (living state; code — src/gpu/eigen.cu [prototype at b04b7d7, reverted at 69fce40], tools/perf/gpu_perf.cpp `--golden` — is implementation) — **M4-T4-b executed: MEASURED NEGATIVE RESULT.** Built an event-based, compacted eigen transport (`k_step_eigen` + reservoir-in-SoA), validated **bit-identical** to the history-per-thread eigen (k/checksum/entropy exact) and 88/88, thread-eff 29%→72% (Nsight) — but **measured a 2–6× wall-clock regression** (per-superstep launch/sync overhead × ~30 generations ≫ the divergence savings), so **reverted** to the faster history-per-thread as the shipped default (eigen.cu unchanged on main). Prototype preserved at `b04b7d7`. **No normative spec change.** Added task **M4-T4-c** (cooperative-groups megakernel — the real fix, perf-only/low-priority). NEXT ACTION moved to **M3-T1** (α-kinetics; unblocked, needs no `fast4`). Lesson recorded: thread efficiency ≠ throughput; measure wall-clock before AND after — session-2026-08-02-d.

2026-08-02 — spec/PROGRESS (SYNC-M3 audit + M3-T1 row + VERIFY 94/81 + NEXT ACTION M2-T1 + ready-queue), spec/SESSIONS, spec/CHANGELOG (living state; code — src/physics/kinetics/kinetics.{h,cpp}, src/ref/{ref_transport.h,ref_generation.cpp}, CMakeLists, tests/unit/test_kinetics.cpp — is implementation) — **SYNC-M3 + M3-T1 executed.** α-mode kinetics core (`physics/kinetics`): E3a `BurstAccumulator` (log10-domain population + cumulants, overflow-proof, renorm-invariant), `nu_eff`, `isotope_fission_shares`, `rossi_alpha` (E3c), `refresh_q` (R-13). **SYNC-M3 fix:** `ref::FissionSource` gains `by_isotope_fissions` (per-isotope Σ_f-weighted, additive) so ν̄_eff = Σproduction/Σfissions is exact + group-correct (01 §4). 94/94 dev (6 `kinetics.`) / 81 CI. **No normative spec change** — kinetics implemented as free functions + `BurstAccumulator` (the testable E3a core); `05 §3`'s `AlphaKinetics::run(HydroModel&,TallySink&)` deferred to M3-T3 when its deps exist (SYNC-M1 rule: don't declare an unimplemented method) — session-2026-08-02-d.

2026-08-02 — spec/PROGRESS (SYNC-M2 + M2-T1 row + NEXT ACTION M2-T2 + ready-queue), spec/SESSIONS, spec/CHANGELOG (living state; data + tooling — data/materials/{pu_ga_delta,u_natural,aluminum,he_compb,b10_acrylic,be_po_urchin}.json, data/materials/README.md, data/scenarios/trinity_canonical.toml, tools/verify/oracle.py §2, docs/VERIFICATION.md — are data/implementation) — **SYNC-M2 + M2-T1 executed.** The 6 canonical Trinity assembly materials, authored with **atom fractions from cited compositions** (appendix §2/§3 + research doc; provenance + choices recorded in data/materials/README.md per the DoD), plus trinity_canonical.toml (the α-mode burst, solid-pit neutronic stack). `oracle.py` §2 extended to cross-check per-layer mass = geometry×density vs the appendix (xs-free): pit −0.02% / tamper +0.42% / pusher in-band / HE booster −0.91%, all ≤3%, 0 findings. `constants_roundtrip` covers all species' molar masses. 94/94. **No normative spec change** — cited data authored per the M2-T1 DoD (README is the specified record mechanism); b10_acrylic B-10 loading is tagged **SIM** (not cited numerically — a future cited value is a `03 §6` amendment). trinity_canonical.toml references `xs_set="fast4"` (blocked) so it is a schema-valid definition that does not yet load — the Σ-dependent M2/M3 work waits on M1-T4a-2 — session-2026-08-02-d.

2026-08-02 — spec/PROGRESS (M2-T2 row + VERIFY 99/86 + NEXT ACTION M3-T2 + ready-queue), spec/SESSIONS, spec/CHANGELOG (living state; code — src/physics/hydro/tier1.{h,cpp}, CMakeLists, tests/unit/test_tier1.cpp — is implementation) — **M2-T2 executed.** Tier-1 parametric compression (`01 §5`): `smootherstep`, `Tier1Compression` (radius_scale/density_ratio/s_of), `compress(LayerStack)`. DoD met: endpoints to 1e-12 + **mass conservation to 1e-12 at s∈{0,.25,.5,.75,1}** (the BLK-01-strength test). 5 `tier1.` tests → 99 dev / 86 CI. **No normative spec change** — the compression math implemented per `01 §5`; the full `HydroModel::step`/`make_hydro` (`05 §4`) deferred to M3-T2 (Tier-2) / M3-T3 (coupling) since EnergyField/CheckpointBlob + the burst loop don't exist yet (SYNC-M1 rule) — session-2026-08-02-d.

2026-08-03 — 01-physics (§5 E4 force sign `(P_drive − P_int)` → `(P_int − P_drive)` + conservation note), DECISIONS (ADR-019), spec/PROGRESS (M3-T2 row + VERIFY 104/91 + NEXT ACTION + ready-queue), spec/SESSIONS, spec/CHANGELOG (living state; code — src/physics/hydro/tier2.{h,cpp}, CMakeLists, tests/unit/test_tier2.cpp — is implementation) — **M3-T2 executed + ADR-019.** Tier-2 hydro: `GuderleyTiming` (derived t_c, MAJ-09) + `SnowplowShell` energy-conserving E4 ODE (RK4). **Implementation contact (README §10) found the E4 shell-motion sign inconsistent with its own conservation claim** — corrected to `M·R̈ = 4πR²(P_int − P_drive)` (physics-formula fix; the `dE_int/dt` state eq + γ + Guderley + Tier-1 unchanged), verified by the `tier2.` conservation test (<1e-6 over 1e4 steps). **MAJ-40 impact analysis:** the fact appeared in exactly one normative location (`01 §5`); `05 §4` restates only the unchanged state eq; `spec/reviews/` archives preserve the original (historical). 5 `tier2.` tests → 104 dev / 91 CI — session-2026-08-02-d.

2026-08-03 — spec/PROGRESS (task table + downstream depends_on + ready-queue), spec/CHANGELOG — **M3-T3 split into M3-T3-a + M3-T3-b** (task decomposition, no ADR — no behaviour/API/schema/gate/constant change). M3-T3's DoD has two separable deliverables straddling the `fast4` line: the 9 `tally_invariants` (03 §5) are testable NOW (the 03 §5 illustrative tally is a normative fixture, QC-04 / 11 §4), but the *canonical* end-to-end artifact bundle needs the blocked `fast4` xs (M1-T4a-2). Split so the doable half ships DONE and the blocked half is isolated (precedent: M0-T3→a/b, M1-T4a→-1/-2). -a = `src/physics/tally/` (TallyResult + invariants + JSON); -b = `src/physics/couple/` (the 02 §3 loop). Downstream M3-T4/M5-T1/M7-T1 `depends_on` retargeted M3-T3→M3-T3-b (the loop is their real dependency) — session-2026-08-03-b.

2026-08-03 — spec/PROGRESS (M3-T3-a row done + VERIFY 109/96 + NEXT ACTION M3-T3-b + ready-queue), spec/SESSIONS, spec/CHANGELOG (living state; code — src/physics/tally/{tally.h,tally.cpp}, tests/unit/test_tally.cpp, tests/unit/spec_examples.h (+tally_example), CMakeLists — is implementation) — **M3-T3-a executed.** The tally module: `TallyResult` (03 §5 schema v1) + all 9 `tally_invariants` + `to_json`/`parse_tally_json` (nlohmann ordered). DoD met: all 9 green on the 03 §5 normative fixture (QC-04 / 11 §4), ported from `oracle.py` §6 (independent agreement, I3 7.5e-8 / I4 3.8e-8). 5 `tally.` tests → 109 dev / 96 CI. **No normative spec change** — the invariants implemented per 03 §5; the `TallySink` interface + the burst that fills a `TallyResult` are M3-T3-b (02 §3 coupling), and the *canonical* end-to-end bundle needs `fast4` (M1-T4a-2) — session-2026-08-03-b.

2026-08-03 — spec/PROGRESS (M3-T3-b row done + VERIFY 114/101 + NEXT ACTION + ready-queue + summary), spec/SESSIONS, spec/CHANGELOG (living state; code — src/physics/couple/{couple.h,couple.cpp}, tests/unit/test_couple.cpp, CMakeLists — is implementation) — **M3-T3-b executed.** The 02 §3 α-mode coupling-loop machinery: `run_burst` (E3a kinetics ← injectable `EigenFn` k_prompt; integer-counter refresh cadence + R-13 auto-halve; tier-1 hydro E3b Λ-rescale; E6 quench + yield_split, BLK-03) + streaming `TallySink`/`BurstTally` → 03 §5 `TallyResult` (log-sum-exp per-isotope/shell). DoD met: 5 `couple.` tests → all 9 `tally_invariants` green on a scripted-burst tally; 114 dev / 101 CI. **No normative spec change** — implemented per 02 §3 / 01 §4-6 / 05 §3-5; full `HydroModel`/`make_hydro`/`CheckpointBlob` (M5) + tier-2 E4 disassembly/E5 + `at_peak_compression` (M3-T4) deferred (SYNC-M1); the canonical bundle (real `EigenFn` on `trinity_canonical`) is fast4-gated (M1-T4a-2). `TallySink` documented as the viz seam for the eventual 3D chain-reaction view — session-2026-08-03-b.

2026-08-03 — spec/PROGRESS (M3-T3-c row done + VERIFY 115/102 + NEXT ACTION step 3 + ready-queue + summary/strategy), spec/SESSIONS, spec/CHANGELOG (living state; code — src/physics/couple/{couple.h,couple.cpp}, tests/unit/test_couple.cpp — is implementation) — **M3-T3-c executed** (owner-delegated strategic pivot toward the `viz/js/simstub.js` fusion contract). `ref_eigen_fn` = real-transport `EigenFn` (rebuild `RefTransport` per geometry + `run_eigen`); `GenerationSample.sites` = per-generation stratified fission-site sample (the volumetric chain-reaction tap). `run_burst` now drives a GENUINE Monte-Carlo eigen and streams real spatial fission sites. DoD met: 6 `couple.` tests (real MC eigen on a Pu sphere → sites inside the sphere); 115 dev / 102 CI. **No normative spec change** — implemented per 05 §2/§5; the `GenerationSample.sites` streaming shape is a NEW data contract on the viz seam (to be documented + coordinated with the -e session before it hardens); the eventual `fields.f16 fission_rate` grid (03 §9) is the complementary voxel form. fast4-independent — session-2026-08-03-b.

2026-08-03 — spec/PROGRESS (M3-T3-d row done + M3-T3-e row + VERIFY 116/103 + NEXT ACTION + summary), spec/SESSIONS, spec/CHANGELOG (living state; code — src/core/material/material.{h,cpp}, src/physics/couple/{couple.h,couple.cpp}, tests/unit/test_couple.cpp — is implementation) — **M3-T3-d executed** (strategy step 3 part 1: the reactivity handle). `MaterialLib::with_density_scale(factor)` (Σ ∝ ρ; scales density + macro) + `ref_eigen_fn_masscons` (mass-conserving: ρ/ρ₀ = (r₀/r)³ from the uniform geometry scale). DoD met: 7 `couple.` tests — a real MC eigen shows k_compressed > k0 > k_expanded (compression ignites, disassembly quenches). 116 dev / 103 CI. **No normative spec change** — implements the E4/E5 hydro↔transport density coupling (01 §5); the `SnowplowShell` dynamics that drive it are M3-T3-e; `with_density_scale` is uniform (per-layer scaling for the full device is a later refinement). fast4-independent — session-2026-08-03-b.

2026-08-03 — spec/PROGRESS (M3-T3-e row done + VERIFY 117/104 + NEXT ACTION step 4 + summary), spec/SESSIONS, spec/CHANGELOG (living state; code — src/physics/couple/{couple.h,couple.cpp}, tests/unit/test_couple.cpp — is implementation) — **M3-T3-e executed** (strategy step 3 part 2: the emergent self-limiting burst). `run_burst` gains a `disassembly` mode: `SnowplowShell` E4 + E5 impulse deposition (E_n = F_n·e_f_joules), R → geometry that `ref_eigen_fn_masscons` dilutes; Λ density-ratio made mode-agnostic. DoD met: 8 `couple.` tests — a REAL MC-transport supercritical Pu-sphere burst ignites, disassembles, and quenches on its own (report.quenched, k_quench<k_peak, finite yield). 117 dev / 104 CI. **No normative spec change** — implements 01 §5 E4/E5 + 02 §3 E6; test-scale core mass for speed (e_f physical); the demon-core chain reaction is now a complete emergent burst. fast4-independent — session-2026-08-03-b.

2026-08-03 — 02-architecture (§2 tree gains `src/api/`), spec/PROGRESS (M3-T3-f row done + M3-T3-g row + VERIFY 122/109 + NEXT ACTION M3-T3-g + ready-queue/summary), spec/SESSIONS, spec/CHANGELOG (living state; code — src/api/run_provenance.{h,cpp}, tests/unit/{test_api.cpp, spec_examples.h}, CMakeLists — is implementation) — **M3-T3-f executed** (step 4 the fusion API, split into M3-T3-f/-g; part 1). New nscore module **`src/api/`** — the studio/binding surface `viz/js/simstub.js` calls; it composes physics (`run_burst`, `ref_eigen_fn_masscons`) so it is nscore, NOT a frontend (D7 keeps `src/app/` physics-free). The `03 §6` **run.json provenance contract**: `RunProvenance` + `to_json`/`parse_run_json` (required-field parse contract on the identity/reproducibility fields) + `compute_unit_id` = sha256(scenario canonical_hash ‖ overrides ‖ seed), overrides SORTED → order-independent dedup key (matching `canonical_hash`'s sorted override block; documented stable byte layout for M5-T2). DoD met: 5 `api.` tests (`03 §6` example parse + idempotent round-trip + field order + required-field rejection + `unit_id` determinism/input-sensitivity/order-independence), read from the spec markdown so they can't drift (M3-T3-a pattern; new `spec_examples::run_example()`); 122 dev / 109 CI. **Spec change: clarification class, no ADR** — `02 §2`'s "complete set of directories" gains `src/api/` per §2's own directory-amendment rule (a directory needs no ADR). Recorded for M3-T3-g: `03 §4` says overrides are "recorded verbatim in run.json" but the `03 §6` example omits `scenario_overrides` (reconcile there); simstub's `run` object is a subset of `03 §6`. fast4-independent — session-2026-08-03-c.

2026-08-03 — spec/PROGRESS (M3-T3-g row done + M3-T3-h row + VERIFY 127/114 + NEXT ACTION M3-T3-h + ready-queue/summary), spec/SESSIONS, spec/CHANGELOG (living state; code — src/api/studio.{h,cpp}, tests/unit/test_api.cpp, CMakeLists — is implementation) — **M3-T3-g executed** (step 4 the fusion API; part 2, split again into M3-T3-g the evaluate gauge + M3-T3-h generate_run). `src/api/studio.{h,cpp}`: `DemonCoreAssembly` (a bare Pu sphere from `pit.mass_kg` + the δ-Pu density DERIVED from cited C-101 OD + C-102 mass ≈15.23, SIM Pu-239/Pu-240 xs — the RealSphere file-based template) + `evaluate(cfg) → {k_eff, k_prompt, sigma_pcm, ready}` via `ref_eigen_fn_masscons` at the compressed geometry (ready = prompt-supercritical) + `StudioConfig::from_json` (the viz dotted keys) + the `evaluate_json` seam. DoD met: 5 new `api.` tests (10 total) — the assembly builds the C-101/C-102-consistent pit; the gauge is sane; and with common random numbers, compression↑ + mass↑ raise k while Pu-240↑ lowers it (the demon-core reactivity handles, emergent from real MC transport); 127 dev / 114 CI. **No normative spec change** — `src/api/` already existed (M3-T3-f); studio is implementation. fast4-independent (SIM xs). Recorded for M3-T3-h: the `03 §4`↔`§6` `scenario_overrides` reconciliation still pending; ASCII-only Catch2 test names (a `—` broke ctest↔Catch2 name matching) — session-2026-08-03-c.

2026-08-03 — 03-data-contracts (§6 run.json example + prose gain `scenario_overrides`), spec/PROGRESS (M3-T3-h row done + VERIFY 131/118 + NEXT ACTION + ready-queue/summary; STEP 4 COMPLETE), spec/SESSIONS, spec/CHANGELOG (living state; code — src/api/{studio.{h,cpp},run_provenance.{h,cpp}}, tests/unit/test_api.cpp — is implementation) — **M3-T3-h executed; STEP 4 (the demon-core fusion API) COMPLETE.** `generate_run(cfg)` in `src/api/studio.{h,cpp}`: the disassembly `run_burst` from the COMPRESSED pit (eigen r_ref = uncompressed r0, so compression → prompt-super and disassembly → quench) + `BurstTally` + a `GenerationSample`-collecting sink + `RunProvenance` → `{tally (03 §5), run (03 §6), samples}` JSON (embeds the tally/run objects + serializes the `sites` stream); detonate = supercritical && quenched && yield>0, else fizzle — EMERGENT from k, no hardcoded rule. DoD met: 4 new `api.` tests (14 total; 131 dev / 118 CI) — measured end-to-end: a 9 kg/comp-2.5 pit ignites (k_peak 1.007) → disassembles → quenches (k 0.394), finite yield 1.32 kt, all 9 tally invariants; a 4 kg/comp-1.0 pit fizzles. **Spec change: clarification class, no ADR** — `03 §6`'s run.json example + prose gain `scenario_overrides`, reconciling the omission against `03 §4`'s standing mandate that overrides are "recorded verbatim in run.json" (MAJ-40 grep: only §4/§6 are normative for run.json; both now consistent). SIM xs → SIM yields (the MECHANISM is real; cited numbers arrive with fast4). fast4-independent — session-2026-08-03-c.

2026-08-03 — spec/PROGRESS (M3-T4 `depends_on` corrected to `M3-T3-b, M5-T1`; NEXT ACTION + ready-queue elevate M5-T1) — **dependency correction found by implementation contact, no ADR.** After completing step 4, assessed M3-T4 (initiator timing) as the next autonomous pick and found it entangled: `at_peak_compression()` (the initiator t_fire driver) is a method on the `05 §4` `HydroModel` interface, whose `serialize()` returns a `CheckpointBlob` (M5); `couple.h` already deferred that whole hierarchy "because the M5 consumer doesn't exist yet," so building `make_hydro`/`HydroModel` for M3-T4 now would declare `serialize()` ahead of its M5 consumer (the SYNC-M1 anti-pattern) or deviate from the interface. The canonical t_fire sweep also needs `fast4`. So M3-T4 genuinely depends on M5-T1 (CheckpointBlob) + fast4, not just M3-T3-b — corrected in the task table; M5-T1 (checkpoint/resume, which provides `CheckpointBlob` and thus unblocks M3-T4) is now the recommended highest-value clean next task — session-2026-08-03-c.

2026-08-03 — spec/PROGRESS (SYNC-M5 recorded; M5-T1 split into M5-T1-a done + M5-T1-b; VERIFY 134/121 + NEXT ACTION M5-T1-b + ready-queue/summary), spec/SESSIONS (SYNC-M5 + M5-T1-a) — **SYNC-M5 + M5-T1-a executed** (code — src/core/hash/crc32.{h,cpp}, src/core/checkpoint/checkpoint.{h,cpp}, tests/unit/test_checkpoint.cpp, CMakeLists — is implementation). SYNC-M5: audited the checkpoint needs vs `03 §8`/`05 §4`/`11 §T-resume`; `CheckpointBlob` (05 §4, deferred by couple.h) is the type M5-T1 defines; split M5-T1 → -a (container) + -b (live-state sections + resume + T-resume gate). M5-T1-a: an in-tree CRC32 (IEEE 802.3, like the in-tree SHA-256 — no new dependency, 02 §4) + the `checkpoint.bin` v2 container (128-B header + section table + `write`/`read_checkpoint` with the R-7 load-rejection rules + `CheckpointBlob`/`SimClockState`). DoD met: 3 `checkpoint.` tests (CRC32 IEEE vector, container round-trip, every R-7 rejection class); 134 dev / 121 CI. **No normative spec change** — `src/core/checkpoint/` and `src/core/hash/` are already in the `02 §2` tree; implements `03 §8`/`05 §4` as written. fast4-independent. Partially unblocks M3-T4 (provides `CheckpointBlob`) — session-2026-08-03-c.

2026-08-03 — spec/PROGRESS (M5-T1-b split into -b done + -c; VERIFY 135/122 + NEXT ACTION M5-T1-c + ready-queue/summary), spec/SESSIONS — **M5-T1-b executed** (code — src/physics/kinetics/kinetics.{h,cpp}, tests/unit/test_kinetics.cpp — is implementation). Split M5-T1-b → -b (the `BurstAccumulator` resume primitive) + -c (the full couple-loop resume + T-resume gate). M5-T1-b: `BurstAccumulator::State` + `state()`/`from_state()` + `serialize_accumulator_state`/`deserialize_accumulator_state` — a bit-exact LE section codec (03 §8 §7) that lives physics-side (core stays physics-free; the generic checkpoint container holds the opaque bytes). DoD met: 1 new `kinetics.` test (7 total) — an accumulator stepped ~40 decades, checkpointed THROUGH the M5-T1-a container, restored, and continued bit-for-bit (`==`) identical to an uninterrupted run; 135 dev / 122 CI. **No normative spec change** — implements `03 §8 §7` as written; the ~24-decade log-domain state round-trips exactly (IEEE bit patterns). fast4-independent — session-2026-08-03-c.

2026-08-03 — spec/PROGRESS (M5-T1-c row done → M5-T1 COMPLETE; VERIFY 136/123 + NEXT ACTION M5-T2 + ready-queue/summary), spec/SESSIONS, docs/HANDOFF-2026-08-03-c.md (NEW) — **M5-T1-c executed; M5-T1 (checkpoint/resume v2 + T-resume, D9) COMPLETE** (code — src/physics/couple/{couple.h,couple.cpp}, tests/unit/test_couple.cpp — is implementation). The full `run_burst` bit-identical resume + the `11 §T-resume` gate: `BurstCheckpoint` + `BurstTally::State`/`state()`/`load_state()` + `serialize_burst_state` (bit-exact LE, physics-side); `run_burst` gains a `BurstResume*` (checkpoint at a refresh boundary → resume, re-deriving the eigen deterministically + reconstructing geometry from geom0+shell/t; eigen_calls captured pre-refresh). DoD met: 1 new `couple.` test (9 total) — a real SIM demon-core disassembly burst killed at a refresh boundary + serialized through the `checkpoint.bin` container resumes to a BIT-IDENTICAL tally (`to_json` equality); 136 dev / 123 CI. **No normative spec change** — implements `03 §8` + `11 §T-resume` as written (ref backend; the GPU same-backend T-resume is a later item). fast4-independent. Unblocks M5-T2 (artifact store, depends_on M5-T1). Plus `docs/HANDOFF-2026-08-03-c.md` as the next session's warm-start prompt — session-2026-08-03-c.

2026-08-03 — spec/PROGRESS (SYNC-M5b recorded; M5-T2 row done → M5 now 2/5; VERIFY 143/130 + NEXT ACTION M5-T3 + ready-queue/Current summary), spec/SESSIONS (SYNC-M5b + M5-T2) — **M5-T2 executed** (code — src/core/store/store.{h,cpp}, tests/unit/test_store.cpp, CMakeLists — is implementation). SYNC-M5b: a focused re-audit of the store vs `03 §7` (M5's milestone SYNC-M5 already ran for M5-T1) — the schema is implemented VERBATIM; the store keys on `unit_id` as an opaque STRING, so `src/core/store/` depends only on sqlite3 (via `nukesim_deps`), NOT on `src/api` (`compute_unit_id` stays in api) — the M5-T1 "generic core container; owning layer supplies the semantics" discipline; the nukefarm CLI + FS queue + samplers are deferred to M5-T3/T4 (they need the sweep engine — a shell now = the SYNC-M1 anti-pattern); the per-run `artifacts/<unit_id>/` bundle emission stays with the run-emitter (api/nukefarm). M5-T2: `ns::store::SweepStore` — the `03 §7` `sweep.db` (`runs(unit_id TEXT PK, params_json, tally_json, wall_s REAL, status)` + `cursor(state_json)`, SQLite) with `record_run` INSERT-OR-IGNORE `unit_id` idempotency (a rerun / second-finisher write is a NO-OP, D6/D8), `get_run`/`is_done`/`status_of`/`count`(`_with_status`), `set_cursor`/`get_cursor` (the singleton cursor); all params bound via prepared statements (injection-safe). DoD met: 7 `store.` tests (empty store, round-trip, injection-safe binding, idempotency no-op, is_done/status, cursor-singleton replace, on-disk reopen-persistence); 143 dev / 130 CI. **No normative spec change** — `src/core/store/` is already in the `02 §2` tree; implements `03 §7` as written. fast4-independent — session-2026-08-03-d.

2026-08-03 — spec/PROGRESS (SYNC-M5c recorded; M5-T3 split into M5-T3-a done + M5-T3-b; M5-T4 dep → M5-T3-b; VERIFY 155/142 + NEXT ACTION M5-T3-b + ready-queue/Current summary), spec/SESSIONS (SYNC-M5c + M5-T3-a) — **M5-T3-a executed** (code — src/app/nukefarm/{sweep.{h,cpp}, samplers/builtin.cpp}, tests/unit/test_nukefarm.cpp, CMakeLists — is implementation). SYNC-M5c: the FIRST `src/app/` frontend; the `Sampler` interface is `06 §2` verbatim (`Tally` = `ns::physics::TallyResult`, a frontend consuming an nscore output — D7-clean); the uncertainty-band check uses the existing `ns::consts::get_lo`/`get_hi(id)` runtime lookup; nukefarm builds as the `nukesim_nukefarm` static lib (the CLI is M5-T3-b). M5-T3-a: `SweepManifest::load`/`parse` — the `03 §7` sweep.toml loader + the **axis_class/ScoreKind/objective enforcement** (MAJ-35, the mechanical `00 §3.4` "explain-don't-search" boundary: an uncertainty axis ⊆ its constant band, pedagogical only {grid, budget≤100, sensitivity}, calibrate all-physical + a target band, unknown key/sampler/constant_id hard-error) + the `06 §2` `Sampler` interface + the grid/lhs/random space-filling samplers + `make_sampler` (OptimizeExtremum + `mcts` rejected). DoD met: 12 `nukefarm.` tests (valid parse; the pedagogical/calibrate/uncertainty-band/unknown-id/unknown-sampler rejections; grid/lhs/random enumeration + determinism); 155 dev / 142 CI. **No normative spec change** — `src/app/nukefarm/` is already in the `02 §2` tree; implements `03 §7` + `06 §2` as written. Documented impl choices in unspecified areas (no ADR): grid = `floor(budget^(1/n))`/axis; lhs/random seeded by a stable FNV-1a of the manifest name. fast4-independent — session-2026-08-03-d.

2026-08-03 — spec/PROGRESS (M5-T3-b split into -b done + -c; M5-T4 dep → M5-T3-c; VERIFY 158/145 + NEXT ACTION M5-T3-c + ready-queue/Current summary), spec/SESSIONS — **M5-T3-b executed** (code — src/app/nukefarm/runner.{h,cpp}, src/api/studio.{h,cpp} [studio_unit_id exposed], tests/unit/test_nukefarm.cpp, CMakeLists — is implementation). The store-backed run-loop ENGINE: `run_sweep(manifest, store, eval)` drives the M5-T3-a `make_sampler` points → `apply_point` (ParamSet → StudioConfig via the flat dotted keys `from_json` maps) → `ns::api::studio_unit_id` → the M5-T2 `SweepStore` (`is_done` SKIP = resume; `record_run` INSERT-OR-IGNORE = no double-count) + the `cursor`. The evaluator is injectable (`default_evaluator` wraps the fast4-independent `generate_run`). **API addition (implementation contact, no ADR):** `ns::api::studio_unit_id(cfg)` exposed on studio.h/.cpp (= `compute_unit_id(cfg_canonical_hash(cfg), {}, seed)`, the EXACT unit_id `generate_run` records; generate_run refactored to call it, so the up-front skip key can never drift from the recorded key). DoD portion met: 3 new `nukefarm.` tests (a 25-point local sweep records each unit once; a partial-resume runs only the remainder + a re-run skips all; a real-burst smoke proves `studio_unit_id` == the recorded `run.json` unit_id); 158 dev / 145 CI. **No normative spec change** — implements `06 §2` + `03 §7` as written. fast4-independent — session-2026-08-03-d.

2026-08-04 — spec/PROGRESS (M5-T3-c split into -c done + -d; M5-T4 dep → M5-T3-d; VERIFY 162/149 + NEXT ACTION M5-T3-d + ready-queue/Current summary), spec/SESSIONS — **M5-T3-c executed** (code — src/app/nukefarm/queue.{h,cpp}, tests/unit/test_nukefarm.cpp, CMakeLists — is implementation). The `06 §2` filesystem work-queue LIBRARY: `WorkQueue` over `pending→claimed→done` + `.lease` files (`enqueue` idempotent-by-unit_id / `claim(now_s)` atomic-rename + lease / `complete` / `reclaim_stale(now_s, threshold_s)` / per-dir counts). Deterministic — time (`now_s`) + threshold are parameters, so the lib is policy-free (the 2×-median policy + the `t_max_s`-fallback amendment ride with the worker/CLI, M5-T3-d). DoD reclaim clause met: 4 new `nukefarm.` tests incl. an induced stale-lease reclaim where the late finisher's `record_run` is a no-op (M5-T2 idempotency) → no double-count; 162 dev / 149 CI. **No normative spec change** — `src/app/nukefarm/` is in the `02 §2` tree; implements `06 §2` as written (the `t_max_s`-fallback ADR is deferred to M5-T3-d, where the threshold policy lives). **Also (owner-requested docs, committed with the M5-T3-b merge b571b1f):** filled `docs/gallery/` (six GadgetLab screenshots — its README referenced four that were never committed) + added a Visualizer section with the pit-microscope hero to the root README + refreshed the stale status snapshot (114→158), honest "schematic · synthetic stub" framing. fast4-independent — session-2026-08-03-d.

2026-08-04 — **spec/DECISIONS (ADR-020, accepted), 06-frontends.md (§2 stale-lease fallback wording)** — behaviour/spec amendment; plus spec/PROGRESS (M5-T3-d split into -d done + -e; M5-T4 dep → M5-T3-e; VERIFY 166/153 + NEXT ACTION M5-T3-e + ready-queue/Current summary), spec/SESSIONS — **M5-T3-d executed** (code — src/app/nukefarm/worker.{h,cpp}, src/core/store/store.{h,cpp} [`recent_wall_s` + a `Stmt::bind_int64`], src/app/nukefarm/runner.{h,cpp} [`param_json` exposed; `apply_point` refactored onto it], tests/unit/test_nukefarm.cpp, CMakeLists — is implementation). **ADR-020 (amendment, cited evidence):** `06 §2`'s stale-lease fallback "3× scenario `t_max_s`" conflated SIM time (~5e-6 s) with wall-clock — 3× ≈ 15 µs marks every lease instantly stale and thrashes the queue — amended to a wall-clock `stale_lease_fallback_s` default 600 s (an operational SIM parameter, not a physical constant / gate). MAJ-40 grep: the only NORMATIVE "3× t_max_s" was `06 §2` (fixed); the `03 §4` / tally `t_max_s` physics field is untouched; the review archives (incl. `reviews/bak/…:716`, where a reviewer originally proposed the flawed fallback) are historical and not edited (the ADR-019 precedent). M5-T3-d code: the distributed-sweep CORE — `stale_threshold_s(store, fallback)` (2× median of the last 10 completed `wall_s`, else the fallback; via the new `SweepStore::recent_wall_s`), `submit(manifest, queue, store)` (enqueue the `make_sampler` points by `studio_unit_id`; payload = the override JSON; skip already-done → resumable), `run_worker(queue, store, eval, clock, threshold)` (reclaim → claim → eval → `record_run` → complete; injected clock; `record_run` idempotency makes a reclaimed unit run twice a no-op). DoD met: 4 new `nukefarm.` tests (the ADR-020 policy; submit-by-unit_id + idempotent; drain 25 into the store; reclaim a pre-staled lease + run it); 166 dev / 153 CI. fast4-independent — session-2026-08-03-d.

2026-08-04 — spec/PROGRESS (M5-T3-e row done → **M5-T3 COMPLETE**; NEXT ACTION M5-T4; VERIFY 168/155 + ready-queue/Current summary), spec/SESSIONS — **M5-T3-e executed; M5-T3 COMPLETE (a–e)** (code — src/app/nukefarm/{cli.{h,cpp}, main.cpp}, tests/unit/test_nukefarm.cpp, CMakeLists — is implementation). The `nukefarm` CLI — the FIRST `src/app/` executable: `cli.{h,cpp}` testable handlers (`cli_submit` = load manifest + open queue/store + `submit`; `cli_worker` = `run_worker` with `wall_clock_seconds` + `stale_threshold_s`; `cli_status` → `StatusReport`) + `main.cpp` a thin CLI11 dispatch (`submit --sweep --queue --db [--local]` / `worker` / `status` / `resume`; `--stale-lease-s` 600, ADR-020; `06 §5` exit codes 0/1/3). DoD met: 2 new `nukefarm.` tests (a `cli_submit`→`cli_worker`→`cli_status` round-trip over a 9-unit grid + a stub eval + a temp-file db; `cli_status` 0 on a fresh store) + a live smoke (`nukefarm status` prints counts; `--help` exits 0); 168 dev / 155 CI (the `nukefarm` exe is a non-test binary, like `gpu_perf` — ctest count unchanged). **No normative spec change** — `src/app/nukefarm/` is in the `02 §2` tree; implements `06 §2` + `06 §5` as written. **M5-T3 (the batch-sweep pipeline) is now complete end-to-end and runnable.** fast4-independent — session-2026-08-03-d.

2026-08-04 — spec/PROGRESS (M5-T4 row done → **M5 4/5**; NEXT ACTION M5-T5; VERIFY 171/158 + ready-queue/Current summary), spec/SESSIONS — **M5-T4 executed** (code — src/app/nukefarm/samplers/builtin.cpp, tests/unit/test_nukefarm.cpp — is implementation). The MCTS sampler: `MctsSampler`, a PUCT tree over the parameter box (k-d bisection of a node's longest axis + progressive widening) behind the `06 §2` `Sampler` interface — `next()` descends by PUCT (`Q + c·√N_parent/(1+N_child)`, expand or select) returning the leaf box CENTRE (deterministic, reproducible); `report()` = `ScoreKind::BandCenter` scoring `-|yield_kt − center| / half_width` toward the calibrate target + backprop along the last path. `make_sampler("mcts")` now builds it, requiring a calibrate objective with `target_yield_kt` (else SweepError — BandCenter needs a centre); `OptimizeExtremum` still rejected; pedagogical+mcts already loader-rejected. DoD met: 3 `nukefarm.` tests (the CONVERGENCE — mcts's best `|yield−center|` beats the seeded random sampler over 300 evals on a paraboloid peaked at (7,3); mcts-needs-a-band; pedagogical+mcts → `parse` throws). 171 dev / 158 CI. **No normative spec change** — implements `06 §2` + `03 §7` as written. **M5 is now 4/5 (T1, T2, T3, T4); only M5-T5 (container) remains.** fast4-independent — session-2026-08-03-d.

2026-08-04 — docs/FUSION-BINDING.md (NEW), spec/PROGRESS (ready-queue item 2 → the fusion binding STARTED / C++ half done + the Current note), spec/SESSIONS — **the FUSION BINDING started** (owner-directed, not a numbered milestone task; code — src/app/studio_bridge/main.cpp, tools/studio_server.py, CMakeLists — is implementation). Mechanism (owner-delegated): a **local dev-server over a CLI bridge** — not WASM (no Emscripten on the box + it can't use CUDA) and not a native/Electron addon yet (the Steam/M7 path); all three wrap the SAME `evaluate_json`/`generate_run_json` DATA surface, so this is a low-regret common foundation. `studio_bridge` (a non-test exe) exposes `evaluate` / `generate-run` as cfg-JSON → result-JSON; `tools/studio_server.py` (stdlib, no deps) serves it over HTTP (`POST /evaluate`,`/generate-run`,`GET /health`). Proven live: `evaluate` → a real MC gauge; `generate-run` on a 9 kg/2.5× pit → **detonate**, k_peak 1.013 → self-quench → 1.62 kt + the 628-generation sample/fission-site stream, fetched over HTTP with zero new C++ deps + no Emscripten. The remaining half — the JS adapter replacing `viz/js/simstub.js` (fetch + reconstruct the flux/compression closures) — is the -e/viz track's domain (`docs/FUSION-BINDING.md`). `ctest` unchanged at 171 (both are non-test binaries, like `gpu_perf`/`nukefarm`). **No normative spec change** — `src/app/` + `tools/` are in the tree; reuses the done `03 §6` / `src/api` surface. Still SIM xs until `fast4` (the mechanism is real). fast4-independent — session-2026-08-03-d.

2026-08-04 — **data/xs/{fast4.json, PROVENANCE-fast4.md, fast4.template.json} (NEW — the cited real 4-group xs, a-set); tools/xs/ (NEW pipeline: build_fast4.py, weighting.py, gate_probe.cpp, setup_wsl_openmc.sh, fetch_endf.sh, setup_hdf5.sh, README.md); spec/DECISIONS (ADR-021 + ADR-022, accepted); src/ref/{ref_generation,ref_transport}.cpp (ADR-021 fix); spec/01-physics.md §2 E1c + spec/05-module-transport.md §1 (ADR-021 amendment)** — plus spec/PROGRESS (M1-T4a-2a done; G0a/G0b → measured-OUTSIDE-band; VERIFY 172/159; NEXT ACTION + ready-queue + blockers), spec/SESSIONS, tests/unit/test_benchmarks.cpp (+1 fast4 sane-k eigen guard), CMakeLists (gate_probe target). **M1-T4a-2a executed** (owner-authorized Path B — code + data are implementation): ENDF/B-VIII.0 (pre-reconstructed HDF5, 293.6 K, Zenodo 8410375) collapsed to `data/xs/fast4.json` (the 8 Godiva+Jezebel isotopes) via openmc.data under WSL — openmc has no Windows build, and this openmc lacks resonance reconstruction so the pre-reconstructed HDF5 was used; documented fast-metal weight; NO xs tuned to a gate. **G0a/G0b MEASURED** (`gate_probe`, 200k×190): Godiva +2562 / Jezebel +1581 pcm — outside ±500. **ADR-021 (fix, cited evidence):** the `ref/` transport correction flew on Σ_tr but did the collision split on **full Σ_t** — INCONSISTENT, scaling the fission source by Σ_tr/Σ_t (realistic forward-peaked μ̄ has ~17000 pcm leverage on Godiva) → fixed to collide on Σ_tr with the scatter reduced to Σ_s−μ̄Σ_s; no-op for μ̄=0 (byte-identical) so the suite stays 172/172. `01 §2` E1c + `05 §1` amended (MAJ-40 grep: those two were the only normative Σ_t-collision spots). **ADR-022 (owner-accepted):** accept the honest 4-group result — the coarse 4-group structure is the residual, not the data; deferred closing paths = self-consistent weighting / a finer group structure; NEVER tune xs. BLK-14 respected (public ENDF/B-VIII.0, no ICSBEP). fast4-b (M1-T4a-2b) remains, not owner-gated. — session-2026-08-04-a

2026-08-04 — **src/gpu/{eigen.cu, transport.cu} (ADR-021 device transport-correction fix); tests/unit/test_gpu.cpp (+1 fast4 Godiva GPU-vs-CPU parity guard); spec/DECISIONS (ADR-021 Consequences → GPU parity DONE); spec/PROGRESS (VERIFY 173/159 + ready-queue/NEXT ACTION), spec/SESSIONS** — **ADR-021 completed on the GPU** (the ADR had flagged it for M4). The device eigen `k_generation` + the fixed-source transport flew on σ_tr but collided on σ_t — the identical inconsistency to the CPU `ref/` — and got the identical fix (collide on σ_tr, scatter reduced to σ_s−μ̄σ_s). The device group data already carried `mu_bar` + `d_group_sigma_tr()`, so no device-data change. Validated: a new `gpu.` test runs the device eigen AND the CPU oracle on the fast4 Godiva assembly (μ̄≠0) and asserts agreement to within 600 pcm — G0c on real data, unmakeable by the μ̄=0 SIM differential tests. Byte-identical for μ̄=0 (the 13 SIM `gpu.` tests unchanged); `ctest -R "^gpu\."` → 14/14, full suite 173/173 (159 CPU + 14 gpu). GPU tests are dev-only, so CI stays 159. — session-2026-08-04-a

2026-08-04 — **tools/gen_gates/gen_gates.py (NEW) + data/benchmarks/gates.toml (NEW, generated); src/app/nukebench/gates.{h,cpp} (NEW — the nukesim_nukebench lib) + tests/unit/test_nukebench.cpp (NEW); CMakeLists (nukesim_nukebench lib + test_nukebench + the gen_gates.check ctest)** — plus spec/PROGRESS (M1-T5 split → M1-T5-a done / M1-T5-b next; VERIFY 178/164; ready-queue/NEXT ACTION), spec/SESSIONS. **M1-T5-a executed** (code + data are implementation): `gen_gates` generates `gates.toml` (03 §10) for G0a/G0b — seeds [1..5], C-900 eigen, criteria `k_deviation_pcm` abs_le C-930 + `sigma_pcm` le C-931 (thresholds single-sourced from `constants.data.toml` by id, never inlined) + `spec_sha256`(08-validation.md, CRLF-normalized) + `--check`. The `nukesim_nukebench` gates loader enforces two integrity guards: each criterion value == its constant (`ns::consts`), and `spec_sha256` == sha256(08-validation.md) (in-tree `core/hash`). 5 `nukebench.` tests (load + both guards + unknown-gate + the gen_gates staleness ctest); 178/164 green. **No normative spec change** — implements 03 §10 as written; `src/app/nukebench` + `tools/gen_gates` are in the 02 §2 tree. G0c/G1-G5 deferred to M1-T5-b. Noted (not fixed — separate): `data/constants.toml` is invalid TOML (a gen_constants emission bug), harmless because the C++ reads the generated header. — session-2026-08-04-a

2026-08-04 — **src/app/nukebench/{gate_report.{h,cpp}, cli.{h,cpp}, main.cpp} (NEW — the gate runner + report + the nukebench CLI exe); tests/unit/test_nukebench.cpp (+3 tests); CMakeLists (gate_report/cli in the lib + the nukebench exe)** — plus spec/PROGRESS (M1-T5-b done + the M1-T5-c row; VERIFY 181/167; ready-queue/NEXT ACTION), spec/SESSIONS. **M1-T5-b executed**: the `nukebench gate` subcommand + `gate_report.json` (03 §11). `run_gate` runs the 08 §2 procedure over the normative seeds (load fast4 + the gate's scenario + `run_eigen` at the gate's `[gate.eigen]` config → measurements → criteria → verdict); the 03 §11 `GateReport` + `to_json`/`parse` + the APPEND-ONLY merge (anti-seed-shopping, MAJ-22); `cli_gate` loads gates.toml (M1-T5-a spec_sha256/drift guards) → runs → writes `artifacts/gate_reports/<gate>/` → exit 0 (pass) / 4 (fail). DoD met (live smokes): `nukebench gate --gate G0a` → honest FAIL exit 4 (+2562 pcm, ADR-022); `--seed`+`--gate` → exit 2; unknown gate → exit 3. 3 new `nukebench.` tests; 181/167 green. **No normative spec change** — implements 06 §1/§5 + 03 §11 as written. `run`/`diff`(G0c) + a gpu gate backend + the committed C-900 gate_report artifacts → M1-T5-c. — session-2026-08-04-a

2026-08-05 — **artifacts/gate_reports/{G0a,G0b}/gate_report.json (NEW — the committed C-900 gate evidence, QC-07); src/app/nukebench/{cli.{h,cpp},main.cpp} (report provenance: --git/--device + ISO-8601 started/finished); tests/unit/test_nukebench.cpp (+1 provenance test)** — plus spec/PROGRESS (M1-T5-c split → c-1 done / c-2 next; gate table G0a/G0b → committed report paths; VERIFY 182/168), spec/SESSIONS. **M1-T5-c-1 executed** (code + committed data are implementation): completed the `03 §11` report provenance the M1-T5-b runner left empty (`cli_gate` stamps git/device/started/finished), then generated + committed the real **C-900** (1e6×250, 5 seeds) `gate_report.json` for G0a/G0b on the ref oracle — honest FAILs **Godiva +2560 pcm** / **Jezebel +1601 pcm** (ADR-022; σ≤7 pcm < C-931 25 ⇒ the σ-criterion passes, only k-deviation fails — the 4-group residual, not statistics; no xs tuned). QC-07: the gate table now cites in-repo report paths. **No normative spec change** — implements `03 §11` + `06 §5` as written. 182/168 green. c-2 (gpu backend) + c-3 (run/diff, G0c) remain. — session-2026-08-05-a
2026-08-05 — **docs/viz-hookup-state.md (NEW — main-track orientation note for the -e/viz track)** — a living note documenting the sim↔viz fusion-binding seam state, the core-vision requirements (one holistic spacetime / true-3D volumetric / zoom-as-camera-focus, with the resurfaced owner source), the honest gap vs the current viz code (main's dots pitscope vs the true-3D InstancedMesh on `viz/atom-proto`, unreconciled with main's real-physics adapter; zoom-as-mode unmet), and the main-track data-contract pledge. Documentation only; no code/spec/gate change. — session-2026-08-05-a

2026-08-05 — **spec/DECISIONS (ADR-023, accepted); spec/09-rendering.md §4 (true-3D-not-points + the demon-core-view requirement); spec/10-ui.md (Panel 7 — Demon core)** — **ADR-023: the demon-core 3D chain-reaction view is a normative first-class deliverable** (one holistic spacetime / true-3D volumetric particles not points/2D / zoom-as-camera-focus not a mode / representative sampling of the real `FissionSource::sites`, never O(N²)). Promotes the owner's "main course" vision (session `58656ddb` 2026-08-03T17:45Z; `docs/viz-hookup-state.md` §3.1; the `core-vision-3d-chain-reaction` memory) from orientation-doc/memory into the NORMATIVE spec, so a flat-points or scene-hiding-mode render is now spec-non-conformant. MAJ-40 grep: `09 §4`'s "neutron points" was the only normative render-primitive "points" wording (fixed); other spec "points" are point-kinetics / point-source / dump-kind / working-point (unrelated). No code / constant / gate-threshold / `gates.toml` / `08-validation.md` change (spec_sha256 unaffected); 182/182 green. Enforcement is on future M7/-e render work; the current live `viz/js/pitscope.js` (`THREE.Points` + scene-hiding `toggle()`) is now the tracked reconciliation target. — session-2026-08-05-a

2026-08-05 — **src/app/nukebench/gate_report.cpp (run_gate ref|gpu dispatch, CUDA-guarded); tests/unit/test_gpu.cpp (+1 gpu run_gate parity test); CMakeLists.txt (nukesim_gpu → nukesim_nukebench inside NUKESIM_WITH_CUDA + test_gpu link); src/app/nukebench/main.cpp (--backend help)** — plus spec/PROGRESS (M1-T5-c-2 done → c-3 next; VERIFY 183/168), spec/SESSIONS. **M1-T5-c-2 executed:** the gpu backend for the gate runner — `run_gate` dispatches `ref|gpu`; the gpu path (CUDA-guarded) calls `gpu_eigen` (256×128, launch-invariant) → `k`, `k_sigma`×1e5 → `sigma_pcm`; on a CPU-only build it `#ifdef`s out and throws. `nukesim_gpu` links into the nukebench lib ONLY inside the `NUKESIM_WITH_CUDA` block (CI/CPU-only unaffected). DoD live: `nukebench gate --gate G0a --backend gpu` runs G0a's 5 seeds in **0.79 s** (vs ~70 min ref C-900), honest fail, matches the ref oracle (ADR-021 parity — the +1 `gpu.` test asserts < 600 pcm). **183 dev / 168 CI green.** No normative spec change. G0c (c-3) reuses this gpu backend. — session-2026-08-05-a

2026-08-05 — **tools/gen_gates/gen_gates.py + data/benchmarks/gates.toml (G0c added); src/app/nukebench/{gate_report.{h,cpp} (run_diff + Attempt.k_b), cli.{h,cpp} (cli_diff), main.cpp (diff subcommand)}; tests/unit/test_gpu.cpp (+1 run_diff test)** — plus spec/PROGRESS (M1-T5-c-3 done → c-3b next; VERIFY 184/168), spec/SESSIONS. **M1-T5-c-3 executed:** `nukebench diff` → the **G0c** cross-backend differential (`08 §2` criterion a). `run_diff` runs the gate on ref + gpu (reusing the c-2 gpu backend) + per seed checks |k_ref−k_gpu| ≤ C-932 (100 pcm) AND ≤ 3√(σ²+σ²). G0c in gates.toml (Godiva, 3-seed set). DoD live: `nukebench diff --gate G0c` → backend `ref|gpu`, honest verdict (a 215-pcm-delta reduced-batch seed fails ≤100 pcm → fail, not fudged), ~26 s. +1 `gpu.` test. **184 dev / 168 CI** (the diff/G0c real run is dev-only — gpu; CI CUDA-guarded). No normative spec change (gates.toml is generated; 08-validation.md unchanged → spec_sha256 stable). Deferred: c-3b (committed C-900 G0c report), G0c (b)/(c) (need GPU eigen extended), nukebench run. — session-2026-08-05-a

2026-08-05 — **artifacts/gate_reports/G0c/gate_report.json (NEW — the committed C-900 G0c differential evidence)** — plus spec/PROGRESS (gate table G0c → PASS), spec/SESSIONS. **M1-T5-c-3b:** generated + committed the real **C-900** G0c report (`nukebench diff --gate G0c`, ref + gpu, 3 seeds). **PASS — the FIRST passing gate:** mean |k_ref − k_gpu| = **6.3 pcm** (seeds 8.1/3.9/7.0), ≤ C-932 100 pcm AND ≤ 3σ (≈24 pcm) — the cross-backend proof of ADR-021 (GPU float32 ≡ CPU double eigen on μ̄≠0 fast4 Godiva). Provenance git d61e0f2, dirty=false. No code/spec change (a data artifact + the gate-table citation). — session-2026-08-05-a
