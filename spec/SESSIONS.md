# SESSIONS — Append-Only Session Log

> Every session appends ONE entry at END (README.md §5.9). Append-only — never edit history.
> Keep entries compact; they are the first thing the next session reads after PROGRESS.md.

## Entry format

```
### YYYY-MM-DD — <session-id> — <task IDs>
- **Did:** concrete bullets (files created/changed, decisions applied).
- **Evidence:** exact commands run + key output lines (gate exits, test counts, timings).
- **State:** green / red (build+tests), branch, commit hashes.
- **Blockers:** what stopped progress, if anything.
- **Notes for next session:** gotchas, context that isn't obvious from the spec.
```

## Bootstrap prompt

The canonical bootstrap prompt lives at `README.md` §9 (single source — do not duplicate the text here; MIN-12). Paste THAT into a fresh session.

---

### 2026-08-02 — session-2026-08-02-a — SPEC AUTHORING (no task ID)
- **Did:** Authored the complete spec system under `C:\NUCLEAR\spec\`: README (router + session protocol), 00 overview/scope, 01 physics (E1–E8), 02 architecture (D1–D9), 03 data contracts (all schemas), 04 core modules, 05 transport/kinetics/hydro, 06 frontends, 07 milestones M0–M7 with task IDs + gates G0–G4, 08 validation (Godiva/Jezebel + gate procedures), 09 rendering, 10 UI, 11 testing, 12 deployment, 13 risks, PROGRESS/SESSIONS/DECISIONS/CHANGELOG, appendix/constants. Source: `compass_artifact_wf-c65239f6-5229-5cf8-862c-4480219688c2_text_markdown.md` + prior brainstorm decisions (C++/CUDA/OptiX; full 4-stage scope; all three frontends + cloud sweeps incl. MCTS).
- **Evidence:** n/a (documentation only; no build exists).
- **State:** green (docs only). No git repo yet — M0-T1 creates it.
- **Blockers:** none.
- **Notes for next session:** Implementation has NOT started. Begin at PROGRESS.md NEXT ACTION (M0-T1). `08-validation.md` contains `[VERIFY]` placeholders that M1-T4 must replace with exact ICSBEP sheet values — do not treat them as final. The research doc should be copied (not moved) into `research/` by M0-T1.

### 2026-08-02 — session-2026-08-02-a (cont.) — PRIOR UPDATE / SPEC AMENDMENT (no task ID)
- **Did:** User reported this machine has a full C/C++/CUDA toolchain + RTX 4070 Ti SUPER and sibling CUDA projects. Verified directly: CUDA 13.1 (V13.1.80), RTX 4070 Ti SUPER (sm_89, 16,376 MiB, driver 610.47, RT cores), MSVC 14.44, CMake 4.3.3, Python 3.13.2, OptiX SDK 9.1.0 (`C:\ProgramData\NVIDIA Corporation\OptiX SDK 9.1.0`), vcpkg at `C:\vcpkg`. Sibling projects inspected: `C:\Buddhabrot_CUDA` (CLAUDE.md + DESIGN_SPEC pattern), `C:\backrooms` (PROGRESS.md + vcpkg manifest presets — the "contracts-and-gates" process the user referenced; our spec system is congruent), `C:\Booster_Lander_Simulator` (DECISIONS.md/RUN_STATE/HANDOFF files — same multi-session pattern as our PROGRESS/SESSIONS/DECISIONS). Amended spec per its own amendment protocol: toolchain pins (`12-deployment.md`), GPU-first policy + D1/D2 notes (`02-architecture.md`), M4 retimed to run in parallel with M2/M3 (`07-milestones.md`), G4 perf targets re-based to 4070 Ti SUPER (`08-validation.md`), sm targets (`05-module-transport.md`), environment notes (`PROGRESS.md`), ADR-009 (`DECISIONS.md`), CHANGELOG entry.
- **Evidence:** `nvcc --version` → 13.1.80; `nvidia-smi` → RTX 4070 Ti SUPER, 16376 MiB, 8.9, 610.47; OptiX SDK 9.1.0 dir listing; `/c/vcpkg/vcpkg.exe` exists; `cmake --version` → 4.3.3; `python --version` → 3.13.2.
- **State:** green (docs only; still no repo — M0-T1 creates it).
- **Blockers:** none.
- **Notes for next session:** Default to `--backend gpu` everywhere; CPU `ref/` is oracle-only. OptiX 9.1 is present locally (M6 unblocked on that axis). When writing CMake presets in M0-T2, set `VCPKG_ROOT=C:\vcpkg` and follow backrooms' vcpkg-manifest preset idiom. Don't re-verify the toolchain — it's recorded in PROGRESS.md; only re-check if a build error suggests drift.

### 2026-08-02 — session-2026-08-02-a (cont.) — REVIEW TRIAGE (spec v0.2, no task ID)
- **Did:** Triaged ALL THREE external reviews (see ADR-012 for the full adjudication): `spec/reviews/REVIEW_2026-08-02.md` (BLK/MAJ), `spec/reviews/bak/REVIEW_2026-08-02.md` (A/B/C/D), `spec/reviews/bak/REVIEW_2026-08-02_other-agent_0129.md` (lettered). Read via `C:\chunker` (worked cleanly; `.chunks/` dirs are regenerable tool artifacts, gitignored). Applied the consolidated amendment: **full rewrites** of 01-physics, 02-architecture, 03-data-contracts, 04-module-core, 05-module-transport, 06-frontends, 07-milestones, 08-validation, appendix/constants, 10-ui, 11-testing, 13-risks, spec/README, PROGRESS; **targeted edits** to 00-overview, 09-rendering, 12-deployment, DECISIONS, CHANGELOG, SESSIONS, root README. Physics fixes that change the model: E1c full implicit capture; E3a source term + ν̄_eff + k_prompt wording; Λ from eigen (E3b, ∝1/ρ); energy-conserving E4; E5 defined; Tier-1 formula fixed (was r→0); Tier-2 derived t_c + γ=5/3 label; E6 quench now ε·F_peak (was peak-power termination, ~2× yield error); quench split reported. Gate redesign: all thresholds carry constant IDs (C-930–C-946) + derivations; normative seed sets; G1a mass-fraction gate; G1b 4-criterion; G2 envelope band [16.6,26.8]kt + consistency + timing + σ + max_q; G3 decoupled from c_a calibration; G4 per-device + gen/s + eigen-call cap; G5 parity first-class; CONDITIONAL fallback for G0a/b. Process: repo root = C:\NUCLEAR (no spec copy); ready-queue + depends_on; claim tiebreak; WIP journals; impact-analysis rule; falsifiable VERIFY; checkpoint v2 (identity+CRC+native precision+eigen state); canonical_hash normative; deterministic GPU design (prefix-sum slots, parent-derived streams, fixed-point reductions). Scope: interactive-vs-automated distinction + axis_class mechanical enforcement; rendering physical-honesty rule. Owner decisions applied: public GitHub + MIT (ADR-011); ICSBEP owner-gated (M1-T4b), open-literature default (M1-T4a).
- **Evidence:** reviews verified by spot-recomputation before acceptance (TOML parse error, tally 10× arithmetic, Guderley γ=5/3 value, pit r/M/ρ over-determination 6.30 vs 6.15 kg, Fuchs–Nordheim post-peak energy split, fmod-on-float scheduling, entropy degeneracy on 1-cell mesh). Adjudications documented in ADR-012 items 1–9. Reviewer preserve-lists honored (see ADR-012 consequences).
- **State:** green (docs only; still no repo — M0-T1 creates it IN PLACE now).
- **Blockers:** M0-T6-b needs owner to create the public GitHub repo; M1-T4b optional owner ICSBEP decision.
- **Notes for next session:** Spec is now v0.2 — do not read the pre-triage state from memory; the files changed substantially. M0-T1 is the NEXT ACTION per PROGRESS.md. Note M0-T1 no longer copies spec/ anywhere (single-tree rule). `.gitignore` must include `*.chunks/`. If any review finding appears unaddressed, check ADR-012's adjudication list first, then the target file; if genuinely missed, that's a defect — amend per protocol.

### 2026-08-03 — session-2026-08-03-a — QC PASS on the v0.2 triage (no task ID; amendment)
- **Did:** Independent QC of the ADR-012 triage against the three review reports and against arithmetic. Found and fixed 16 items. Blocking-class: **QC-01** ν̄ labelled PROMPT in `01 §1`/C-020 while `01 §4` applied a (1−β) correction on top — double-counting worth ~650 pcm on Godiva, larger than G0a's whole tolerance (→ ADR-013: ν̄ is TOTAL, `beta` REQUIRED per isotope, k_prompt derived once downstream); **QC-02** G2 criterion 4 used the `use=crosscheck` kt/kg constant inside a gate, which `11 §4`'s own static check greps for (restated via Φ_kt/C-910/C-916); **QC-03** molar masses existed for 6 of the 16 species `08 §1` requires, and C-915 was *natural* Ga while `03 §3` forbids natural-abundance expansion and the canonical pit uses Ga-69/Ga-71 separately — so number density was uncomputable for the canonical material (added C-915a/b, C-919–C-929 + a completeness rule with a hard-error check). Major: **QC-04** the replacement tally example failed 2 of its own 8 invariants (I3 5.5e-4, I4 1.25e-3, against 1e-6); **QC-05** invariant 4 was structurally unsatisfiable at 1e-6 with any Pu-240 fissions (3.6e-5 error from using M_Pu239 for all Pu); **QC-06** G2's eigen configuration was unspecified — a ~60× cost swing; **QC-07** all of `artifacts/` was gitignored, so every gate-evidence path in PROGRESS.md pointed at an unreadable file. Plus QC-08…QC-16 (wip dir, Σ_a→Σ_c + Σ_f=0, invariant 9, band-coupling notes, closed-risk section, Godiva budget annotation, claim-protocol bootstrap exemption, verification oracle).
- **Evidence:** all nine `03 §5` invariants re-derived in Python at printed precision on the new example — I2 0.0, I3 7.5e-8, I4 2.6e-7, I5 0.0, I6 0.0, I9 exact. Materials example independently confirmed self-consistent (atom fractions 1.000000, Ga 1.0008 wt%, Pu-240 1.00 at%, ρ 15.23 × V(4.585) = 6.149 kg). Φ_kt = 1.4508041e23. Transfer-matrix rows all 1.000000, no upscatter.
- **State:** green (docs only). Spec **v0.3**. No repo yet — M0-T1 creates it.
- **Blockers:** none new. Owner pre-step recommended: create the empty public repo before M0-T1.
- **Notes for next session:** **Review is closed at v0.3** (README §10) — further spec change comes from implementation contact, not review. Two things to steal rather than write: `C:\Astrophage\scripts\canon.py` + `docs/VERIFICATION.md` (the generated first-principles oracle; it caught a 47× integrator error there, and it is the mechanism that would have caught BLK-01/03/04 here) and Astrophage's CMake/preset setup, which already builds C++20 + CUDA 13.1 + sm_89 + MSVC on this exact machine. Both are wired into M0-T2/M0-T3 DoDs. Astrophage's determinism trio (id-keyed streams, 64-bit fixed-point accumulation, stable sort) is the same design `05 §6` now mandates — it is proven in production at `m5-green`, so port it rather than re-deriving.

### 2026-08-03 — session-2026-08-03-a — M0-T1 (repo initialisation)
- **Did:** Executed M0-T1. `git init -b main` in place at `C:\NUCLEAR` (BLK-12 — no nesting, one spec tree). Created the full `02 §2` skeleton with `.gitkeep` in every empty directory. Authored MIT `LICENSE`, `NOTICE.md` (sources + the six hard scope boundaries), `.gitattributes` (LF normalisation, matching `canonical_hash()`'s `04 §6` rule so the CRLF↔LF stability test has nothing to fight), and rewrote the root README to lead with scope rather than features — the first thing a stranger reads on a public repo. `.gitignore` written verbatim from `02 §2` plus a clearly-labelled non-normative hygiene block. Moved the research document to `research/trinity-gadget-technical-reference.md`. Created the public repo and pushed.
- **Evidence:** `git check-ignore` verified in BOTH directions — `artifacts/gate_reports/x.json` and `artifacts/perf_history.jsonl` report NOT ignored (the QC-07 evidence trail survives), while `build/x.o`, `artifacts/run1/tally.json`, `artifacts/perf_history_202608.jsonl`, `*.chunks/` and `.env` are ignored. Secret scan before publishing: zero credential hits (all matches were historical prose — "secret laboratory at Los Alamos" — or tiktoken counters in chunker manifests); no `.env`/`.pem`/`.key`/`id_rsa` anywhere. 69 files tracked. Commit `77d950a`. Remote: <https://github.com/bochen2029-pixel/nuclear-sim>, `main` tracking `origin/main`.
- **State:** green. Spec v0.3.
- **Blockers:** none. The M0-T6-b owner blocker is resolved — the repo exists.
- **Notes for next session:** **The remote now exists, so the full claim protocol applies from M0-T2 onward** (branch + PROGRESS edit + pushed `claim:` commit) and parallel sessions are safe; M0-T1's bootstrap exemption is spent. For M0-T2, port `C:\Astrophage`'s CMake/preset setup rather than writing one — it already builds C++20 + CUDA 13.1 + sm_89 + MSVC on this exact machine, and it has already paid the CMake-4-vs-old-port-minimums and vcpkg-baseline tax. For M0-T3, port `C:\Astrophage\scripts\canon.py` + its generated `docs/VERIFICATION.md`: that generated first-principles oracle is what `11 §5` now requires, and it is the mechanism that would have caught BLK-01/BLK-03/BLK-04 before a line of code existed.

### 2026-08-02 — session-2026-08-02-b — M0-T2 (toolchain)
- **Did:** Authored `CMakeLists.txt`, `CMakePresets.json`, `vcpkg.json`, `vcpkg-configuration.json`, `scripts/build.ps1`, and a toolchain smoke test (`tests/unit/test_toolchain.{cpp,cu}` + `.h`). Presets per `12 §1`: configure `win-x64`/`linux-x64`/`linux-cuda`, build+test `<configure>-deb`/`-rel`, with `CMAKE_POLICY_VERSION_MINIMUM=3.5`, `VCPKG_ROOT=C:\vcpkg`, `CMAKE_CUDA_ARCHITECTURES=89-real;80-virtual;90-virtual`. `NUKESIM_WITH_CUDA` auto-detects nvcc; `NUKESIM_WITH_OPTIX` stays OFF until `OPTIX_SDK_ROOT` resolves (correctly OFF here — the var is unset). Warnings are per-target via `nukesim_strict()` (`02 §4`), never global. Spec amendments (clarification class, no ADR): `02 §2` gains `scripts/`, `02 §4` + `12 §1` name the vcpkg baseline field correctly, `12 §1` records the pinned SHA, triplet and generator choice.
- **Evidence:** `cmake --preset win-x64 && cmake --build --preset win-x64-rel && ctest --preset win-x64-rel` → `100% tests passed, 0 tests failed out of 6`. DoD cold-start evidence via `scripts/build.ps1 -Clean -ColdCache` (build dir deleted, `VCPKG_DEFAULT_BINARY_CACHE` redirected to a scratch dir): all 10 packages genuinely rebuilt from source, `All requested installations completed successfully in: 1.6 min`, then build + 6/6 green, script exit 0. Debug also green (`win-x64-deb`, 6/6). CUDA test is a real device round-trip (kernel increments 41 → 42 on the 4070 Ti SUPER), not a compile-only check.
- **State:** green. Branch `task/M0-T2-toolchain`, merged to `main`.
- **Blockers:** none.
- **Notes for next session:**
  - **Two real toolchain defects were found and fixed; both would have landed on M4.** (1) `-Werror=all-warnings` + an inherited `/W4` makes nvcc's *generated* `tmpxft_*.cudafe1.stub.c` fail with C4211. `/WX` reaches the CUDA host pass through MSBuild's project-level `TreatWarningAsError` property, not as a literal flag, so it is invisible on the nvcc command line. Fixed with a narrow `-Xcompiler=/wd4211`; first-party code cannot emit C4211. Do **not** try `-Xcompiler=/W3` to lower the host level — it raises D9025 which that same inherited property promotes to an error. (2) NVIDIA ships `cudart_static.lib` against the **release** CRT, so Debug builds emitted `LNK4098 LIBCMT conflicts`; fixed with `/NODEFAULTLIB:LIBCMT` on Debug only. Both fixes live in `nukesim_strict()` — call it on every first-party target and you inherit them.
  - **The task text's "port from `C:\Astrophage`" premise was half wrong.** Astrophage has **no `CMakePresets.json` and no vcpkg** — it uses FetchContent and a hand-rolled `build.ps1`. What was portable and was ported: architectures set before the CUDA language is enabled, `LANGUAGES C` for GLFW/GLAD at M7, the per-target strict() function, static runtimes. The vcpkg manifest-preset idiom came from `C:\backrooms` instead, which is what PROGRESS.md's own Environment-notes line actually said. Deliberate departure from Astrophage: `CMAKE_CUDA_STANDARD 20`, not its 17, because `.cu` files will include C++20 core headers; `test_toolchain.cu` exists to prove nvcc 13.1 + MSVC 14.44 accepts that, and it does.
  - **Generator is `Visual Studio 17 2022`, not Ninja** — deliberate, recorded in `12 §1`. Neither `ninja` nor `cl` is on PATH here, so a Ninja preset would make PROGRESS.md's verify-first probe fail from an ordinary terminal on a perfectly green tree. Linux presets use Ninja Multi-Config and need `VCPKG_ROOT` in the environment.
  - **`catch_discover_tests` is registered with `TEST_PREFIX "toolchain."`.** Keep that convention: it names ctest entries after Catch2 *test cases*, so the per-task VERIFY probes the spec already commits to (`ctest -R constants`, `-R rng`, `-R loaders`) only match if each executable carries a module prefix.
  - Two things deliberately **not** done, to stay in scope. (a) No `gate.ps1`: `07`'s gate table says a gate is met only when `nukebench gate` exits 0, and `tools/ci/` belongs to M0-T6 — a second gate mechanism now would contradict `08`. (b) No `.env.example`, which `12 §5` requires but no task owns; its contents are mostly `S3_*`/`RUNPOD_*`, so M5-T5 is the natural owner. **Flagging it so it does not fall through the cracks.**
  - Bookkeeping: the SESSIONS dates now read 08-02, 08-03, 08-03, 08-02. That is not a clock rollback — the machine and every git commit say 2026-08-02, and the two entries above labelled 2026-08-03 are a day ahead of the clock they were written on. This session used the real date per §5.4.

### 2026-08-02 — session-2026-08-02-b (cont.) — M0-T2 POST-MERGE REVIEW FIXES (amendment, no task ID)
- **Did:** Applied four clarification-class amendments from an external review of the merged M0-T2. (1) `02 §4`'s "CUDA host flags routed with `-Xcompiler`" was false on MSVC — corrected, with the accepted C4211 hole recorded. (2) `11 §1` gains the **normative** `TEST_PREFIX` convention + a module→prefix→probe table; the convention was implemented in M0-T2 but lived only in a CMake comment, so M0-T3/T4/T5's already-committed `ctest -R …` probes had no normative basis. (3) `02 §2`'s tree gains `docs/` (missed by M0-T1; M0-T3 writes `docs/VERIFICATION.md` into it). (4) `12 §1` restates the vcpkg triplet as a pin with its silent-failure mode, cross-referenced from the CMake row. Also assigned the ownerless `.env.example` (`12 §5`) to **M0-T6**, not M5-T5 — CI is where secrets first matter and it is four tasks away rather than fifteen.
- **Evidence:** the review proposed replacement text for (1) asserting that nvcc injects `/W3` and that first-party `.cu` host code therefore compiles at `/W3` while `.cpp` compiles at `/W4`, and derived a design rule from it. **That is wrong, and was not adopted.** Rebuilt the `.cu` and read the emitted command line: `-Xcompiler "/EHsc /W4 /nologo /O2 /FS /MT "` — the CUDA host pass compiles at **`/W4`**. Independently corroborated by C4211 being a level-4 warning that fired at all. The real mechanism is that CMake's VS generator hoists `/W4` onto the CUDA host pass and `/WX` arrives via MSBuild's project-level `TreatWarningAsError`, which is also why `/WX` never appears literally on the nvcc line. The amendment records the true mechanism and the true residual risk (`/wd4211` suppresses C4211 for first-party code in the same TU) rather than the invented one (`.cu` host code compiling at a weaker level). VERIFY re-run after all edits: `100% tests passed, 0 tests failed out of 6`.
- **State:** green. Spec-only changes; no build files touched. `main`.
- **Blockers:** none.
- **Notes for next session:**
  - **Process corrections accepted and recorded here rather than re-litigated:** one `claim:` commit per task, not two — §7's race tiebreak reads claim-commit timestamps, and this session pushed both `85fb56c` and `70f22ff`. Session-id dating stands as explained in the entry above.
  - **Treat the NEXT ACTION as a recommendation with reasoning attached, not an order** (§5.2 already says so — "if you disagree, say why and update it first"). M0-T2's handoff line instructed porting presets from `C:\Astrophage`, which has neither presets nor a vcpkg manifest; the spec's own `PROGRESS.md` environment notes pointed at `C:\backrooms`, which has both. Reading past the wrong instruction to the right source was correct. The same discipline applies to review feedback: item (1) above was a genuine defect paired with an incorrect fix, and the build is the tiebreaker.
  - M0-T3 is unblocked and is the recommended pick; `docs/` and the `TEST_PREFIX` row it needs are both now in the spec. `docs/VERIFICATION.md` must be **generated, never hand-written** (`11 §5`) — port `C:\Astrophage\scripts\canon.py`, which unlike the presets genuinely exists. The generator must **hard-error**, not warn, when a material names a species with no molar mass (appendix §3): a missing mass silently corrupts every macroscopic cross section.
  - M0-T4 (Philox) is independently runnable in a parallel session — full claim protocol with timestamp tiebreak is live.

### 2026-08-02 — session-2026-08-02-b (cont.) — M0-T3-a (constants pipeline)
- **Did:** Split M0-T3 per §8 (oversized for one session) into **-a** constants pipeline and **-b** the `11 §5` generated oracle; executed -a. Authored `spec/appendix/constants.data.toml` — 96 entries covering every appendix row (§1 physics, §2 geometry ODs, §3 molar masses, §4 SIM knobs, §5 gate bands). Wrote `tools/gen_constants/gen_constants.py` (validate → resolve derived → emit `data/constants.toml`, `constants_generated.h`, `constants_generated.cuh`; `--check` for staleness), `tools/verify/constants_roundtrip.py` (bijection + drift), `tools/verify/constants_negative.py` (26 validator guards), `tests/unit/test_constants.cpp`, and the CMake wiring. Generated outputs are committed so a fresh clone compiles without Python; `constants.generated_current` is what stops them drifting. Logged **ADR-015** and amended `03 §1`.
- **Evidence:** `ctest --preset win-x64-rel` → **15/15 passed**. `ctest -R constants` → 9 tests. Roundtrip: "96 appendix rows <-> 96 strict entries, bijective". Negative suite: 26 guards behave, including both the DoD names (missing cite, missing status). **Derived C-041 recomputed as 1.4508041e23**, matching to 8 significant figures the value the earlier QC session derived independently in Python — an actual cross-check landing, not a transcription. The roundtrip caught 3 real defects on first run: two were my checker comparing a leading number against range-only cells (`16–17`, `1.0e8–2.0e8`, which have no nominal), one was genuine drift — C-053's citation had `rho^-2` where the appendix has `ρ⁻²`.
- **State:** green. Branch `task/M0-T3-constants`, merged to `main`.
- **Blockers:** none.
- **Notes for next session:**
  - **ADR-015 exists because the schema could not represent two whole appendix sections.** `03 §1` assumed a scalar `value` with an optional band. Appendix §4 is tuples ("1e6 / 50 / 200") and §5 is pure gate bands ("[16.6, 26.8] kt") with no nominal. Forcing a band into `value`+`lo`+`hi` means inventing a midpoint — and the centre of G2's yield band is not a predicted yield, but once emitted as `g2_yield_band` it is one autocomplete from being compared against. `[[band]]` therefore emits **only** `_lo`/`_hi`; there is deliberately no bare identifier. `gen_gates` (M1-T5) must not synthesise a nominal from one.
  - **`appendix_text` on every entry is the drift detector, and it is why this is worth more than a type check.** The appendix Value cells are prose-numerics ("1.8 (1.7–1.8)", "24.8 ± 2", "12.7 (13 class)"). Comparing parsed leading numbers alone would let a widened range pass silently. Each entry mirrors its cell verbatim and the roundtrip compares it exactly after markdown normalisation — which is precisely what caught the `ρ⁻²` drift above.
  - **Band checks are generated `static_assert`s, not a unit test.** `03 §1` originally specified a unit test for `lo ≤ value ≤ hi`; a generated static_assert is strictly stronger (cannot be forgotten for a new constant, fails at compile time). `03 §1` amended to say so. Same idea for PENDING: the header emits `double name() = delete;`, so "reading a PENDING constant raises" becomes a compile error rather than a runtime one.
  - **Geometry masses are deliberately NOT in the strict file.** `03 §1` says ranges stay in the appendix, and §2's mass column is ranges ("108–111 kg"). Consequence for M0-T3-b: `11 §5`'s layer-mass section and the C-102 pit 2.4% over-determination check need `data/materials/*.json` densities, which M2-T1 authors — so -b must emit an explicit PENDING block there rather than quietly omitting the section. Recorded in M0-T3-b's DoD.
  - M0-T4 is the recommended next pick. The C-907 stream ids are already generated as `ns::consts::rng_stream_registry::{source,flight,collision,fission,scatter,hydro,render}` — link `nukesim_core_constants` and use them rather than re-declaring; a divergent stream id would silently correlate two physical processes.

### 2026-08-02 — session-2026-08-02-b (cont.) — M0-T3-a CONFORMANCE FIX + M0-T4 (Philox RNG)
- **Did:** (1) Fixed four M0-T3-a defects against `04 §1`, which I had not read when authoring it — namespace `nukesim::constants` → **`ns::consts`**; `use="crosscheck"` constants moved into **`ns::consts::crosscheck`**; added the runtime `get`/`get_lo`/`get_hi` lookup; made `unit` a required field. Renamed the outputs to `constants.h`/`.cuh`, which is what both `03 §1` and `04 §1` call them, and added `tools/verify/crosscheck_misuse.py`. (2) Executed **M0-T4**: `src/core/rng/philox.h` (the bare Philox4x32-10 bijection, `constexpr`) and `src/core/rng/rng.h` (`ns::rng::Stream` with the normative counter/key layout, `uniform_f`/`uniform_d`/`normal_f`, `state()`, and `fork`), plus `tools/verify/rng_kat_gen.py`, the frozen `tests/unit/rng_kat.inl`, and `tests/unit/test_rng.cpp`.
- **Evidence:** `ctest --preset win-x64-rel` → **27/27 passed**; `-R rng` selects 10, `-R constants` 10, `-R toolchain` 6. All three published Random123 Philox4x32-10 vectors reproduce exactly — the all-zero case is also a `static_assert`, so a regression cannot link. `fork(42,1000,3) = 12597386599640143736` frozen. 4096 progeny ordinals from one parent and 4096 parents at one ordinal both produce 4096 distinct child streams (no collisions). `(ctr,sub)` resume verified at every cut from 1 to 9 draws, i.e. across a block boundary. 28 constants validator guards.
- **State:** green. Branches `fix/M0-T3-a-04-conformance` and `task/M0-T4-rng`, both merged to `main`.
- **Blockers:** none.
- **Notes for next session:**
  - **Read the `04`/`05` module spec for your task, not just the `03` data contract.** M0-T3-a was authored against `03 §1` alone and missed four things `04 §1` requires. One was functional, not cosmetic: without `ns::consts::crosscheck`, `11 §4`'s static misuse grep had no qualifier to grep for, so the rule that C-042/C-043 and the C-09x published yields must never enter a tally or gate was unenforced. Those are figures the model is supposed to *predict*; letting one into a compute path would make the model agree with the answer by construction. `tools/verify/crosscheck_misuse.py` now enforces it — vacuous today (0 compute-path sources) but wired and inside `ctest -R constants`.
  - **The project-local RNG vector was NOT self-recorded, and that distinction is the point.** `04 §2` says to freeze the first 16 `uniform_f()` of `Stream(0,0)`. Recording whatever the C++ emitted would only prove self-consistency. Instead `tools/verify/rng_kat_gen.py` is a second Philox written from the algorithm, and it refuses to emit the golden file unless it first reproduces the three published Random123 vectors. Two independent implementations agreeing on counter packing, sub-word order and the 24-bit conversion is evidence; one agreeing with itself is not. The `--check` mode runs as `rng.kat_independent_reference`, so the independence survives.
  - **`normal_f()` deliberately discards the Box–Muller partner instead of caching it.** Caching is the obvious optimisation and it would be a latent D9 bug: `04 §2` and `03 §8 §2` define the resumable state as exactly `(ctr, sub)`, so a cached deviate is invisible state. A checkpoint taken between two calls would resume having lost it, and kill→resume→bit-identical would fail *only sometimes*. One `cos` is cheaper than an intermittent determinism failure. There is a test asserting resume reproduces `normal_f()` exactly.
  - **`splitmix64` in `04 §2`'s fork formula is ambiguous** — some references omit the golden-ratio increment. Took the full standard variant, documented it in `rng.h`, and froze `fork(42,1000,3)`; that KAT is what makes the choice normative. **M4-T1 must reproduce that exact value on the device** (`04 §2c`).
  - Two process slips this session, both mine: a commit message passed to `-m` contained backticks, which the shell executed and ate two phrases from the message (use `git commit -F -` with a heredoc); and `ctest --preset` writes `Testing/Temporary/` into the repo root, two files of which reached a commit before being untracked and gitignored.

### 2026-08-02 — session-2026-08-02-b (cont.) — M0-T3-b (generated verification oracle)
- **Did:** `tools/verify/oracle.py` generates `docs/VERIFICATION.md` from `constants.data.toml` and the spec markdown, never importing or linking `nscore` (`11 §5`). All six required sections: (1) Φ_kt recomputed + per-nuclide kt/kg basis gap; (2) layer masses — explicitly **PENDING(M2-T1)** with the one piece already computable, the C-102 pit density over-determination; (3) Tier-1 radius map at u ∈ {0,¼,½,¾,1} with endpoint and mass-conservation columns; (4) Fuchs–Nordheim as the reason E6 cannot terminate at k=1; (5) Λ ∝ 1/ρ over the compression range; (6) all nine `03 §5` invariants on the canonical example, **parsed out of `03-data-contracts.md`** so example and check cannot drift. Also extended the strict file and roundtrip to carry the appendix §2 Mass column (`appendix_mass_text` + `mass_kg`/`mass_lo_kg`/`mass_hi_kg`), since invariant 4 needs the pit's 6.15 kg and parsing it out of prose would be fragile.
- **Evidence:** `ctest --preset win-x64-rel` → **28/28**; `-R oracle` selects 1. **9/9 tally invariants pass**: I3 = 7.534e-08, which independently reproduces the QC session's recorded 7.5e-8, and I4 = 3.864e-08 against a 1e-6 tolerance. Tier-1 endpoints hold at 0.00e+00 relative error and mass conservation at 1.47e-16; s=1 reproduces C-060 = 2.2 exactly by the closed loop ρ/ρ₀ = (r₀/r)³. Post-peak yield fraction of the canonical example is 0.470, inside `01 §6`'s healthy 0.3–0.5 band.
- **State:** green. Branch `task/M0-T3-b-oracle`, merged to `main`. **M0 is now done except M0-T5 and M0-T6.**
- **Blockers:** none.
- **Notes for next session:**
  - **The oracle caught a real defect on its first run, and the loop closed properly.** The appendix told readouts using C-042/C-043 to label a "~5%" basis difference. Measured against the C-040 basis it is **+5.3% for Pu-239 but only +0.5% for U-235** — implied per-fission energies ≈190 MeV and ≈181 MeV. Sher & Beck genuinely report a different release per nuclide, so the *constants* are fine; the *wording* was wrong, and a U-235 readout applying "~5%" would have overstated its own error by an order of magnitude. Appendix amended to state both figures. This is the intended cycle: oracle finds it → spec is amended → `oracle.verification_current --strict` keeps it from coming back.
  - **`--strict` is deliberate.** The ctest entry fails the build on a numerical discrepancy, not merely on a stale file. An oracle whose findings nobody has to act on is decoration.
  - **The PENDING block in §2 is deliberate too.** Layer-mass recomputation needs `data/materials/*.json` densities (M2-T1). It emits an explicit "PENDING — requires M2-T1" block rather than omitting the section, because an absent section reads as "checked and fine". **M2-T1 should extend `oracle.py` §2 rather than writing a separate check.**
  - The oracle reads the `03 §5` example by parsing the spec markdown. If that example is ever edited, the invariants are re-evaluated automatically — but it also means a malformed edit to that JSON block breaks `ctest -R oracle` rather than passing silently. That is the intended failure direction.
  - M0-T5 (loaders) is the only thing left before M0-T6, and it is the last gate on all of M1.

### 2026-08-02 — session-2026-08-02-b (cont.) — M0-T5 (xs / materials / scenario loaders)
- **Did:** `src/core/diagnostics.h` (`LoadError` carrying file/field/constraint, plus a non-fatal `LoadWarning` for `03 §3`'s WARN-not-ERROR rules), `src/core/xs/` (schema v2 in full), `src/core/material/` (atom fractions → number densities → macroscopic Σ, `04 §5`), `src/core/scenario/` (the whole of `03 §4` including `[data]` resolution, `[ui.*]` range enforcement, the override grammar, `mode="td"`, and the `[source]` rules), and `tests/unit/test_loaders.cpp`. Two spec amendments, both clarification class.
- **Evidence:** `ctest --preset win-x64-rel` → **39/39**. All five per-task probes resolve: `-R toolchain` 6, `-R constants` 11, `-R rng` 10, `-R loaders` 11, `-R oracle` 1. Positive tests parse the spec's **own** example documents, extracted from `03-data-contracts.md` at run time by `tests/unit/spec_examples.h` — `03`'s header says tests parse them verbatim, and a copied fixture would drift. Negative coverage is one case per violation class: `sigma_a` migration, in-file `sigma_t`, missing `mu_bar`, missing `beta`, non-descending bounds, wrong group count, upscatter, transfer row ≠ 1, null transfer outside a SIM set, unknown schema version, missing cite, implausible beta; fractions ≠ 1, species with no molar mass, non-positive density; `mode="td"`, unknown key at top level and in a table, non-increasing radii, `layers` under `[data]`, unknown tally, missing `[source]`, wrong `t_zero`, tier 3, value outside its `[ui.*]` range, missing seed, xs-name mismatch, unresolvable material, and the jezebel/`pu_ga_delta` prohibition.
- **State:** green. Branch `task/M0-T5-loaders`, merged to `main`. **M0 is complete except M0-T6.**
- **Blockers:** none.
- **Notes for next session:**
  - **`03 §4`'s example had `layers` inside `[data]`, and only writing the loader revealed it.** In TOML every bare key after a table header joins that table, so the array — written below `[data]` with just a blank line between — parsed to `data.layers`, not a top-level `layers`. Everything else said top-level was intended (`[data]`'s own comment is "which datasets to load"; `04 §4` has `LayerStack::from_scenario`; the override grammar is `layers.<id>.<field>` with no `data.` prefix). Example amended to put the array first, and the loader emits a dedicated diagnostic if it is found under `[data]` — checked BEFORE the generic unknown-key sweep, because "unknown key: data.layers" would send the reader hunting for a typo. This is exactly the defect class `README §10` predicted: findable only by running code.
  - **`[source]` under a non-`fixed_source` mode WARNs, it does not error.** `03 §4` says "REQUIRED iff mode = fixed_source", and I first implemented the strict biconditional — which rejected the spec's own canonical example, since that example carries a `[source]` block under `mode = "alpha"` as a schema illustration. Ignore-with-WARN is the pattern `03 §4` already uses for `compression.t_c_s` at tier 2, so it is what the loader does. Worth knowing before someone "fixes" it back.
  - **`04 §3`'s `IsotopeXS` predated ADR-013** and listed only `{name, g, transfer}` — no `beta`, which `03 §2` makes REQUIRED and which is the *only* enforcement of the TOTAL-ν̄ convention (a prompt-only dataset is numerically undetectable). Struct amended. The loader's diagnostic for a missing `beta` says so explicitly rather than "field required".
  - **Materials may name species the xs set does not carry.** A structural element with a molar mass but no cross sections contributes mass (and therefore mean molar mass and number density) but nothing to Σ. That is deliberate — `Constituent::iso` is null in that case. The *molar mass* is the hard error, per appendix §3, because that is what silently corrupts every macroscopic cross section.
  - The loaders' positive fixtures clone the §2 Pu239 entry to fill out the isotope set and stub four materials, all marked `status = "SIM"` with a "test scaffolding" cite. They are scaffolding in a temp directory, never committed data — **M1-T4a authors the cited xs set, M2-T1 the cited materials.**
  - `canonical_hash()` (`04 §6`) is **not** implemented (see the M0-T6 entry below — still true). Its test is grouped with the geometry line in `04 §7` (M1-T1), and it needs SHA-256, which is not in the vcpkg baseline — an in-tree implementation checked against the NIST vectors is the obvious route, but adding a crypto dependency instead would need an ADR (`02 §4`). Flagging it so it is not assumed present.

### 2026-08-02 — session-2026-08-02-b (cont.) — M0-T6 (CI) — **M0 COMPLETE**
- **Did:** `tools/ci/local_ci.ps1` and `tools/ci/local_ci.sh` (the same sequence the workflow runs, non-zero on any failure), `.github/workflows/ci.yml` (windows-2022 + ubuntu-latest + a gate-evidence job), and `.env.example`. Amended `02 §2`'s tree for `.env.example` and `.github/workflows/`, and `12 §1`'s MSVC row for the runner pin.
- **Evidence:** Actions **green on `main`** — all three jobs succeed. Locally 39/39. Took three CI rounds; each red was a real finding, recorded below rather than smoothed over.
- **State:** green. Branch `task/M0-T6-ci`, merged to `main`. **M0 (T1–T6) is complete.**
- **Blockers:** none.
- **Notes for next session:**
  - **`windows-latest` no longer has Visual Studio 2022.** vcpkg built all six ports happily with the newer compiler and then `project()` failed with "could not find any instance of Visual Studio", because the `win-x64` preset names the "Visual Studio 17 2022" generator. Pinned `runs-on: windows-2022`, which is the toolchain `12 §1` actually specifies (MSVC 14.44). This is the principled fix, not a workaround: CI validating an unpinned, rolling toolchain defeats the purpose of a pins table. **Moving to a newer VS is a `12 §1` amendment first, then a preset change** — recorded in the MSVC row.
  - **The Linux job found two real defects on first contact with gcc, and MSVC `/W4` had shown neither.** (1) `-Wdangling-reference`: `require_table` returned a `const toml::table&` derived from a parameter, a shape gcc cannot prove safe and is right to distrust; it returns a pointer now and callers dereference at the binding site. (2) `-Wunused-function`: `weight_pct` was dead code. Removed, not silenced. Earlier in the same task I pre-emptively fixed a third class — `size_t` compared against `kGroups` (an `int`, because `04 §3`'s `groups()` returns `int`) trips `-Wsign-compare` on gcc and nothing on MSVC; `xs.h` now carries `kGroupsN` for size comparisons. **Expect gcc to keep finding things MSVC does not; that is why `11 §3` has a Linux row.**
  - **`${{ env.VCPKG_INSTALLATION_ROOT }}` silently expands to the empty string.** The `env` context sees only workflow-defined variables, not runner-provided ones. Bridge runner env vars in the shell (`export VCPKG_ROOT="${VCPKG_INSTALLATION_ROOT:-}"`), not in an `env:` block.
  - **Both CI jobs are CPU-only, deliberately.** Hosted runners carry no CUDA toolkit, so `NUKESIM_WITH_CUDA` auto-detects OFF — which is exactly the configuration `12 §2` requires to keep working. `11 §3`'s GPU row is explicitly self-hosted/on-demand and belongs to M4-T3/M4-T4, when there is GPU code to run. Rejected pulling `nvidia/cuda:13.1-devel-ubuntu22.04` for the Linux job: `12 §3` requires that tag be VERIFIED at M5-T5 first, and building CI on an unverified tag would produce failures unrelated to the code.
  - The `gate-evidence` job asserts the **committed** `artifacts/gate_reports/` exists and is still un-ignored before archiving it (QC-07). The archive is an addition, never a substitute — build artifacts expire, and an expired artifact turns "trust gates, not prose" back into prose.
  - **Next milestone: run the M1 SYNC audit before claiming anything** (`07 §SYNC`) — module APIs vs `04`/`05`, scenario paths vs `03 §4`, constants vs the appendix — and record it as `SYNC-M1`. M1-T1 (geometry) and M1-T4a (open-literature benchmarks + the cited `fast4` dataset) are both runnable and independent of each other. Per ADR-009, **M4-T1 SHOULD start immediately after M1-T1**, in parallel with M2/M3.
  - Still outstanding and easy to assume present: `canonical_hash()` (`04 §6`) is unimplemented and needs SHA-256, which is not in the vcpkg baseline. An in-tree implementation checked against the NIST vectors avoids an ADR; a crypto dependency would need one (`02 §4`).

### 2026-08-02 — session-2026-08-02-b (cont.) — SYNC-M1 + M1-T1 (geometry, tracker, canonical_hash)
- **Did:** Ran the mandatory **SYNC-M1** audit first (recorded in PROGRESS.md): found `04 §3`'s `mix()` unimplemented (extracted from `MaterialLib::load_file` and declared in `core/material`, since it takes a `Material`), and `04 §5`'s `Material::fracs` type unable to carry a species with a molar mass but no cross sections (spec amended to the implemented `Constituent` form). Then executed **M1-T1**: `src/core/geometry/` (`LayerStack`, `Tracker`, `AnalyticSphereTracker`), `src/core/hash/sha256.{h,cpp}`, and `Scenario::canonical_hash()` / `canonical_form()` — the last carried here per SYNC-M1 finding (3), since `04 §7` groups its stability matrix with the geometry test line.
- **Evidence:** **54/54 green, first run.** `-R geometry` selects 16. All six per-task probes resolve: toolchain 6, constants 11, rng 10, loaders 11, geometry 16, oracle 1. SHA-256 reproduces all four published NIST vectors including the 1,000,000-'a' multi-block case, and the streaming interface agrees with the one-shot form across an arbitrary chunk split. `canonical_hash` verified against `04 §6`'s full stability matrix — invariant under comment changes, CRLF↔LF, equivalent float spellings (`2.2`/`2.20`, `5.0e-6`/`0.000005`) and tally reordering; and it *moves* for seed, compression ratio, a layer radius, particle count, and a change to the resolved xs data.
- **State:** green. Branch `task/M1-T1-geometry`, merged to `main`; Actions green.
- **Blockers:** none.
- **Notes for next session:**
  - **SHA-256 is in-tree, deliberately.** `canonical_hash()` needs it and no vcpkg baseline port provides one; `02 §4` makes a new dependency an ADR. An in-tree implementation checked against published NIST vectors avoids one, and the usual "never roll your own crypto" caution does not bite here — this is identity and change detection, never security, and correctness is externally verifiable exactly as the Philox KATs are.
  - **`canonical_hash` hashes the PARSED struct, not the source text.** That is why every stability property in `04 §6` holds by construction rather than by careful text handling — key order, comments, CRLF and float spelling all vanish at parse time, and defaults are already materialized. The canonical form is a **flat, lexicographically sorted `key=value` projection**, not re-emitted TOML: `04 §6`'s "tables before arrays-of-tables" rule exists to make the byte string unique, which a flat sorted map achieves with no residual nesting ambiguity, and nothing ever re-parses it. Absent optionals emit an explicit `none` so that "field absent" and "field equal to the default" cannot collide. Tallies are sorted before hashing — they are a set, not a sequence.
  - **The nudge-direction test is the one to keep.** `04 §4`/MIN-14 requires nudging **along the direction of travel**, and the test asserts both directions from the same point on the pit surface. Getting it backwards returns an escaping neutron to the body it just left, which manifests as a leakage deficit — no crash, no obviously wrong number, and it would survive to a gate. M1-T2's pure-capturer leakage check is the first thing that would actually notice.
  - `locate()` treats a point exactly on a boundary as belonging to the INNER layer, and `distance_to_boundary` requires roots strictly greater than ε. Those two conventions are a pair: together they stop a particle standing on a surface from being handed a zero-length step forever. Changing one without the other reintroduces the loop.
  - **M1-T1 needed no spec amendment** — `04 §4` and `§6` were implementable as written. Worth noting after M0-T2, M0-T3-a and M0-T5 each turned up a defect: the spec's physics/geometry sections are in better shape than its data-contract examples were.
  - Per ADR-009, **M4-T1 (GPU buffers + device Philox) SHOULD start now**, in parallel with M1/M2/M3. It has everything it needs: `04 §2`'s layout, the frozen KATs, and `fork(42,1000,3) = 12597386599640143736` to reproduce on the device.

### 2026-08-02 — session-2026-08-02-b (cont.) — M1-T2 (ref/ transport, implicit capture)
- **Did:** `src/ref/ref_transport.{h,cpp}` — E1a–E1e exactly per `01 §2`: free flight on the **transport-corrected** Σ_tr, boundary advance + re-locate, collision with isotope sampled ∝ n_iΣ_t,i, weight-weighted fission production tallied, implicit-capture weight reduction `w ← w·Σ_s,i/Σ_t,i`, Russian roulette at C-902's w_min/w_surv, isotropic-in-lab scatter with the group drawn from `transfer[from][·]`. Plus `TallyAcc` (per-history moments), `SourceSpec`, and `analytic_k_inf()` as an independent oracle. Amended the M1-T2 DoD's leakage criterion and anchored every VERIFY probe.
- **Evidence:** **61/61 green.** Leakage matches `exp(−Σ_c·R)` at Σ_c·R ∈ {0.5, 2.0, 5.0}; k_inf matches `ν̄Σ_f/(Σ_c+Σ_f)` within 3σ on three media spanning k_inf ≈ 0.48 to 1.45. Anchored probes are now **provably disjoint AND exhaustive**: 6+11+1+10+11+15+7 = 61 = the total test count.
- **State:** green. Branch `task/M1-T2-ref-transport`, merged to `main`; Actions green.
- **Blockers:** none.
- **Notes for next session:**
  - **The DoD's "within 1σ" leakage criterion was unachievable, and this is the important finding.** Implemented verbatim, it failed at Σ_c·R = 5 with a 1.19σ pull. That is not a bias: leakage in a pure capturer is a plain Bernoulli with p = exp(−Σ_c·R), and the code's σ matched the analytic binomial σ to 1.5%. **A within-1σ acceptance test has a 31.7% false-failure rate per check — all three depths pass only 31.8% of the time — and more histories cannot help, because the pull is N(0,1) regardless of N.** Two tempting non-fixes were rejected: raising the history count (does nothing) and searching for a seed that passes (fitting the test to an outcome, which the spec forbids for `c_a` and which is no better here). Amended to 3σ, matching the k_inf half of the same DoD, **plus a 16-seed mean-pull bias check** — that resolves systematic error at 0.75σ with a 0.27% false-failure rate, i.e. it is strictly *more* sensitive than the criterion it replaces. **If any future DoD states a 1σ or 2σ acceptance threshold, it has the same defect.**
  - **VERIFY probes are now anchored (`ctest -R "^<module>\."`), and `11 §1` says they MUST be.** Running them exposed real collisions: `-R ref` matched `constants.runtime lookup resolves, and **ref**uses what it must`, and `-R geometry` matched `constants.canonical **geometry** radii are strictly nested`. `-R` is an unanchored regex over the whole test name and Catch2 names entries after free prose, so the alternative was policing test wording forever. Anchoring is the payoff of the `TEST_PREFIX` convention; the sum-equals-total check above is worth repeating whenever a module is added.
  - **Fission progeny are tallied but NOT propagated, deliberately.** Propagating them is a fission-source iteration, i.e. the eigen solver — M1-T3's job. What `run_fixed_source` measures is production per source neutron, which in a leakage-free medium is exactly k_inf, and that is what makes the analytic check possible at all. M1-T3 adds the bank and the outer iteration on top; `RefTransport::run_eigen` and `last_source()` from `05 §1` are **not yet declared**, by design — declaring an unimplemented method is what SYNC-M1 caught for `mix()`.
  - `rng::Stream` has no default constructor on purpose (`04 §2` makes seed and stream id required), so `Particle` is aggregate-initialized. A default-seeded stream is a determinism hazard; keep it that way.
  - One stream per history keyed by history index, so what a history sees depends on its own identity and never on execution order — the same rule `fork()` enforces for progeny (BLK-11/E2). This is what makes the OpenMP-per-history option in `05 §1` safe to add later without changing results.

### 2026-08-02 — session-2026-08-02-b (cont.) — M1-T3 (eigen: power iteration, entropy, Λ)
- **Did:** `src/ref/ref_generation.cpp` (the progeny propagation M1-T2 deliberately left out: bank `⌊w·ν̄·(Σ_f/Σ_t)/k_gen + ξ⌋` at the collision site, birth group from χ, track-length time accumulation) and `src/physics/eigen/` (power iteration E2a, fixed 8³ Shannon entropy E2b with the W=5 window, dual σ taking the LARGER of active-cycle and batched-means, `beta_eff`, Λ = ℓ/k, α = (k−1)/Λ). Group speeds derived by scaling C-031 as v ∝ √E rather than introducing a neutron mass the appendix does not carry.
- **Evidence:** **70/70 green.** Probes still disjoint and exhaustive: 6+11+1+10+11+15+7+9 = 70. Λ falls monotonically with density and Λ(2ρ)/Λ(ρ) lands inside `05 §2`'s [0.4, 0.6]. `beta_eff` on a pure Pu-239 medium equals C-022 to 1e-12, and `k_prompt` is exactly `k·(1−β_eff)`. Doubling the batch moves k by under 3σ.
- **State:** green. Branch `task/M1-T3-eigen`, merged to `main`; Actions green.
- **Blockers:** none new. **One obligation created for M1-T5 — see below.**
- **Notes for next session:**
  - **MEASURED: the entropy-window convergence test is correlation-limited, and C-901's `eig_h_tol = 1e-3` was unreachable.** σ_H came out **0.0249 at batch 2.5e3 and 0.0215 at batch 1e4** — a 4× batch increase bought a 16% noise reduction, not the 2× independent sampling predicts. The reason is structural: generation *n+1*'s source is sampled FROM generation *n*, so successive H values are strongly autocorrelated — the same inter-cycle correlation that already forces the batched-means σ for k (MAJ-32). **Raising the batch is not the fix.** I first wrote a note claiming a ~7/N floor from the flat-distribution variance formula; that formula's leading term cancels only for a *uniform* distribution, and a real fission source is peaked. Measuring disproved it, and the note in `05 §2` is now the measurement. There is a permanent regression test (`eigen.entropy noise is correlation-dominated…`) that would catch a future change making the noise scale as 1/√N — which would mean the source had stopped propagating between generations.
  - **Obligation for M1-T5, before G0a is claimed:** measure σ_H at the real gate configuration (C-900, batch 1e6) on a real benchmark, and either confirm 1e-3 is reachable or amend C-901 to the measured value. **A convergence criterion that can never fire silently degrades E2b to an `I_min`-only test** — i.e. no source-convergence test at all, while still reporting "converged". This is recorded in `05 §2`.
  - **The bad-source DoD passes without the concentrated run converging, and that is correct.** `05 §2` asks that it need ≥5 MORE inactive generations; not converging inside the budget satisfies that a fortiori. It is also the physics: spreading a point source through an optically thick sphere is a random walk taking ~(R/mfp)² generations, which for the test geometry is hundreds. The test additionally asserts H is still *climbing* at the end, so "unconverged" is distinguished from "stuck at a wrong plateau".
  - **ADR-013 held.** The solver returns `k_eff` on total ν̄ and never a β-corrected k; `k_prompt()` is the single derivation point. The test asserts the pcm gap equals `k·β·1e5` — note it scales WITH k, so an absolute pcm bound written for a critical system is wrong on a supercritical one (my first version asserted <400 pcm on a k≈2.6 sphere and failed for exactly that reason).
  - Group speeds are `C-031 · √(E_g/1 MeV)` with E_g the geometric mean of the group bounds — geometric, not arithmetic, because the group structure is log-spaced and an arithmetic midpoint sits far too high in the wide bottom group. This introduces no new constant.
  - `RefTransport::run_eigen`/`last_source()` from `05 §1` are still not declared; the solver is a free function taking the transport, which keeps it backend-agnostic for M4-T3. If a future SYNC wants the `05 §1` shape, it is a thin wrapper.

### 2026-08-02 — session-2026-08-02-b (cont.) — M1-T4a-1 (open-literature benchmark models)
- **Did:** Split M1-T4a per §8 into **-1** (benchmark models, this task) and **-2** (the cited `fast4` dataset). Authored `data/benchmarks/{godiva,jezebel}.md`, `data/materials/{u_godiva,pu_ga_jezebel}.json`, `data/scenarios/{godiva,jezebel}.toml`, `tests/unit/test_benchmarks.cpp`; replaced every `[M1-T4a]` placeholder in `08 §1`; logged **ADR-016** (Godiva) and **ADR-017** (Jezebel).
- **Evidence:** **75/75 green**, probes disjoint and exhaustive. Source is **JEFF Report 16 Annex 3** (OECD/NEA, open, retrieved 2026-08-02), which reproduces the **CSEWG Benchmark Book BNL 19302 ENDF 202 Rev. 11-81** specifications — Godiva is fast benchmark F5, Jezebel F1. **No ICSBEP Handbook access was sought or used (BLK-14).** Every derived quantity independently reproduces the approximate figures the spec previously carried: Godiva ρ 18.7421 / 52.431 kg / 93.7112–5.2686–1.0202 wt%; Jezebel ρ 15.6112 / 17.022 kg / Ga 1.0197 wt%.
- **State:** green. Branch `task/M1-T4a-1-benchmarks`, merged to `main`; Actions green.
- **Blockers:** none for -1. **M1-T4a-2 is deliberately open — see below.**
- **Notes for next session:**
  - **I split M1-T4a because one half must not be faked.** The models are retrievable and checkable; the `fast4` dataset is **~450 physical values across 16 species** and needs either a published multigroup library or ENDF through a documented collapse. Writing plausible cross sections and tagging them `PUBLIC` would make G0a/G0b compare the code against numbers I invented — the exact failure the project's provenance discipline exists to prevent. **M1-T4a-2 may legitimately end in `blocked`; that is a better outcome than a filled-in file.**
  - **Atom densities are the primary datum, not weight percentages.** The published models give nuclei/b-cm, which is what the transport consumes; ρ, mass and wt% are derived. That inversion is what turned the spec's old placeholder values into independent cross-checks — all of them reproduced. A future edit that changes a derived value without changing the atom densities is a defect, and `test_benchmarks` recomputes every card number from the committed material so the cards cannot drift.
  - **"4.5% Pu-240" has three readings and they all round the same.** 4.4710 wt% of the material, 4.5171 wt% of the plutonium, 4.4990 at% of the plutonium. Sources disagree on the basis; it does not matter here. `composition_check.Pu240_wt_pct_of_Pu` carries the second, because that is what the loader recomputes. Recorded in ADR-017 so it is not re-litigated.
  - **Jezebel's gallium is split Ga-69/Ga-71 by explicit author choice** (60.108/39.892, IUPAC), because `03 §3` forbids the *loader* expanding natural abundance — the rule exists to force exactly this recorded decision. The split reproduces the natural-Ga mass to 7 digits, so ρ is unchanged by it.
  - **Adding the first real material files exposed two latent defects in tooling I wrote earlier, both previously vacuous.** (1) `constants_roundtrip.py`'s molar-mass completeness check read a `composition` key; `03 §3` names it `isotopes`. With zero material files it had always passed; with two it failed on correct data. **A verifier with nothing to verify proves nothing about itself** — worth remembering for every other "wired but vacuous" check in this tree (`crosscheck_misuse.py` is still in that state). (2) `oracle.py` retired its layer-mass PENDING block on `any(*.json)`, so benchmark spheres would have silently satisfied a check that needs the *canonical assembly* materials; it now keys on `pu_ga_delta.json` specifically (M2-T1).
  - The benchmark scenarios carry the **C-900 gate eigen configuration** (1e6/50/200), asserted by test. C-900b is never valid for a gate claim.

### 2026-08-02 — session-2026-08-02-c — SYNC-M4 + M4-T1 (GPU backend foundation) + M1-T4a-2 → blocked
- **Did:** (1) Assessed the NEXT ACTION **M1-T4a-2** and marked it **`blocked` (owner)**: the cited isotope-complete 4-group `fast4` xs set is ~450 physical values needing a published multigroup library or ENDF through a documented collapse; no NJOY/ENDF/ACE tooling or data is present locally, BLK-14 forbids ICSBEP, and inventing the values is the one failure the provenance discipline exists to prevent. The benchmark scenarios already reference `xs_set="fast4"` (which does not yet exist), so G0a/G0b cannot run until the owner supplies data. Blocker + unblock path recorded in PROGRESS.md. (2) Ran **SYNC-M4** before entering milestone M4 (recorded in PROGRESS.md's SYNC checklist). (3) Executed **M4-T1** on the ADR-009 fallback: `src/core/hd.h` (the `NUKESIM_HD` host/device macro); device-annotated `core/rng/{philox,rng}.h` so the SAME Philox/Stream/fork serves the device (behaviour-preserving on host); `src/gpu/buffers.cuh` (RAII SoA `ParticleBuffers`: pos/dir(θ,φ)/group/weight/stream-state); `src/gpu/gpu_backend.{h,cu}` (device Philox KAT kernels; fixed-point int64 reduction; exclusive-prefix-sum progeny-slot scan; a plain-typed host bridge that keeps Catch2 out of nvcc); `tests/unit/test_gpu.cpp` (6 `gpu.` tests); CMake wiring — all `NUKESIM_WITH_CUDA`-guarded.
- **Evidence:** `cmake --preset win-x64 && cmake --build --preset win-x64-rel && ctest --preset win-x64-rel` → **81/81 passed** (75 CPU + 6 `gpu.`), 60.9 s. `ctest -R "^gpu\."` → 6/6; disjoint+exhaustive holds (75+6=81). The device reproduces the three published Random123 vectors, the frozen `uniform_f` vector **bit-for-bit**, and `fork(42,1000,3)=12597386599640143736`. The fixed-point reduction returns an **identical int64** across launch configs {64×128, 256×256, 13×97, 1024×64}; the progeny scan is identical across tile sizes {128,256,100} and matches a host scan. First build failed on nvcc error #20013 (see notes); fixed and rebuilt clean under `-Werror=all-warnings`.
- **State:** green. Branch `task/M4-T1-gpu-buffers`, claim commit `f5e9cad`, merged to `main`. Actions on `main` build CPU-only → 75, unaffected by the CUDA-guarded additions.
- **Blockers:** **M1-T4a-2** owner-blocked (cited xs data — a published multigroup library, or an authorised ENDF+collapse pipeline). Nothing blocks M4.
- **Notes for next session:**
  - **The device Philox is the SAME source as ref/, not a copy** — the only way "device KATs match CPU" is a guarantee rather than a coincidence. `NUKESIM_HD` (empty on host, `__host__ __device__` under nvcc) **plus `--expt-relaxed-constexpr`** on the `nukesim_gpu` target is what compiles `core/rng` for the device. The flag is required because MSVC's `std::array::operator[]` (Counter/Key) is `constexpr __host__`, not host-device — precisely what philox.h's "compiles for device unchanged" depends on. Do NOT replace it with a second device Philox; the KATs would then only prove self-consistency.
  - **Determinism is by construction.** The reduction accumulates in fixed-point **int64** (exactly associative + commutative), so any thread/block partition sums to the identical accumulator, with exactly ONE promotion to double at the very end. Promoting per-block to double and summing across a *varying* block count would reintroduce float non-associativity and break bit-identity across block counts — so `05 §6` item 4's "promoted to double once per block; fixed tree" is honoured in spirit (fixed-point per block) but the promotion is deferred to the end. A future SYNC may tighten that wording; clarification, not behaviour change. **FP atomicAdd is never used; integer atomicAdd is, and is exact.**
  - **`__umulhi` is deliberately NOT used yet.** M4-T1 keeps the portable `uint64` `mulhilo32` on the device so the KATs are bit-identical to the CPU by construction. Substituting `__umulhi` is the **M4-T4** perf pass and MUST re-verify the KATs afterwards (a different multiply path is exactly the change that yields plausible-but-wrong noise).
  - **Fixed-point SCALE = 2^40**, a documented numerical detail, not a physical constant (cf. the Philox multipliers, deliberately not in `constants.data.toml`). Resolution ~9.1e-13; a single-reduction total must stay < 2^23 ≈ 8.4e6, which the M4-T1 workloads honour. M4-T2's per-generation tallies should promote to double once per block per generation to bound magnitude across a full burst.
  - **M4-T2 is the clean next pick** and reuses everything here: the SoA buffers, the scan for fission-bank slots, the fixed-point reduction for tallies, and `fork` for progeny streams. Its DoD is a T-diff subset (ref↔gpu statistical, G0c criteria) on a fixed-source toy case — it does **not** need the blocked fast4 xs; a SIM/stub xs set suffices for a determinism/parity toy (as M0-T5's fixtures did).
  - **CI is CPU-only and stays 75.** Everything GPU is `NUKESIM_WITH_CUDA`-guarded and the hosted runners auto-detect it OFF. Do not add a GPU CI job here — `11 §3`'s GPU row is self-hosted/on-demand and belongs to M4-T3/M4-T4. `catch_discover_tests` lists `gpu.` cases at build time without launching kernels, so it needs no GPU to discover and is simply absent on CI.

### 2026-08-02 — session-2026-08-02-c (cont.) — M4-T2-a (device geometry + material/xs)
- **Did:** SPLIT M4-T2 → -a (this task) / -b (event transport + T-diff). Executed -a: `src/gpu/geometry.cuh` (a POD, float re-implementation of `core/geometry`'s AnalyticSphereTracker — ray-sphere, locate, inner-radius, nudge-along-direction, all host/device); `src/gpu/materials.cuh` (`DeviceMaterials`: flattened per-layer macro Σ_t/Σ_tr + per-isotope `DGroup` slots, the float mirror of `mix()`/`RefTransport::LayerData`); `src/gpu/device_data.{h,cu}` (host-callable parity bridge — uploads the CPU LayerStack/MaterialLib, runs the device tracker and a device Σ-recompute, returns results CUDA-free); two `gpu.` parity tests + a `ToyWorld` (2 isotopes, 2 materials, 2 layers, built through the real loaders). CMake: `device_data.cu` added to `nukesim_gpu`, which now links `nukesim_core`; `test_gpu` gets `NUKESIM_SOURCE_DIR`.
- **Evidence:** `ctest --preset win-x64-rel` → **83/83** (75 + 8 `gpu.`), 63.7 s; anchored counts sum exactly (6+11+1+10+11+15+7+9+5+8 = 83). Device tracker agrees with the CPU double tracker on axial/tangential/miss/inside-out rays (distance within 1e-4 rel; locate/nudge exact); device macro Σ_t/Σ_tr and νΣ_f agree with `mix()` within 1e-4 rel across both layers × 4 groups. CI CPU-only path unaffected (75).
- **State:** green. Branch `task/M4-T2-a-device-geom`, claim commit `1a…` (split), merged to `main`.
- **Blockers:** none for M4. M1-T4a-2 still owner-blocked (cited xs data).
- **Notes for next session:**
  - **The device tracker/materials are a FLOAT re-implementation, NOT a reuse** — unlike the Philox (one constexpr source). `core/geometry` carries double + std::vector + std::string; a kernel cannot. So `geometry.cuh` re-derives the SAME `04 §4` math in float, and the parity test (float vs the CPU double) is the safety net against the two drifting — cross-backend is parity, not bit-identity (`01 §9` / G0c), so this is correct, not a compromise. **A change to the CPU tracker math must be mirrored here or the parity test fails** (the intended failure direction).
  - **Two nvcc build findings, both device-only, both under `-Werror=all-warnings`:** (1) MSVC's `HUGE_VALF` macro trips nvcc **error #221** (a double literal narrowed to float) — use a `constexpr float` infinity from `std::numeric_limits` instead. (2) reusing `core/rng` on device needed `--expt-relaxed-constexpr` (M4-T1, because `std::array::operator[]` is `constexpr __host__`); the same flag covers these headers. The `.cu` **host** pass compiles at `/W4` but **without `/WX`**, so only device warnings are fatal — keep kernel code warning-clean.
  - **`DeviceMaterials` is intentionally partial after -a:** the per-isotope `DGroup` slots (nu/chi/all σ/mu_bar) + per-layer macro Σ are built and parity-tested, but `global_index` and the per-isotope `transfer` matrix are left null. They are collision-only (used AFTER isotope selection), so M4-T2-b builds them alongside the collision kernel that consumes them — the SYNC-M1 lesson (do not ship data no code reads).
  - **Device numerical detail:** the tracker boundary epsilon is `1e-4f` cm (the CPU's 1e-9 is below the float ULP near ~10 cm) and only matters for a particle sitting ON a boundary — M4-T2-b's concern, not the clean-ray parity here.
  - **M4-T2-b reuses all of this:** `geometry.cuh` (flight distance), `materials.cuh` (Σ_tr for flight, per-isotope for collision), `buffers.cuh` (SoA), `gpu_backend`'s scan (fission-bank slots) + fixed-point reduction (tallies). The `ToyWorld` in `test_gpu.cpp` (real loaders, temp files) is the toy-builder for the T-diff, and `test_ref.cpp`'s `World` is the ref side of the same problem.

### 2026-08-02 — session-2026-08-02-c (cont.) — M4-T2-b (event-based GPU transport + T-diff)
- **Did:** Executed M4-T2-b in four green increments. **transport.{h,cu}** — the event loop: SoA particles, one flight resolved per superstep (E1a–E1e implicit capture — flight on Σ_tr, boundary advance/relocate, isotope ∝ nᵢΣ_t,ᵢ, weight-weighted fission production, implicit-capture `w·=Σ_s/Σ_t`, E1e roulette, isotropic scatter with the group from `transfer`), looping until all terminate. **device_problem.cuh** — full device problem data (geometry + materials incl. transfer). Increments: (A) pure-capturer leakage T-diff; (B) scattering+fission k_inf T-diff; (C) **prefix-sum compaction** of live particles (per-tile Hillis–Steele + single-block tile-sum scan + scatter, no atomic cursor); (D) **deterministic fission bank** — ⌊production+ξ⌋ progeny per history at exclusive-prefix-sum slots with `fork` streams, exposed as size + a position-weighted checksum. Tests build toys through the real loaders (`PureCaptureWorld`, `FissionWorld`), with `ref/` as the oracle.
- **Evidence:** `ctest --preset win-x64-rel` → **86/86** (75 + 11 `gpu.`), 65 s; anchored sum 6+11+1+10+11+15+7+9+5+11 = 86. **T-diff (G0c) vs ref:** pure-capturer leaked fraction matches exp(−Σ_c·R) within 0.005 and ref within 3√(σ²+σ²); scattering/fission production-per-source matches the analytic k_inf within 3% and ref within 3σ, leakage also within 3σ. **Determinism:** leaked / k / bank-size / bank-checksum all bit-identical across launch configs {64×128, 512×256} on the fissioning (multi-superstep) medium. Fission-bank size ≈ total production within 2%. Clean under −Werror; no new nvcc findings after the earlier device fixes.
- **State:** green. Branch `task/M4-T2-b-gpu-transport` (increments A–D committed), merged to `main`.
- **Blockers:** none for M4. M1-T4a-2 still owner-blocked (cited xs data).
- **Notes for next session:**
  - **The GPU transport is validated against the CPU oracle — the first differential (G0c-style) evidence in the project.** It draws float uniforms (01 §9) with per-particle streams `fork`ed from the SOURCE INDEX (never buffer position), (ctr,sub) riding in the SoA and resumed each superstep — so the result is bit-identical across launch geometries, and ref (double) is compared STATISTICALLY only (never bit-for-bit; impossible cross-backend by design). Each superstep resolves one flight in a single branchy step kernel.
  - **Two deferrals, both principled, both in the task table:** (1) the per-event **branchless-kernel split** behind the partition is a warp-divergence optimisation → **M4-T4** (02 §4 profile-first); the single step kernel is correct + deterministic. (2) the fission bank is BUILT here (deterministic slots + fork, tested) but **not propagated** — fixed-source does not iterate the fission source (ref's `run_fixed_source` doesn't either); PROPAGATION is the eigen power iteration → **M4-T3**.
  - **The fission bank uses a SEPARATE per-history stream** (`fork(kFissionBase,0,p)`) for the progeny-count ξ, so it does not perturb the transport RNG — building it leaves the T-diff results unchanged. Its determinism (slots by prefix-sum, streams by fork, count by id-keyed ξ) is what the **position-weighted** checksum guards; a plain sum would miss a slot-ordering bug.
  - **`gpu_fixed_source` bounds `histories` ≤ ~1.05e6** (kTile²=1024²): the compaction scan is two-level (per-tile, then a single-block scan of the tile sums), capping live-per-superstep at kTile². The gate batch (C-900, 1e6) sits just under this; a larger batch needs a three-level or device-wide scan.
  - **M4-T3 reuses everything:** `DeviceProblem`, the step physics, the compaction scan, and the fission bank. The eigen path is: run a generation banking progeny, propagate the bank as the next generation's source, iterate E2a with the fixed-8³ entropy E2b on the GPU, and drive the backend-agnostic `physics/eigen` solver. The `RefTransport`-shaped `GpuTransport` interface (`05 §1`) can finally be **declared** — it now has an implementation behind it (the SYNC-M1 rule: never declare an unimplemented method).

### 2026-08-02 — session-2026-08-02-c (cont.) — M4-T3-a (GPU k-eigenvalue power iteration) + M4-T3 SPLIT
- **Did:** SPLIT M4-T3 → -a (GPU eigen path + differential, this task) / -b (the FORMAL G0c gate via `nukebench`, blocked on M1-T5). Executed -a: `src/gpu/eigen.{h,cu}` — the fission-source iteration on the GPU. Each generation, one thread transports one source neutron to completion (history-per-thread), RESERVOIR-samples one fission site weighted by per-collision production (from a separate fission stream, so the transport RNG is unperturbed) with birth group from χ, and emits ⌊production/k + ξ⌋ progeny there; the progeny bank at exclusive-prefix-sum slots (reusing `gpu_backend::progeny_offsets`) with `fork` streams and become the next generation's source. k iterates as production/source (E2a); the fixed-8³ source entropy is the E2b signal. Two `gpu.` tests: determinism+sanity, and the **differential vs `ref/`'s `physics/eigen`** on a finite Pu sphere.
- **Evidence:** `ctest --preset win-x64-rel` → **88/88** (75 + 13 `gpu.`), 67 s; anchored sum …+13 = 88. GPU eigen k matches ref eigen k on the 12 cm Pu sphere within a toy-batch tolerance (0.92 s for the pair). k / source-checksum / entropy all **bit-identical** across launch configs {64×128, 256×256}. Clean under −Werror after renaming the `k_gen` kernel (it collided with a host variable of the same name → the launch would have resolved to the float).
- **State:** green. Branch `task/M4-T3-gpu-eigen` (increments A–B committed), merged to `main`.
- **Blockers:** **M4-T3-b (formal G0c gate) is blocked** on M1-T5 (`nukebench`+`gates.toml`), which is blocked on M1-T4a-2. **M1-T4a-2 is now the project critical path** — it gates M1-T5, all formal gate reports (G0a/G0b/G0c), and M2.
- **Notes for next session:**
  - **The GPU eigen is a self-contained power iteration, not (yet) the shared backend-agnostic solver `05 §2` describes.** It is validated to MATCH `ref/`'s `physics/eigen` (the differential), which is what G0c actually checks. Unifying the two behind one `Transport` interface (so `run_eigen` drives either backend) is a clean refactor — worthwhile, not blocking.
  - **Fission-site RESERVOIR sampling (one site per source neutron) is a deliberate simplification that keeps k UNBIASED** — the eigenvalue is production/source, insensitive to intra-neutron progeny clustering, and the reservoir samples sites ∝ fission density (the fundamental-mode source) — while avoiding per-collision spatial banking (which needs per-particle progeny buffers). It inflates source variance, so the 8³ entropy is approximate; adequate for E2b's convergence signal. A full per-collision source is a fidelity upgrade if a gate needs it.
  - **The differential uses a toy-batch relative tolerance (3%), NOT the strict G0c ≤100 pcm.** At batch 4000 the successive-cycle correlation inflates the true σ well beyond the naive active-cycle estimate (the MAJ-32 effect), so a tight absolute bound would false-fail. The strict G0c (≤100 pcm at C-900 batch, via `nukebench diff` → committed report) is **M4-T3-b**, blocked on M1-T5.
  - **`gpu_eigen`'s history-per-thread transport is a divergence hotspot** (deep-scattering neutrons stall a warp); M4-T4 should swap it for the event-based superstep transport (M4-T2-b) + the per-event branchless-kernel split, and move the per-generation host round-trips (progeny counts / production / entropy) onto the device.
  - **M4 status: T1, T2-a, T2-b, T3-a done; T3-b blocked (nukebench); T4 (perf) is the last unblocked M4 task.** After M4-T4, M4 is done except the formal gate reports, which wait on M1-T4a-2.
